{
  "version": "3.0.0",
  "generatedAt": "2026-02-03T16:06:53.158Z",
  "mode": "ai-enriched",
  "stats": {
    "totalHooks": 99,
    "totalComponents": 101,
    "componentsWithContext": 60,
    "totalCategories": 13
  },
  "categories": [
    {
      "name": "General",
      "patterns": [
        {
          "name": "useApiKeyManager",
          "description": "Hook for useApiKeyManager",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Handles the lifecycle of Orderly API keys, including generation, retrieval, and revocation. Essential for building user settings pages where users manage their bot access or third-party integrations.",
          "example": "import { useApiKeyManager } from '@orderly.network/hooks';\n\nexport const ApiKeySettings = () => {\n  // Destructure the manager to handle creation and listing of keys\n  const { \n    createApiKey, \n    getApiKeys, \n    deleteApiKey, \n    isLoading, \n    apiKeys \n  } = useApiKeyManager();\n\n  const handleCreateNewKey = async () => {\n    try {\n      // Create a key with specific permissions\n      await createApiKey({\n        label: 'Trading Bot Key',\n        // Enable specific scopes based on your app's needs\n        scopes: ['read', 'trade'] \n      });\n      // Refresh the list after creation\n      await getApiKeys();\n    } catch (error) {\n      console.error('Failed to create API Key', error);\n    }\n  };\n\n  return (\n    <div className=\"p-4 border rounded\">\n      <h3>API Key Management</h3>\n      <button \n        onClick={handleCreateNewKey}\n        disabled={isLoading}\n        className=\"px-4 py-2 bg-blue-500 text-white rounded\"\n      >\n        {isLoading ? 'Generating...' : 'Generate New Key'}\n      </button>\n\n      <ul className=\"mt-4\">\n        {apiKeys?.map((key) => (\n          <li key={key.key_id} className=\"flex justify-between items-center\">\n            <span>{key.label}</span>\n            <button \n              onClick={() => deleteApiKey(key.key_id)}\n              className=\"text-red-500\"\n            >\n              Revoke\n            </button>\n          </li>\n        ))}\n      </ul>\n    </div>\n  );\n};",
          "notes": [
            "Always refresh the key list (getApiKeys) immediately after creating or deleting a key to keep the UI in sync.",
            "Handle the isLoading state carefully, as key generation involves cryptographic operations that may take a moment.",
            "Be explicit with scopes (e.g., 'read' vs 'trade') to ensure the principle of least privilege for user security."
          ],
          "related": ["useAccountInfo", "useOrderlyContext"]
        },
        {
          "name": "useApiStatusActions",
          "description": "Hook for useApiStatusActions",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Provides actions to control and monitor the connection status to the Orderly Network API. Useful for implementing global connection indicators, health checks, or handling maintenance modes.",
          "example": "import { useApiStatusActions } from '@orderly.network/hooks';\n\nexport const ConnectionStatus = () => {\n  // Access actions to manipulate the global API status state\n  const { checkHealth, setMaintenanceMode, isConnected } = useApiStatusActions();\n\n  return (\n    <div className=\"flex items-center gap-4 p-4 bg-gray-100 rounded\">\n      <div className=\"flex items-center gap-2\">\n        <span className={`w-3 h-3 rounded-full ${isConnected ? 'bg-green-500' : 'bg-red-500'}`} />\n        <span>API Status: {isConnected ? 'Connected' : 'Disconnected'}</span>\n      </div>\n\n      <button \n        onClick={checkHealth}\n        className=\"px-3 py-1 text-sm border rounded hover:bg-gray-50\"\n      >\n        Re-check Health\n      </button>\n\n      {/* Example of manually toggling maintenance mode for testing */}\n      <button \n        onClick={() => setMaintenanceMode(true)}\n        className=\"px-3 py-1 text-sm text-red-600 border border-red-200 rounded\"\n      >\n        Simulate Maintenance\n      </button>\n    </div>\n  );\n};",
          "notes": [
            "This hook returns actions, not just state; use it to trigger events like reconnection or health checks.",
            "It is often used in conjunction with a status selector to display the current state (connected/disconnected) to the user.",
            "Use setMaintenanceMode to gracefully block user actions when the network is scheduled for downtime."
          ],
          "related": ["useAccountInfo", "useOrderlyContext"]
        },
        {
          "name": "useDataCenterContext",
          "description": "Hook for useDataCenterContext",
          "installation": "npm install @orderly.network/hooks",
          "usage": "This hook exposes the Data Center, which acts as a local cache for market configurations, symbol metadata, and other static data required for trading operations.",
          "example": "import { useDataCenterContext } from '@orderly.network/hooks';\n\nexport const MarketInfo = ({ symbol }: { symbol: string }) => {\n  // Access static or semi-static market data (symbols, configs, tickers)\n  const { getSymbolInfo } = useDataCenterContext();\n\n  const marketData = getSymbolInfo(symbol);\n\n  if (!marketData) return <div>Market {symbol} not found</div>;\n\n  return (\n    <div className=\"flex gap-4\">\n      <span>Symbol: {marketData.symbol}</span>\n      <span>Min Size: {marketData.min_size}</span>\n      <span>Tick Size: {marketData.tick_size}</span>\n    </div>\n  );\n};",
          "notes": [
            "Useful for retrieving trading rules (min order size, tick size) before placing orders.",
            "Avoids redundant API calls by keeping market configuration data in context.",
            "Helper functions like `getSymbolInfo` are commonly exposed here."
          ],
          "related": ["useOrderlyContext", "useMarkets", "useSymbolInfo"]
        },
        {
          "name": "useCommission",
          "description": "Hook for useCommission",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Retrieves commission statistics for the connected user, specifically focusing on the trading volume generated which determines commission levels or payouts.",
          "example": "import { useCommission } from '@orderly.network/hooks';\n\nexport const ReferralStats = () => {\n  // Fetches commission data, specifically returning volume metrics.\n  const { data, isLoading } = useCommission();\n\n  if (isLoading) return <div>Loading commission stats...</div>;\n\n  return (\n    <div className=\"bg-blue-50 p-4 rounded\">\n      <h3 className=\"font-semibold\">Commission Volume</h3>\n      {/* The hook specifically returns volume data related to referrals */}\n      <p className=\"text-2xl font-bold\">\n        {data?.volume ? parseFloat(data.volume).toFixed(2) : '0.00'}\n      </p>\n      <p className=\"text-xs text-gray-500\">Total Trading Volume Generated</p>\n    </div>\n  );\n};",
          "notes": [
            "Returns a `volume` property, usually representing the 30-day or total volume.",
            "Data is often used to calculate VIP tiers or referral rebate percentages.",
            "Requires the user to be part of the referral program to return meaningful data."
          ],
          "related": ["useRefereeInfo", "useReferralRebateSummary", "useAccountInfo"]
        },
        {
          "name": "useRefereeInfo",
          "description": "Hook for useRefereeInfo",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Retrieves a list of accounts (referees) that have been referred by the currently connected wallet. This is used for tracking referral network growth.",
          "example": "import { useRefereeInfo } from '@orderly.network/hooks';\n\nexport const MyReferrals = () => {\n  // Fetches information about users who registered using your referral code.\n  const { data: referees, isLoading } = useRefereeInfo();\n\n  if (isLoading) return <div>Loading referrals...</div>;\n\n  return (\n    <div>\n      <h3>My Referees ({referees?.length || 0})</h3>\n      <ul>\n        {referees?.map((referee: any, index: number) => (\n          <li key={index} className=\"py-2 border-b\">\n            {/* Displaying referee details (structure depends on API response) */}\n            <span>User: {referee.user_address || 'Unknown'}</span>\n            <span className=\"ml-4 text-gray-500\">\n              Registered: {new Date(referee.created_at).toLocaleDateString()}\n            </span>\n          </li>\n        ))}\n      </ul>\n    </div>\n  );\n};",
          "notes": [
            "Returns an array of referee objects containing details like registration date or status.",
            "Useful for building a 'Referral Dashboard' to show network size.",
            "May return an empty array if the user has not referred anyone yet."
          ],
          "related": ["useCommission", "useReferralRebateSummary", "useReferralCode"]
        },
        {
          "name": "useAllBrokers",
          "description": "Hook for useAllBrokers",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Fetches a comprehensive list of all brokers registered within the Orderly Network. This is essential for onboarding flows where a user needs to select or switch between different broker partners.",
          "example": "import { useAllBrokers } from '@orderly.network/hooks';\n\nexport const BrokerSelector = () => {\n  // Fetches the list of all available brokers on the network\n  const { data: brokers, isLoading, error } = useAllBrokers();\n\n  if (isLoading) return <div>Loading brokers...</div>;\n  if (error) return <div>Failed to load brokers.</div>;\n\n  return (\n    <select className=\"broker-select\">\n      <option value=\"\">Select a Broker</option>\n      {brokers?.map((broker) => (\n        <option key={broker.broker_id} value={broker.broker_id}>\n          {broker.name}\n        </option>\n      ))}\n    </select>\n  );\n};",
          "notes": [
            "The returned data typically includes broker IDs, names, and metadata like logos.",
            "This data is relatively static, so it is often cached aggressively by the SDK.",
            "Useful for building a 'Switch Broker' interface or displaying partner attribution."
          ],
          "related": ["useAccount", "useConfig"]
        },
        {
          "name": "useCurEpochEstimate",
          "description": "Hook for useCurEpochEstimate",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Calculates and returns the estimated trading rewards or fee rebates a user will receive for the current epoch (time period). It factors in trading volume and holding tiers.",
          "example": "import { useCurEpochEstimate } from '@orderly.network/hooks';\n\nexport const RewardsEstimateCard = () => {\n  // Provides an estimate of rewards for the current epoch based on user activity\n  const { data: estimate, isLoading } = useCurEpochEstimate();\n\n  return (\n    <div className=\"reward-card\">\n      <h3>Current Epoch Rewards</h3>\n      {isLoading ? (\n        <span>Calculating...</span>\n      ) : (\n        <p className=\"highlight\">\n          Estimated: {estimate?.rewardToken} {estimate?.estimatedAmount}\n        </p>\n      )}\n    </div>\n  );\n};",
          "notes": [
            "The estimate is dynamic and updates as the user trades or epoch parameters change.",
            "Returns '0' or null if the user has no activity in the current epoch.",
            "Do not use this for final payout logic; it is purely for estimation/UI display."
          ],
          "related": ["useAccount", "useFeeState"]
        },
        {
          "name": "useCalculatorService",
          "description": "Hook for useCalculatorService",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Provides a utility service for performing complex trading calculations. This includes estimating total cost, price impact, and margin requirements before an order is placed.",
          "example": "import { useCalculatorService } from '@orderly.network/hooks';\n\nexport const TradePreview = ({ symbol, side, quantity, price }) => {\n  // Accesses the calculator service to estimate costs, impact, and fees\n  const calculator = useCalculatorService();\n\n  const calculateTotal = () => {\n    if (!calculator || !quantity || !price) return 0;\n    \n    // Example calculation logic provided by the service\n    return calculator.calculateTotalCost(symbol, quantity, price);\n  };\n\n  return (\n    <div className=\"trade-preview\">\n      <p>Estimated Cost: ${calculateTotal().toFixed(2)}</p>\n    </div>\n  );\n};",
          "notes": [
            "This hook returns a service object, not just data. You call methods on it.",
            "Essential for building 'Order Preview' modals to show users exactly what they will spend.",
            "Calculations are based on current order book depth and market conditions."
          ],
          "related": ["useFeeState", "useAccount"]
        },
        {
          "name": "useConfig",
          "description": "Hook for useConfig",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Reads the global configuration object passed to the OrderlyProvider. It contains environment variables, API endpoints, and feature flags necessary for the SDK to function.",
          "example": "import { useConfig } from '@orderly.network/hooks';\n\nexport const NetworkStatus = () => {\n  // Retrieves global configuration settings for the SDK environment\n  const config = useConfig();\n\n  return (\n    <div className=\"network-info\">\n      <h4>Environment: {config?.networkId}</h4>\n      <p>Broker ID: {config?.brokerId}</p>\n      <p>Chain ID: {config?.chainId}</p>\n    </div>\n  );\n};",
          "notes": [
            "Values are read-only at the component level; they are set when initializing the app.",
            "Useful for conditional rendering based on the environment (e.g., Testnet vs Mainnet).",
            "Contains domain settings for API endpoints."
          ],
          "related": ["useAccount", "useKeyStore"]
        },
        {
          "name": "useDatabaseInitialization",
          "description": "Hook for useDatabaseInitialization",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Manages the initialization of the local IndexedDB used by Orderly for caching order history, balances, and account settings to improve performance and offline capability.",
          "example": "import { useDatabaseInitialization } from '@orderly.network/hooks';\n\nexport const AppWrapper = ({ children }) => {\n  // Ensures the local IndexedDB is initialized before rendering app features\n  const { isInitialized, error } = useDatabaseInitialization();\n\n  if (error) {\n    return <div>Database Error. Please clear storage.</div>;\n  }\n\n  if (!isInitialized) {\n    return <div>Initializing local storage...</div>;\n  }\n\n  // Only render children when DB is ready\n  return <>{children}</>;\n};",
          "notes": [
            "Must be checked before trying to access historical data or local cache.",
            "Usually runs once on application mount.",
            "If initialization fails, the app may need to prompt the user to clear site data."
          ],
          "related": ["useAccount", "useEventEmitter"]
        },
        {
          "name": "useEventEmitter",
          "description": "Hook for useEventEmitter",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Provides access to the Orderly SDK's event bus. It allows components to subscribe to real-time events such as order updates, account changes, or socket connection status.",
          "example": "import { useEffect } from 'react';\nimport { useEventEmitter } from '@orderly.network/hooks';\nimport { toast } from 'react-toastify';\n\nexport const NotificationListener = () => {\n  const emitter = useEventEmitter();\n\n  useEffect(() => {\n    if (!emitter) return;\n\n    // Listen for specific SDK events\n    const handleOrderFill = (data) => {\n      toast.success(`Order filled: ${data.quantity} @ ${data.price}`);\n    };\n\n    emitter.on('ORDER_FILLED', handleOrderFill);\n\n    // Cleanup listener on unmount\n    return () => {\n      emitter.off('ORDER_FILLED', handleOrderFill);\n    };\n  }, [emitter]);\n\n  return null; // This component is logic-only\n};",
          "notes": [
            "Always remove event listeners in the useEffect cleanup to prevent memory leaks.",
            "Crucial for real-time UI updates without constantly polling the API.",
            "Common events include 'ACCOUNT_UPDATE', 'ORDER_FILLED', and 'POSITION_UPDATE'."
          ],
          "related": ["useAccount", "useDatabaseInitialization"]
        },
        {
          "name": "useFeeState",
          "description": "Hook for useFeeState",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Retrieves the current fee rates applicable to the user's account. These rates often change based on trading volume, staking tiers, or holding specific tokens.",
          "example": "import { useFeeState } from '@orderly.network/hooks';\n\nexport const FeeDisplay = () => {\n  // Fetches the current fee rates (taker/maker) for the connected account\n  const { data: feeState, isLoading } = useFeeState();\n\n  if (isLoading) return <div>--</div>;\n\n  return (\n    <div className=\"fee-info\">\n      <span>Maker Fee: {(feeState?.makerRate * 100).toFixed(4)}%</span>\n      <span>Taker Fee: {(feeState?.takerRate * 100).toFixed(4)}%</span>\n    </div>\n  );\n};",
          "notes": [
            "Returns rates as decimals (e.g., 0.0005 for 0.05%), usually requiring formatting for display.",
            "Updates automatically when the user's volume tier changes.",
            "Useful for calculating precise costs in the trade preview."
          ],
          "related": ["useCalculatorService", "useCurEpochEstimate"]
        },
        {
          "name": "useKeyStore",
          "description": "Hook for useKeyStore",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Handles the storage and retrieval of the user's private key (or scoped key) used to sign transactions. It abstracts the security logic of keeping keys accessible only to the SDK.",
          "example": "import { useKeyStore } from '@orderly.network/hooks';\n\nexport const WalletStatus = () => {\n  // Manages the private key storage and signing logic\n  const { keyStore, isConnected } = useKeyStore();\n\n  return (\n    <div className=\"wallet-status\">\n      <h3>Key Store Status</h3>\n      <p>Connected: {isConnected ? 'Yes' : 'No'}</p>\n      {keyStore && (\n        <p>Address: {keyStore.address.slice(0, 6)}...{keyStore.address.slice(-4)}</p>\n      )}\n    </div>\n  );\n};",
          "notes": [
            "This hook interacts with the browser's secure storage (LocalStorage/SessionStorage depending on implementation).",
            "Never log the raw keyStore object to the console in production.",
            "It is the bridge between the user's wallet (like MetaMask) and the Orderly off-chain signing."
          ],
          "related": ["useAccount", "useConfig"]
        },
        {
          "name": "useNetworkInfo",
          "description": "Hook for useNetworkInfo",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Retrieves information about the currently connected blockchain network. This is essential for ensuring the user's wallet is connected to the correct chain (e.g., Arbitrum Sepolia vs Mainnet) supported by Orderly.",
          "example": "import { useNetworkInfo } from '@orderly.network/hooks';\n\nexport const NetworkDisplay = () => {\n  // Destructure the network ID and detailed info object\n  const { id, info } = useNetworkInfo();\n\n  return (\n    <div className=\"network-status\">\n      <h3>Network Status</h3>\n      <p>Chain ID: <strong>{id}</strong></p>\n      <p>Network Name: <strong>{info?.name}</strong></p>\n      {/* Example: Conditionally rendering based on network */}\n      {id === 42161 ? (\n        <span className=\"badge\">Arbitrum One</span>\n      ) : (\n        <span className=\"badge warning\">Unknown Network</span>\n      )}\n    </div>\n  );\n};",
          "notes": [
            "Use this hook to validate that the user's wallet is on the correct chain before allowing transactions.",
            "The `info` object often contains metadata like the chain name, native currency symbol, and RPC URL.",
            "It automatically reacts to wallet network changes (e.g., user switching networks in MetaMask)."
          ],
          "related": ["useAccount", "usePreLoadData"]
        },
        {
          "name": "useParamsCheck",
          "description": "Hook for useParamsCheck",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Validates trading parameters (such as price, quantity, and symbol) against Orderly's requirements locally before making a network request. This helps prevent unnecessary API errors.",
          "example": "import { useParamsCheck } from '@orderly.network/hooks';\n\nexport const OrderValidation = () => {\n  // Hypothetical order parameters\n  const orderParams = {\n    symbol: \"PERP_ETH_USDC\",\n    side: \"BUY\",\n    order_type: \"LIMIT\",\n    price: \"2500.00\",\n    quantity: \"0.1\"\n  };\n\n  // Check if parameters are valid before sending to API\n  const checkResult = useParamsCheck(orderParams);\n\n  const handleSubmit = () => {\n    if (checkResult.valid) {\n      console.log('Parameters are valid, proceeding...');\n    } else {\n      console.error('Validation failed:', checkResult.message);\n    }\n  };\n\n  return (\n    <button onClick={handleSubmit}>\n      Place Order\n    </button>\n  );\n};",
          "notes": [
            "Performs client-side validation to catch user input errors instantly.",
            "Reduces load on the API by rejecting malformed requests early in the UI flow.",
            "Always pair this with backend validation, but use this for immediate UI feedback."
          ],
          "related": ["usePoster", "useAccount"]
        },
        {
          "name": "usePoster",
          "description": "Hook for usePoster",
          "installation": "npm install @orderly.network/hooks",
          "usage": "A utility hook for sending authenticated HTTP POST requests to the Orderly API. It handles the complexities of signing requests with the user's private key (or broker key) automatically.",
          "example": "import { usePoster } from '@orderly.network/hooks';\n\nexport const OrderSubmitter = () => {\n  // usePoster provides a function to send authenticated POST requests\n  const poster = usePoster();\n\n  const createOrder = async () => {\n    try {\n      const response = await poster('/v1/order', {\n        symbol: 'PERP_BTC_USDC',\n        side: 'BUY',\n        order_type: 'MARKET',\n        quantity: '0.01'\n      });\n      \n      console.log('Order submitted successfully:', response.data);\n    } catch (error) {\n      console.error('Order submission failed:', error);\n    }\n  };\n\n  return <button onClick={createOrder}>Execute Trade</button>;\n};",
          "notes": [
            "Abstracts away the cryptographic signing process required for private endpoints.",
            "Essential for any write operations such as placing orders, canceling orders, or updating leverage.",
            "Ensure the user is authenticated (via useAccount) before calling this to avoid signature errors."
          ],
          "related": ["useAccount", "useWallet"]
        },
        {
          "name": "usePreLoadData",
          "description": "Hook for usePreLoadData",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Manages the initial data fetching phase of the application. It ensures that critical data (like account info, allowances, and config) is loaded before the main interface is rendered.",
          "example": "import { usePreLoadData } from '@orderly.network/hooks';\n\nexport const AppInitializer = ({ children }) => {\n  const { error, done } = usePreLoadData();\n\n  if (error) {\n    return (\n      <div className=\"error-screen\">\n        <h2>Initialization Failed</h2>\n        <p>Please refresh the page or check your connection.</p>\n      </div>\n    );\n  }\n\n  if (!done) {\n    return (\n      <div className=\"loading-screen\">\n        <p>Setting up Orderly Network...</p>\n      </div>\n    );\n  }\n\n  // Render the main app only after data is preloaded\n  return <main>{children}</main>;\n};",
          "notes": [
            "Prevents the UI from rendering in a broken state while essential data is still fetching.",
            "Returns a `done` boolean to easily toggle loading spinners or splash screens.",
            "Handles global initialization errors gracefully so the app can display a fallback UI."
          ],
          "related": ["useAccount", "useNetworkInfo"]
        },
        {
          "name": "useRestrictedInfo",
          "description": "Hook for useRestrictedInfo",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Checks if the user's IP address or jurisdiction falls under restricted regions (e.g., OFAC sanctioned countries). This is crucial for compliance (KYC/AML) in DeFi applications.",
          "example": "import { useRestrictedInfo } from '@orderly.network/hooks';\n\nexport const RegionGuard = () => {\n  const { invalidRegions } = useRestrictedInfo();\n\n  // If the user's region is flagged as restricted\n  if (invalidRegions) {\n    return (\n      <div className=\"restriction-banner\">\n        <h3>Service Unavailable</h3>\n        <p>Trading is not available in your current region due to regulatory restrictions.</p>\n      </div>\n    );\n  }\n\n  return (\n    <div className=\"trading-interface\">\n      {/* Main trading UI goes here */}\n      <p>Welcome to the trading terminal.</p>\n    </div>\n  );\n};",
          "notes": [
            "Automatically detects the user's region based on their IP address.",
            "Use this to block access to specific features or the entire application to remain compliant.",
            "It is a read-only check; blocking the UI is the responsibility of the developer."
          ],
          "related": ["useAccount", "usePreLoadData"]
        },
        {
          "name": "useTrack",
          "description": "Hook for useTrack",
          "installation": "npm install @orderly.network/hooks",
          "usage": "A helper hook for tracking user events and analytics. It standardizes how events are sent to the analytics provider configured within the Orderly SDK.",
          "example": "import { useTrack } from '@orderly.network/hooks';\n\nexport const TradeButton = () => {\n  // Returns a tracking function to log events\n  const track = useTrack();\n\n  const handleClick = () => {\n    // Log a custom event for analytics\n    track('trade_button_clicked', {\n      symbol: 'PERP_ETH_USDC',\n      button_location: 'header',\n      timestamp: Date.now()\n    });\n    \n    console.log('Trade logic executed...');\n  };\n\n  return <button onClick={handleClick}>Buy Now</button>;\n};",
          "notes": [
            "Standardizes event tracking across the application, making analytics consistent.",
            "Useful for monitoring user behavior, conversion funnels, and debugging UI interactions.",
            "Accepts an event name and a payload object for custom metadata."
          ],
          "related": ["useTrackingInstance", "useAccount"]
        },
        {
          "name": "useTrackingInstance",
          "description": "Hook for useTrackingInstance",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Provides direct access to the underlying analytics instance (e.g., Segment, Google Analytics) used by the SDK. This allows for advanced analytics operations beyond simple event tracking.",
          "example": "import { useTrackingInstance } from '@orderly.network/hooks';\n\nexport const CustomAnalytics = () => {\n  // Access the raw tracking instance (e.g., Segment, Analytics.js)\n  const trackingInstance = useTrackingInstance();\n\n  const identifyUser = (userId) => {\n    if (trackingInstance) {\n      // Directly calling methods on the underlying analytics library\n      trackingInstance.identify(userId, {\n        plan: 'premium',\n        platform: 'web'\n      });\n    }\n  };\n\n  return <button onClick={() => identifyUser('user_123')}>Identify User</button>;\n};",
          "notes": [
            "Use this when you need low-level control over the analytics library (e.g., group identification, aliasing).",
            "Allows you to integrate custom plugins or middleware into the analytics pipeline.",
            "Ensure the instance exists before calling methods to avoid runtime errors."
          ],
          "related": ["useTrack", "useAccount"]
        }
      ]
    },
    {
      "name": "Order Management",
      "patterns": [
        {
          "name": "useOrderEntry",
          "description": "Hook for useOrderEntry",
          "installation": "npm install @orderly.network/hooks",
          "usage": "A legacy hook for handling order submission logic. It manages the local state of an order form and provides a function to submit the order to the Orderly Network.",
          "example": "import { useOrderEntry } from '@orderly.network/hooks';\n\n/**\n * Note: This hook is marked as deprecated in the source path.\n * Consider using useOrderEntryFromStore or useOrderStore for new implementations.\n * This example demonstrates legacy usage.\n */\nexport const LegacyOrderForm = () => {\n  const { \n    submitOrder, \n    orderState, \n    updateOrderState \n  } = useOrderEntry();\n\n  const handleSubmit = async () => {\n    // Basic validation before submission\n    if (!orderState.symbol || !orderState.amount) return;\n\n    try {\n      await submitOrder({\n        symbol: orderState.symbol,\n        side: orderState.side, // 'BUY' or 'SELL'\n        order_type: orderState.orderType, // 'MARKET', 'LIMIT'\n        order_quantity: orderState.amount,\n        // price is required for LIMIT orders\n        order_price: orderState.orderType === 'LIMIT' ? orderState.price : undefined \n      });\n    } catch (err) {\n      console.error('Order submission failed', err);\n    }\n  };\n\n  return (\n    <div className=\"flex flex-col gap-2\">\n      <input \n        placeholder=\"Amount\" \n        onChange={(e) => updateOrderState({ amount: e.target.value })} \n      />\n      <button onClick={handleSubmit}>Place Order</button>\n    </div>\n  );\n};",
          "notes": [
            "This hook is deprecated; prefer the newer store-based hooks (useOrderStore) for better state management and performance.",
            "It combines form state and submission logic, which can make testing harder compared to separating concerns.",
            "Ensure you check if the order type is 'LIMIT' before including the price field in the submission payload."
          ],
          "related": ["useOrderStore", "useOrderEntryFromStore", "useOrderEntity"]
        },
        {
          "name": "useOrderEntryFromStore",
          "description": "Hook for useOrderEntryFromStore",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Reads the global state of the order entry form (symbol, price, amount, side, etc.) from the central store. Ideal for summary components or validation logic that needs to react to form changes.",
          "example": "import { useOrderEntryFromStore } from '@orderly.network/hooks';\n\n/**\n * This hook reads directly from the Zustand store managing order entry state.\n * It is useful for components that need to display the current form state\n * without necessarily modifying it.\n */\nexport const OrderSummaryDisplay = () => {\n  // Select specific slices of state from the order entry store\n  const { \n    symbol, \n    price, \n    amount, \n    side, \n    orderType \n  } = useOrderEntryFromStore();\n\n  const total = price && amount ? (parseFloat(price) * parseFloat(amount)).toFixed(2) : '0.00';\n\n  return (\n    <div className=\"p-4 bg-gray-50 rounded border border-gray-200\">\n      <h4 className=\"font-bold mb-2\">Order Summary</h4>\n      <div className=\"grid grid-cols-2 gap-2 text-sm\">\n        <span>Symbol:</span> <span className=\"text-right font-mono\">{symbol || '-'}</span>\n        \n        <span>Side:</span> \n        <span className={`text-right font-bold ${side === 'BUY' ? 'text-green-600' : 'text-red-600'}`}>\n          {side || '-'}\n        </span>\n        \n        <span>Type:</span> <span className=\"text-right\">{orderType || '-'}</span>\n        <span>Price:</span> <span className=\"text-right font-mono\">{price || '-'}</span>\n        <span>Amount:</span> <span className=\"text-right font-mono\">{amount || '-'}</span>\n        \n        <span className=\"font-bold border-t pt-1\">Total:</span> \n        <span className=\"text-right font-bold border-t pt-1 font-mono\">{total}</span>\n      </div>\n    </div>\n  );\n};",
          "notes": [
            "This is a read-heavy hook; use it to display data. For modifying inputs, prefer useOrderStore or specific actions.",
            "It automatically subscribes to store updates, ensuring the UI re-renders whenever the order form changes.",
            "Useful for calculating derived values (like total cost or margin requirements) in real-time as the user types."
          ],
          "related": ["useOrderStore", "useOrderEntity", "useAccountInfo"]
        },
        {
          "name": "useOrderEntity",
          "description": "Hook for useOrderEntity",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Provides metadata about the current order entity, specifically the maximum quantity (`maxQty`) and the current `symbol`. This is critical for input validation and UI constraints.",
          "example": "import { useOrderEntity } from '@orderly.network/hooks';\n\nexport const QuantityInput = () => {\n  // Returns specific metadata about the current trading context\n  const { maxQty, symbol } = useOrderEntity();\n\n  return (\n    <div className=\"flex flex-col gap-1\">\n      <label className=\"text-xs text-gray-500 flex justify-between\">\n        <span>Quantity ({symbol})</span>\n        <span>Max: {maxQty}</span>\n      </label>\n      \n      <input\n        type=\"number\"\n        // Use maxQty to prevent user from entering impossible order sizes\n        max={maxQty}\n        step=\"0.01\"\n        className=\"border p-2 rounded\"\n        placeholder=\"0.00\"\n      />\n      \n      <p className=\"text-[10px] text-gray-400\">\n        Order size cannot exceed {maxQty} for this asset.\n      </p>\n    </div>\n  );\n};",
          "notes": [
            "Use maxQty to set the 'max' attribute on HTML inputs or to validate form data before submission.",
            "The symbol returned reflects the currently selected trading pair in the order entry store.",
            "This hook helps prevent API errors caused by submitting orders larger than the allowed limit for the specific asset."
          ],
          "related": ["useOrderEntryFromStore", "useOrderStore", "useAccountInfo"]
        },
        {
          "name": "useOrderStore",
          "description": "Hook for useOrderStore",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Provides actions to mutate the global order entry state. This is the primary way to update form fields (symbol, side, price, amount) from your UI components.",
          "example": "import { useOrderStore } from '@orderly.network/hooks';\n\n/**\n * This hook provides access to the ACTIONS of the order store.\n * Use it to dispatch updates to the order form from UI components.\n */\nexport const OrderControls = () => {\n  // Destructure actions to update the global order state\n  const { \n    setSymbol, \n    setSide, \n    setPrice, \n    setAmount, \n    resetOrder \n  } = useOrderStore();\n\n  return (\n    <div className=\"flex gap-2\">\n      <select \n        onChange={(e) => setSymbol(e.target.value)}\n        className=\"border p-2 rounded\"\n      >\n        <option value=\"PERP_ETH_USDC\">ETH-PERP</option>\n        <option value=\"PERP_BTC_USDC\">BTC-PERP</option>\n      </select>\n\n      <div className=\"flex bg-gray-200 rounded p-1\">\n        <button \n          onClick={() => setSide('BUY')}\n          className=\"px-4 py-1 rounded bg-green-500 text-white\"\n        >\n          Buy\n        </button>\n        <button \n          onClick={() => setSide('SELL')}\n          className=\"px-4 py-1 rounded bg-red-500 text-white\"\n        >\n          Sell\n        </button>\n      </div>\n\n      <button \n        onClick={resetOrder}\n        className=\"px-4 py-2 text-gray-600 underline\"\n      >\n        Reset Form\n      </button>\n    </div>\n  );\n};",
          "notes": [
            "Separate your read components (using useOrderEntryFromStore) from your write components (using useOrderStore) for cleaner architecture.",
            "The resetOrder action is useful for clearing the form after a successful submission or when switching trading pairs.",
            "Actions update the global Zustand store, meaning changes are instantly reflected across all components subscribed to that state."
          ],
          "related": ["useOrderEntryFromStore", "useOrderEntity", "useOrderEntry"]
        },
        {
          "name": "useMarketTradeStream",
          "description": "Hook for useMarketTradeStream",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Subscribes to a WebSocket feed of individual trade fills (taker trades) for a specific market. This provides a real-time list of the most recent transactions occurring on the order book.",
          "example": "import { useMarketTradeStream } from '@orderly.network/hooks';\n\nexport const RecentTrades = ({ symbol }: { symbol: string }) => {\n  // Subscribes to the public trade stream for the given symbol\n  const { data: trades } = useMarketTradeStream(symbol);\n\n  return (\n    <div className=\"trade-history\">\n      <h4>Recent Trades</h4>\n      <table>\n        <thead>\n          <tr>\n            <th>Price</th>\n            <th>Size</th>\n            <th>Time</th>\n          </tr>\n        </thead>\n        <tbody>\n          {trades?.slice(0, 10).map((trade, index) => (\n            <tr key={index}>\n              <td>{trade.price}</td>\n              <td>{trade.quantity}</td>\n              <td>{new Date(trade.timestamp).toLocaleTimeString()}</td>\n            </tr>\n          ))}\n        </tbody>\n      </table>\n    </div>\n  );\n};",
          "notes": [
            "Returns an array of trade objects, usually ordered by most recent first.",
            "Updates in real-time via WebSocket connection managed by the SDK.",
            "Essential for building 'Recent Trades' or 'Market Activity' components."
          ],
          "related": ["useMarketsStream", "useOrderBook", "useMarket"]
        },
        {
          "name": "useOrderStream",
          "description": "Hook for useOrderStream",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Maintains a real-time list of the user's orders (open, filled, or canceled) by subscribing to the private WebSocket channel. It keeps the local state in sync with the exchange order book.",
          "example": "import { useOrderStream } from '@orderly.network/hooks';\n\nexport const OpenOrders = () => {\n  // Subscribes to the user's private order updates via WebSocket\n  const { data: orders } = useOrderStream();\n\n  return (\n    <div className=\"order-panel\">\n      <h3>Your Orders</h3>\n      {orders && orders.length > 0 ? (\n        <table>\n          <thead>\n            <tr>\n              <th>Symbol</th>\n              <th>Side</th>\n              <th>Size</th>\n              <th>Status</th>\n            </tr>\n          </thead>\n          <tbody>\n            {orders.map((order) => (\n              <tr key={order.order_id}>\n                <td>{order.symbol}</td>\n                <td className={order.side === 'BUY' ? 'text-green' : 'text-red'}>\n                  {order.side}\n                </td>\n                <td>{order.quantity}</td>\n                <td>{order.status}</td>\n              </tr>\n            ))}\n          </tbody>\n        </table>\n      ) : (\n        <p>No open orders.</p>\n      )}\n    </div>\n  );\n};",
          "notes": [
            "Requires an authenticated session (API key or Wallet).",
            "Automatically handles order status updates (e.g., 'NEW' to 'FILLED') without polling.",
            "The primary data source for the 'Open Orders' and 'Order History' UI components."
          ],
          "related": ["useSubAccountAlgoOrderStream", "usePositionStream", "useAccountBalance"]
        },
        {
          "name": "useOrderbookStream",
          "description": "Hook for useOrderbookStream",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Establishes a websocket connection to receive real-time depth (orderbook) data for a specific trading pair. It returns two-dimensional arrays representing price and quantity levels.",
          "example": "import { useOrderbookStream } from '@orderly.network/hooks';\n\nexport const OrderBook = ({ symbol }: { symbol: string }) => {\n  // Connects to the websocket and streams real-time orderbook updates\n  const { asks, bids } = useOrderbookStream(symbol);\n\n  return (\n    <div className=\"flex gap-4 font-mono text-sm\">\n      <div className=\"w-1/2\">\n        <h3 className=\"text-red-500 mb-2\">Asks (Sell)</h3>\n        <ul>\n          {asks.slice(0, 5).map((ask, i) => (\n            <li key={i} className=\"flex justify-between\">\n              <span>{ask[0]}</span>\n              <span>{ask[1]}</span>\n            </li>\n          ))}\n        </ul>\n      </div>\n      <div className=\"w-1/2\">\n        <h3 className=\"text-green-500 mb-2\">Bids (Buy)</h3>\n        <ul>\n          {bids.slice(0, 5).map((bid, i) => (\n            <li key={i} className=\"flex justify-between\">\n              <span>{bid[0]}</span>\n              <span>{bid[1]}</span>\n            </li>\n          ))}\n        </ul>\n      </div>\n    </div>\n  );\n};",
          "notes": [
            "The returned arrays (asks/bids) are typically sorted: Asks ascending (lowest sell), Bids descending (highest buy).",
            "Data updates automatically on every market tick; no manual polling is required.",
            "Use the symbol argument to ensure you are receiving data for the correct market (e.g., 'PERP_ETH_USDC')."
          ],
          "related": ["useSymbolInfo", "usePublicDataObserver", "useTradesStream"]
        },
        {
          "name": "useOrderlyContext",
          "description": "Hook for useOrderlyContext",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Provides access to the core Orderly SDK context. This is the primary way to access global application state such as the connected wallet address, broker ID, account ID, and the current network environment.",
          "example": "import { useOrderlyContext } from '@orderly.network/hooks';\n\nexport const ConnectionStatus = () => {\n  // Access the global Orderly SDK state (address, brokerId, network, etc.)\n  const {\n    state: { address, orderlyKey, accountId },\n    domain // The current environment domain (e.g., 'orderly.network')\n  } = useOrderlyContext();\n\n  return (\n    <div className=\"text-sm text-gray-600\">\n      <p><strong>Account ID:</strong> {accountId || 'Not Connected'}</p>\n      <p><strong>Wallet:</strong> {address?.slice(0, 6)}...{address?.slice(-4)}</p>\n      <p><strong>Environment:</strong> {domain}</p>\n    </div>\n  );\n};",
          "notes": [
            "This is a context hook, so it must be used within a component wrapped by `OrderlyProvider`.",
            "Useful for conditional rendering based on connection status or account ID.",
            "Contains the `domain` setting which is crucial for determining if you are on testnet or mainnet."
          ],
          "related": ["useDataCenterContext", "useWalletTopic", "useAccountInfo"]
        }
      ]
    },
    {
      "name": "Positions",
      "patterns": [
        {
          "name": "usePositionClose",
          "description": "Hook for usePositionClose",
          "installation": "npm install @orderly.network/hooks",
          "usage": "A specialized hook to handle the closing of open trading positions. It simplifies the logic of placing a market order to reduce an existing position to zero.",
          "example": "import { usePositionClose } from '@orderly.network/hooks';\n\nexport const PositionRow = ({ symbol, size, entryPrice }) => {\n  const { closePosition, isClosing } = usePositionClose();\n\n  const handleClose = async () => {\n    if (!window.confirm(`Are you sure you want to close your ${symbol} position?`)) {\n      return;\n    }\n\n    try {\n      // Execute a market order to reduce the position to 0\n      await closePosition({ \n        symbol: symbol,\n        // Usually implies reducing position to zero\n        size: size \n      });\n    } catch (error) {\n      console.error('Failed to close position', error);\n    }\n  };\n\n  return (\n    <tr className=\"border-b\">\n      <td className=\"p-2\">{symbol}</td>\n      <td className=\"p-2\">{size}</td>\n      <td className=\"p-2\">{entryPrice}</td>\n      <td className=\"p-2 text-right\">\n        <button \n          onClick={handleClose}\n          disabled={isClosing}\n          className={`px-3 py-1 rounded text-sm ${\n            isClosing ? 'bg-gray-300' : 'bg-red-500 text-white hover:bg-red-600'\n          }`}\n        >\n          {isClosing ? 'Closing...' : 'Close'}\n        </button>\n      </td>\n    </tr>\n  );\n};",
          "notes": [
            "This hook abstracts away the complexity of calculating the opposite order required to flatten a position.",
            "The isClosing state is crucial for UX, preventing the user from spamming the close button while the transaction is processing.",
            "Always confirm with the user before closing, as this is an irreversible financial action."
          ],
          "related": ["useAccountInfo", "useOrderEntry", "usePortfolio"]
        },
        {
          "name": "usePositionStream",
          "description": "Hook for usePositionStream",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Subscribes to the user's private position data via websocket. It provides real-time Profit and Loss (PnL) metrics, including total unrealized PnL and Return on Investment (ROI) across all open positions.",
          "example": "import { usePositionStream } from '@orderly.network/hooks';\n\nexport const AccountSummary = () => {\n  // Streams real-time position updates and PnL calculations\n  const { unrealPnL, total_unreal_pnl, unrealPnlROI } = usePositionStream();\n\n  return (\n    <div className=\"p-4 border rounded shadow-sm bg-white\">\n      <h2 className=\"text-lg font-bold mb-4\">Portfolio Performance</h2>\n      \n      <div className=\"grid grid-cols-3 gap-4\">\n        <div>\n          <p className=\"text-gray-500 text-xs\">Total Unreal PnL</p>\n          <p className={`text-xl font-semibold ${total_unreal_pnl >= 0 ? 'text-green-600' : 'text-red-600'}`}>\n            ${total_unreal_pnl?.toFixed(2)}\n          </p>\n        </div>\n        \n        <div>\n          <p className=\"text-gray-500 text-xs\">ROI</p>\n          <p className={`text-xl font-semibold ${unrealPnlROI >= 0 ? 'text-green-600' : 'text-red-600'}`}>\n            {unrealPnlROI?.toFixed(2)}%\n          </p>\n        </div>\n\n        <div>\n          <p className=\"text-gray-500 text-xs\">Unreal PnL (Raw)</p>\n          <p className=\"text-xl font-semibold\">\n            {unrealPnL?.toFixed(4)}\n          </p>\n        </div>\n      </div>\n    </div>\n  );\n};",
          "notes": [
            "Requires the user to be connected/authenticated with the Orderly SDK.",
            "Values update in real-time as mark prices and funding rates change.",
            "Useful for dashboard headers or portfolio summary widgets where live PnL tracking is critical."
          ],
          "related": ["useAccount", "usePrivateDataObserver", "useSettleSubscription"]
        }
      ]
    },
    {
      "name": "Account & Wallet",
      "patterns": [
        {
          "name": "useAccountInfo",
          "description": "Hook for useAccountInfo",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Fetches and returns the user's account information, such as total equity, account value, and available balance. This is essential for displaying portfolio health and buying power.",
          "example": "import { useAccountInfo } from '@orderly.network/hooks';\n\nexport const AccountBalanceBar = () => {\n  // Returns comprehensive account data including equity, balance, and margin\n  const { \n    accountValue, \n    totalEquity, \n    availableBalance, \n    isLoading \n  } = useAccountInfo();\n\n  if (isLoading) {\n    return <div className=\"h-12 bg-gray-200 animate-pulse rounded\" />;\n  }\n\n  return (\n    <div className=\"flex justify-between items-center p-4 bg-white shadow rounded-lg\">\n      <div>\n        <p className=\"text-xs text-gray-500 uppercase\">Total Equity</p>\n        <p className=\"text-xl font-bold text-gray-900\">\n          ${totalEquity ? parseFloat(totalEquity).toFixed(2) : '0.00'}\n        </p>\n      </div>\n      \n      <div className=\"text-right\">\n        <p className=\"text-xs text-gray-500 uppercase\">Available</p>\n        <p className=\"text-lg font-semibold text-blue-600\">\n          ${availableBalance ? parseFloat(availableBalance).toFixed(2) : '0.00'}\n        </p>\n      </div>\n    </div>\n  );\n};",
          "notes": [
            "The data usually updates in real-time or near real-time via WebSocket connections managed by the SDK.",
            "Handle the isLoading state to prevent layout shifts or displaying '0' while data is being fetched on initial load.",
            "Use availableBalance to validate if a user has sufficient funds before allowing an order submission."
          ],
          "related": ["usePortfolio", "useOrderEntry", "usePositionClose"]
        },
        {
          "name": "useSubAccountAlgoOrderStream",
          "description": "Hook for useSubAccountAlgoOrderStream",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Subscribes to the stream of algorithmic orders (such as Take-Profit, Stop-Loss, or Trailing Stops) associated with a specific sub-account ID.",
          "example": "import { useSubAccountAlgoOrderStream } from '@orderly.network/hooks';\n\nexport const AlgoOrdersPanel = ({ accountId }: { accountId: number }) => {\n  // Subscribes to algo orders (TP/SL, Stop Market) for a specific sub-account\n  const { data: algoOrders } = useSubAccountAlgoOrderStream(accountId);\n\n  return (\n    <div className=\"algo-orders\">\n      <h3>Conditional Orders (Account {accountId})</h3>\n      <ul>\n        {algoOrders?.map((order) => (\n          <li key={order.algo_order_id} className=\"mb-2 border-b pb-2\">\n            <div className=\"flex justify-between\">\n              <span>{order.symbol}</span>\n              <span className={order.side === 'BUY' ? 'text-green-500' : 'text-red-500'}>\n                {order.side}\n              </span>\n            </div>\n            <div className=\"text-xs text-gray-500\">\n              Trigger: {order.trigger_price} | Type: {order.type}\n            </div>\n          </li>\n        ))}\n      </ul>\n    </div>\n  );\n};",
          "notes": [
            "Specifically targets 'algo' orders, distinct from standard limit/market orders handled by `useOrderStream`.",
            "Requires the `accountId` (sub-account ID) to filter the stream correctly.",
            "Critical for managing risk strategies and conditional order visibility."
          ],
          "related": ["useOrderStream", "useAlgoOrder", "useSubAccountList"]
        },
        {
          "name": "useWalletSubscription",
          "description": "Hook for useWalletSubscription",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Maintains a WebSocket connection to listen for changes in the user's wallet state, including balance updates from trades, deposits, or withdrawals.",
          "example": "import { useWalletSubscription } from '@orderly.network/hooks';\n\nexport const WalletSummary = () => {\n  // Subscribes to real-time updates for the wallet's balance and portfolio state\n  const { data, status } = useWalletSubscription();\n\n  if (status === 'connecting') return <div>Connecting to wallet...</div>;\n\n  return (\n    <div className=\"wallet-summary\">\n      <h3>Portfolio Value</h3>\n      <p className=\"total-equity\">\n        ${data?.total_equity ? parseFloat(data.total_equity).toFixed(2) : '0.00'}\n      </p>\n      <div className=\"details\">\n        <p>Available: ${parseFloat(data?.available_balance || 0).toFixed(2)}</p>\n        <p>Margin: ${parseFloat(data?.holding_balance || 0).toFixed(2)}</p>\n      </div>\n    </div>\n  );\n};",
          "notes": [
            "This provides a holistic view of the account, including `total_equity` and `unrealized_pnl`.",
            "It automatically reconnects if the socket drops, ensuring the UI stays synced with the server.",
            "Use this to update the header bar or dashboard summary in real-time."
          ],
          "related": ["useBalanceTopic", "usePositionStream", "useAccountInfo"]
        },
        {
          "name": "useWalletTopic",
          "description": "Hook for useWalletTopic",
          "installation": "npm install @orderly.network/hooks",
          "usage": "This hook manages the WebSocket subscription to the private wallet topic. It is essential for receiving real-time updates on the user's asset balances and portfolio changes without manual polling.",
          "example": "import { useWalletTopic } from '@orderly.network/hooks';\n\nexport const WalletBalance = () => {\n  // Subscribes to the private wallet channel for real-time balance updates.\n  // Returns the current wallet state/data payload.\n  const { data, error, isConnected } = useWalletTopic();\n\n  if (!isConnected) return <div>Connecting to wallet feed...</div>;\n  if (error) return <div>Error loading wallet data</div>;\n\n  return (\n    <div className=\"p-4 border rounded\">\n      <h3 className=\"font-bold mb-2\">Real-time Wallet State</h3>\n      {/* Data structure depends on the specific Orderly API version, usually contains holdings */}\n      <pre>{JSON.stringify(data, null, 2)}</pre>\n    </div>\n  );\n};",
          "notes": [
            "Requires an active Orderly Key and wallet connection to subscribe to private data.",
            "Automatically handles reconnection logic if the WebSocket drops.",
            "Returns the raw data payload, which you may need to parse or format for display."
          ],
          "related": ["useOrderlyContext", "useAccountBalance", "usePrivateTopic"]
        },
        {
          "name": "useSubAccountDataObserver",
          "description": "Hook for useSubAccountDataObserver",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Monitors the asset holdings of a specific sub-account in real-time. It returns a 'holding' array or object representing the assets and their quantities within that sub-account.",
          "example": "import { useSubAccountDataObserver } from '@orderly.network/hooks';\n\nexport const SubAccountHoldings = ({ subAccountId }: { subAccountId: number }) => {\n  // Observes real-time holding data for a specific sub-account.\n  // Returns 'holding' data structure.\n  const { holding, isLoading } = useSubAccountDataObserver(subAccountId);\n\n  if (isLoading) return <div>Loading holdings...</div>;\n\n  return (\n    <div className=\"bg-gray-100 p-3 rounded\">\n      <h4>Sub-Account {subAccountId} Holdings</h4>\n      {holding ? (\n        <ul>\n          {holding.map((item: any) => (\n            <li key={item.token}>\n              {item.token}: {item.holding}\n            </li>\n          ))}\n        </ul>\n      ) : (\n        <p>No holdings found.</p>\n      )}\n    </div>\n  );\n};",
          "notes": [
            "Requires a valid `subAccountId` (integer) passed as an argument.",
            "Updates automatically via WebSocket when the sub-account trades or transfers funds.",
            "Useful for portfolio management views across multiple accounts."
          ],
          "related": ["useSubAccountMaxWithdrawal", "useSubAccountWS", "useSubAccounts"]
        },
        {
          "name": "useSubAccountMaxWithdrawal",
          "description": "Hook for useSubAccountMaxWithdrawal",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Determines the maximum amount of a specific token that can be withdrawn from a sub-account, taking into account margin requirements and open positions.",
          "example": "import { useSubAccountMaxWithdrawal } from '@orderly.network/hooks';\n\nexport const WithdrawalLimit = ({ subAccountId, token }: { subAccountId: number, token: string }) => {\n  // Calculates the maximum amount that can be withdrawn from a sub-account for a specific token.\n  const { data, isLoading } = useSubAccountMaxWithdrawal(subAccountId, token);\n\n  const maxWithdraw = data?.max_withdraw || '0';\n\n  return (\n    <div className=\"text-sm\">\n      <span>Max Withdrawable ({token}): </span>\n      <span className=\"font-mono font-bold text-green-600\">{maxWithdraw}</span>\n    </div>\n  );\n};",
          "notes": [
            "Essential for risk management to prevent over-withdrawing which could cause liquidation.",
            "Requires both the `subAccountId` and the `token` symbol (e.g., 'USDC', 'ETH').",
            "The value changes dynamically based on market prices and position sizes."
          ],
          "related": ["useSubAccountDataObserver", "useSubAccountWS", "useWithdraw"]
        },
        {
          "name": "useSubAccountWS",
          "description": "Hook for useSubAccountWS",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Handles the WebSocket connection lifecycle for sub-account specific data. It ensures that the client is listening to the correct channels for sub-account updates.",
          "example": "import { useSubAccountWS } from '@orderly.network/hooks';\nimport { useEffect } from 'react';\n\nexport const SubAccountConnector = ({ subAccountId }: { subAccountId: number }) => {\n  // Manages the WebSocket connection specifically for sub-account data streams.\n  const { connected, connect, disconnect } = useSubAccountWS();\n\n  useEffect(() => {\n    // Ensure connection is active when viewing a specific sub-account\n    connect();\n    return () => {\n      // Cleanup if necessary, though SDK often handles this globally\n      // disconnect(); \n    };\n  }, [connect]);\n\n  return (\n    <div className=\"flex items-center gap-2\">\n      <div className={`w-3 h-3 rounded-full ${connected ? 'bg-green-500' : 'bg-red-500'}`} />\n      <span>Sub-Account {subAccountId} Feed: {connected ? 'Live' : 'Offline'}</span>\n    </div>\n  );\n};",
          "notes": [
            "Abstracts the complexity of subscribing to specific sub-account topics.",
            "Useful for ensuring connection status before rendering sensitive trading data.",
            "Often used internally by other sub-account hooks, but can be used for UI status indicators."
          ],
          "related": ["useSubAccountDataObserver", "useSubAccountMaxWithdrawal", "useWalletTopic"]
        },
        {
          "name": "useAccountRewardsHistory",
          "description": "Hook for useAccountRewardsHistory",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Retrieves the history of trading rewards claimed by the user. The data is organized by 'epochs' (specific time periods for reward campaigns) and associated brokers.",
          "example": "import { useAccountRewardsHistory } from '@orderly.network/hooks';\n\nexport const RewardsHistory = () => {\n  // Fetches historical trading rewards data.\n  // Returns data containing epoch_id and broker information.\n  const { data, isLoading } = useAccountRewardsHistory();\n\n  if (isLoading) return <div>Loading reward history...</div>;\n\n  return (\n    <div className=\"overflow-x-auto\">\n      <table className=\"min-w-full text-left\">\n        <thead>\n          <tr>\n            <th className=\"p-2\">Epoch ID</th>\n            <th className=\"p-2\">Broker</th>\n            <th className=\"p-2\">Status</th>\n          </tr>\n        </thead>\n        <tbody>\n          {data?.map((reward: any) => (\n            <tr key={reward.epoch_id} className=\"border-t\">\n              <td className=\"p-2\">{reward.epoch_id}</td>\n              <td className=\"p-2\">{reward.broker}</td>\n              <td className=\"p-2\">\n                <span className=\"px-2 py-1 bg-green-100 text-green-800 rounded text-xs\">\n                  Claimed\n                </span>\n              </td>\n            </tr>\n          ))}\n        </tbody>\n      </table>\n    </div>\n  );\n};",
          "notes": [
            "Returns an array of historical records, each containing `epoch_id` and `broker`.",
            "Useful for auditing past rewards or displaying a 'Rewards History' page.",
            "May require pagination if the history is extensive."
          ],
          "related": ["useAccountRewards", "useOrderlyContext", "useTradingRewards"]
        },
        {
          "name": "useAccount",
          "description": "Hook for useAccount",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Core hook for managing the user's account state. It distinguishes between Main and Sub-accounts and provides methods to create new accounts or refresh account data from the server.",
          "example": "import { useAccount } from '@orderly.network/hooks';\n\nexport const AccountManager = () => {\n  const { \n    isMainAccount, \n    subAccount, \n    refresh, \n    create \n  } = useAccount();\n\n  const handleCreateSubAccount = async () => {\n    try {\n      await create();\n      await refresh(); // Refresh state after creation\n    } catch (err) {\n      console.error('Creation failed', err);\n    }\n  };\n\n  return (\n    <div className=\"account-panel\">\n      <div className=\"status\">\n        Type: {isMainAccount ? 'Main Account' : 'Sub Account'}\n      </div>\n      \n      {isMainAccount && (\n        <button onClick={handleCreateSubAccount}>\n          Create Sub-Account\n        </button>\n      )}\n\n      <button onClick={refresh}>Refresh Data</button>\n    </div>\n  );\n};",
          "notes": [
            "isMainAccount is a boolean flag to toggle UI features specific to the main wallet.",
            "The refresh function is vital to sync local state after on-chain transactions.",
            "create() handles the logic for generating a new sub-account ID linked to the main account."
          ],
          "related": ["useKeyStore", "useAllBrokers"]
        }
      ]
    },
    {
      "name": "Orderly Network",
      "patterns": [
        {
          "name": "usePortfolio",
          "description": "Hook for usePortfolio",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Retrieves the user's full portfolio composition, including individual token holdings and their USD values. Ideal for dashboard views showing asset allocation.",
          "example": "import { usePortfolio } from '@orderly.network/hooks';\n\nexport const AssetList = () => {\n  // Returns a list of assets held in the portfolio\n  const { portfolio, isLoading } = usePortfolio();\n\n  return (\n    <div className=\"w-full max-w-md\">\n      <h3 className=\"text-lg font-semibold mb-4\">Your Assets</h3>\n      \n      {isLoading ? (\n        <p>Loading portfolio...</p>\n      ) : (\n        <table className=\"w-full text-left border-collapse\">\n          <thead>\n            <tr className=\"text-gray-500 text-sm border-b\">\n              <th className=\"py-2\">Asset</th>\n              <th className=\"py-2 text-right\">Size</th>\n              <th className=\"py-2 text-right\">Value (USD)</th>\n            </tr>\n          </thead>\n          <tbody>\n            {portfolio?.holding?.map((asset) => (\n              <tr key={asset.token} className=\"border-b last:border-0\">\n                <td className=\"py-3 font-medium\">{asset.token}</td>\n                <td className=\"py-3 text-right font-mono\">\n                  {parseFloat(asset.holding).toFixed(4)}\n                </td>\n                <td className=\"py-3 text-right font-mono\">\n                  ${parseFloat(asset.value_usd).toFixed(2)}\n                </td>\n              </tr>\n            ))}\n          </tbody>\n        </table>\n      )}\n    </div>\n  );\n};",
          "notes": [
            "The portfolio structure often contains a 'holding' array which lists individual assets like USDC, ETH, etc.",
            "This hook aggregates data across different sub-accounts or chains if applicable to the Orderly configuration.",
            "Use this data to calculate percentages for a pie chart visualization of asset allocation."
          ],
          "related": ["useAccountInfo", "useApiKeyManager"]
        },
        {
          "name": "useStatisticsDaily",
          "description": "Hook for useStatisticsDaily",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Provides a daily snapshot of trading performance. It aggregates total volume, net PnL, and ROI, making it perfect for dashboard summary widgets.",
          "example": "import { useStatisticsDaily } from \"@orderly.network/hooks\";\n\nexport const DailyStatsCard = () => {\n  // Fetches aggregated daily trading statistics\n  // Returns: vol (Volume), pnl (Profit and Loss), roi (Return on Investment)\n  const { data, isLoading } = useStatisticsDaily();\n\n  if (isLoading) return <div>Loading stats...</div>;\n\n  return (\n    <div className=\"grid grid-cols-3 gap-4 p-4 bg-white shadow rounded-lg\">\n      <StatCard label=\"Volume (24h)\" value={data?.vol} prefix=\"$\" />\n      <StatCard label=\"PnL (24h)\" value={data?.pnl} prefix=\"$\" isPnl />\n      <StatCard label=\"ROI (24h)\" value={data?.roi} suffix=\"%\" />\n    </div>\n  );\n};\n\n// Helper component for display\nconst StatCard = ({ label, value, prefix = \"\", suffix = \"\", isPnl = false }) => {\n  const colorClass = isPnl && value < 0 ? \"text-red-500\" : (isPnl && value > 0 ? \"text-green-500\" : \"text-gray-800\");\n  \n  return (\n    <div className=\"text-center\">\n      <p className=\"text-xs text-gray-500 uppercase\">{label}</p>\n      <p className={`text-xl font-bold ${colorClass}`}>\n        {prefix}{value?.toFixed(2)}{suffix}\n      </p>\n    </div>\n  );\n};",
          "notes": [
            "Returns `vol`, `pnl`, and `roi` as the primary metrics.",
            "Data is typically aggregated for the current day or the most recent 24-hour period.",
            "Useful for giving users a quick health check of their daily trading activity."
          ],
          "related": ["useAssetsHistory", "useAccount", "useDistributionHistory"]
        },
        {
          "name": "useBalanceSubscription",
          "description": "Hook for useBalanceSubscription",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Maintains a real-time subscription to the user's account balance. It updates instantly upon trade execution, fee deduction, or deposit, without requiring a page refresh.",
          "example": "import { useBalanceSubscription } from \"@orderly.network/hooks\";\n\nexport const LiveBalance = () => {\n  // Subscribes to real-time balance updates via WebSocket\n  // Returns the current balance object that updates automatically\n  const { data: balance, isLoading } = useBalanceSubscription();\n\n  if (isLoading) return <div>Connecting to feed...</div>;\n\n  return (\n    <div className=\"p-4 bg-blue-50 rounded-lg border border-blue-100\">\n      <h3 className=\"text-sm text-blue-600 font-semibold uppercase tracking-wider mb-1\">\n        Total Equity (Live)\n      </h3>\n      <div className=\"text-3xl font-bold text-gray-900\">\n        ${balance?.total_equity || \"0.00\"}\n      </div>\n      \n      <div className=\"mt-4 grid grid-cols-2 gap-4 text-sm\">\n        <div>\n          <span className=\"text-gray-500\">Available:</span>\n          <span className=\"ml-1 font-medium\">{balance?.available_balance}</span>\n        </div>\n        <div>\n          <span className=\"text-gray-500\">Margin:</span>\n          <span className=\"ml-1 font-medium\">{balance?.margin_balance}</span>\n        </div>\n      </div>\n    </div>\n  );\n};",
          "notes": [
            "Uses WebSocket connections under the hood for low-latency updates.",
            "Returns a balance object containing `total_equity`, `available_balance`, and `margin_balance`.",
            "Must be used within an Orderly Provider context and requires an authenticated session."
          ],
          "related": ["useWSObserver", "useAccount", "useOrderSubscription"]
        },
        {
          "name": "useChain",
          "description": "Hook for useChain",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Identifies the active blockchain network the SDK is currently configured to interact with. This is vital for multi-chain dApps to ensure transactions are signed for the correct chain.",
          "example": "import { useChain } from \"@orderly.network/hooks\";\n\nexport const NetworkIndicator = () => {\n  // Returns the currently selected blockchain network context\n  // Returns: chain_name (e.g., 'arbitrum', 'optimism', 'bsc')\n  const chain = useChain();\n\n  return (\n    <div className=\"flex items-center gap-2 px-3 py-1 bg-gray-200 rounded-full\">\n      <span className=\"w-2 h-2 rounded-full bg-green-500 animate-pulse\" />\n      <span className=\"text-xs font-bold uppercase text-gray-700\">\n        {chain?.chain_name || \"Unknown Network\"}\n      </span>\n    </div>\n  );\n};",
          "notes": [
            "Returns `chain_name` (string), such as 'arbitrum' or 'optimism'.",
            "Use this to conditionally render chain-specific UI elements (e.g., different bridge addresses).",
            "Relies on the OrderlyProvider's configuration; ensure the provider is set up correctly."
          ],
          "related": ["useChainInfo", "useWallet", "useAccount"]
        },
        {
          "name": "useChainInfo",
          "description": "Hook for useChainInfo",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Provides detailed metadata about the current blockchain, such as the Chain ID, RPC URL, and Block Explorer URL. It is best used for generating links or configuring web3 providers.",
          "example": "import { useChainInfo } from \"@orderly.network/hooks\";\n\nexport const TransactionLink = ({ txHash }: { txHash: string }) => {\n  // Retrieves detailed configuration for the current chain\n  // Returns: chain_id, rpc_url, explorer_url, etc.\n  const chainInfo = useChainInfo();\n\n  if (!chainInfo) return null;\n\n  const explorerUrl = `${chainInfo.explorer_url}/tx/${txHash}`;\n\n  return (\n    <a \n      href={explorerUrl} \n      target=\"_blank\" \n      rel=\"noopener noreferrer\"\n      className=\"text-blue-500 hover:underline text-sm flex items-center gap-1\"\n    >\n      View Transaction \n      <svg className=\"w-3 h-3\" fill=\"none\" stroke=\"currentColor\" viewBox=\"0 0 24 24\"><path strokeLinecap=\"round\" strokeLinejoin=\"round\" strokeWidth={2} d=\"M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14\" /></svg>\n    </a>\n  );\n};",
          "notes": [
            "Returns an object with `chain_id`, `rpc_url`, `explorer_url`, and `native_currency`.",
            "Essential for constructing valid transaction links or debugging network connection issues.",
            "Pairs well with `useChain` when you need both the name and the configuration details."
          ],
          "related": ["useChain", "useWallet", "useTransferHistory"]
        },
        {
          "name": "useChains",
          "description": "Hook for useChains",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Retrieves the list of supported blockchain networks configured in the Orderly SDK, categorized by environment (testnet vs mainnet). This is essential for building network switchers or validating connection targets.",
          "example": "import { useChains } from '@orderly.network/hooks';\n\nexport const ChainSelector = () => {\n  // Returns an object containing 'testnet' and 'mainnet' arrays of chain info\n  const { testnet, mainnet } = useChains();\n\n  return (\n    <div className=\"p-4\">\n      <h3 className=\"font-bold mb-2\">Supported Networks</h3>\n      \n      <div className=\"mb-4\">\n        <h4 className=\"text-sm text-gray-500\">Mainnet</h4>\n        <ul>\n          {mainnet.map((chain) => (\n            <li key={chain.id} className=\"flex items-center gap-2\">\n              <span>{chain.name}</span>\n              <span className=\"text-xs bg-gray-200 px-1 rounded\">{chain.id}</span>\n            </li>\n          ))}\n        </ul>\n      </div>\n\n      <div>\n        <h4 className=\"text-sm text-gray-500\">Testnet</h4>\n        <ul>\n          {testnet.map((chain) => (\n            <li key={chain.id} className=\"flex items-center gap-2\">\n              <span>{chain.name}</span>\n              <span className=\"text-xs bg-blue-200 px-1 rounded\">{chain.id}</span>\n            </li>\n          ))}\n        </ul>\n      </div>\n    </div>\n  );\n};",
          "notes": [
            "Returns an object with two keys: `testnet` and `mainnet`, each containing an array of chain objects.",
            "Chain objects typically include `id`, `name`, and `rpc_url` required for wallet connections.",
            "Useful for dynamically generating dropdown menus for wallet connection flows."
          ],
          "related": ["useWallet", "useAccount", "useNetwork"]
        },
        {
          "name": "useComputedLTV",
          "description": "Hook for useComputedLTV",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Calculates real-time Loan-to-Value (LTV) components for a specific asset or position. It breaks down the calculation into quantity, current index price, and the asset's risk weight.",
          "example": "import { useComputedLTV } from '@orderly.network/hooks';\n\nexport const MarginHealthIndicator = ({ symbol, quantity }: { symbol: string, quantity: number }) => {\n  // Calculates Loan-to-Value metrics based on position size and current oracle prices\n  const { qty, indexPrice, weight } = useComputedLTV(symbol, quantity);\n\n  // Example logic: Calculate notional value\n  const notionalValue = qty * indexPrice;\n\n  return (\n    <div className=\"border p-4 rounded\">\n      <h3 className=\"font-semibold\">Risk Metrics: {symbol}</h3>\n      <div className=\"grid grid-cols-2 gap-4 mt-2 text-sm\">\n        <div>\n          <span className=\"text-gray-500\">Quantity:</span>\n          <span className=\"ml-2 font-mono\">{qty.toFixed(4)}</span>\n        </div>\n        <div>\n          <span className=\"text-gray-500\">Index Price:</span>\n          <span className=\"ml-2 font-mono\">${indexPrice.toFixed(2)}</span>\n        </div>\n        <div>\n          <span className=\"text-gray-500\">Collateral Weight:</span>\n          <span className=\"ml-2 font-mono\">{weight}%</span>\n        </div>\n        <div>\n          <span className=\"text-gray-500\">Notional Value:</span>\n          <span className=\"ml-2 font-mono\">${notionalValue.toFixed(2)}</span>\n        </div>\n      </div>\n    </div>\n  );\n};",
          "notes": [
            "Essential for building margin calculators or liquidation risk UI components.",
            "Relies on real-time oracle data for `indexPrice`.",
            "The `weight` return value is crucial for understanding how much collateral value this specific asset contributes."
          ],
          "related": ["useAccount", "usePosition", "useMarginSummary"]
        },
        {
          "name": "useConvert",
          "description": "Hook for useConvert",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Provides functionality to convert one asset to another within the Orderly ecosystem (e.g., swapping stablecoins or managing collateral types).",
          "example": "import { useConvert } from '@orderly.network/hooks';\nimport { useState } from 'react';\n\nexport const TokenConverter = () => {\n  const [fromToken, setFromToken] = useState('USDC');\n  const [toToken, setToToken] = useState('USDT');\n  const [amount, setAmount] = useState('100');\n\n  // Hook to handle conversion logic (e.g. USDC to USDT)\n  const { convert, isLoading, error } = useConvert();\n\n  const handleConvert = async () => {\n    try {\n      const txHash = await convert({\n        fromToken,\n        toToken,\n        amount\n      });\n      console.log('Conversion successful:', txHash);\n    } catch (err) {\n      console.error('Conversion failed:', err);\n    }\n  };\n\n  return (\n    <div className=\"flex flex-col gap-2 p-4 border rounded\">\n      <input \n        value={amount} \n        onChange={(e) => setAmount(e.target.value)} \n        placeholder=\"Amount\"\n        className=\"border p-2 rounded\"\n      />\n      <div className=\"flex gap-2\">\n        <select value={fromToken} onChange={(e) => setFromToken(e.target.value)} className=\"border p-2\">\n          <option value=\"USDC\">USDC</option>\n          <option value=\"USDT\">USDT</option>\n        </select>\n        <span>to</span>\n        <select value={toToken} onChange={(e) => setToToken(e.target.value)} className=\"border p-2\">\n          <option value=\"USDT\">USDT</option>\n          <option value=\"USDC\">USDC</option>\n        </select>\n      </div>\n      <button \n        onClick={handleConvert} \n        disabled={isLoading}\n        className=\"bg-blue-600 text-white p-2 rounded disabled:opacity-50\"\n      >\n        {isLoading ? 'Converting...' : 'Convert'}\n      </button>\n      {error && <p className=\"text-red-500 text-sm\">{error.message}</p>}\n    </div>\n  );\n};",
          "notes": [
            "Handles the transaction execution for token conversion on-chain.",
            "Returns loading and error states to manage UI feedback during the swap.",
            "Ensure the user has sufficient balance of the `fromToken` before calling."
          ],
          "related": ["useBalance", "useAllowance", "useWallet"]
        },
        {
          "name": "useFundingDetails",
          "description": "Hook for useFundingDetails",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Provides granular information about the funding state of a perpetual market, including timestamps for the next funding event and predicted rates.",
          "example": "import { useFundingDetails } from '@orderly.network/hooks';\n\nexport const FundingInfo = ({ symbol }: { symbol: string }) => {\n  // Fetches detailed funding metrics for a specific perpetual market\n  const details = useFundingDetails(symbol);\n\n  if (!details) return <div>Loading funding details...</div>;\n\n  return (\n    <div className=\"text-sm\">\n      <h4 className=\"font-semibold mb-2\">Funding Details ({symbol})</h4>\n      <div className=\"grid grid-cols-2 gap-y-2\">\n        <span className=\"text-gray-500\">Next Funding Time:</span>\n        <span>{new Date(details.nextFundingTime).toLocaleTimeString()}</span>\n        \n        <span className=\"text-gray-500\">Predicted Rate:</span>\n        <span className={details.predictedRate > 0 ? 'text-green-500' : 'text-red-500'}>\n          {details.predictedRate}%\n        </span>\n        \n        <span className=\"text-gray-500\">Last Rate:</span>\n        <span>{details.lastRate}%</span>\n      </div>\n    </div>\n  );\n};",
          "notes": [
            "Helps traders predict upcoming costs or payments for holding positions.",
            "Includes timing data (`nextFundingTime`) to display countdowns.",
            "Distinct from historical rates; focuses on the current and immediate future state."
          ],
          "related": ["useFundingRate", "useFundingRateHistory", "useMarkPrice"]
        },
        {
          "name": "useFundingRate",
          "description": "Hook for useFundingRate",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Fetches the current funding rate for a specific perpetual market symbol. This is the primary rate used to calculate fees paid or received by traders.",
          "example": "import { useFundingRate } from '@orderly.network/hooks';\n\nexport const CurrentFundingRate = ({ symbol }: { symbol: string }) => {\n  // Fetches the current funding rate for a single symbol\n  const { rate, markPrice, indexPrice } = useFundingRate(symbol);\n\n  return (\n    <div className=\"flex items-center space-x-4 p-2 bg-white border rounded shadow-sm\">\n      <div className=\"text-center\">\n        <p className=\"text-xs text-gray-500\">Funding Rate</p>\n        <p className={`font-bold ${rate > 0 ? 'text-green-600' : 'text-red-600'}`}>\n          {rate ? `${(rate * 100).toFixed(4)}%` : '-'}\n        </p>\n      </div>\n      <div className=\"h-8 w-px bg-gray-200\"></div>\n      <div className=\"text-center\">\n        <p className=\"text-xs text-gray-500\">Mark Price</p>\n        <p className=\"font-mono\">{markPrice}</p>\n      </div>\n      <div className=\"h-8 w-px bg-gray-200\"></div>\n      <div className=\"text-center\">\n        <p className=\"text-xs text-gray-500\">Index Price</p>\n        <p className=\"font-mono\">{indexPrice}</p>\n      </div>\n    </div>\n  );\n};",
          "notes": [
            "Returns the raw rate value (usually requiring multiplication by 100 for percentage display).",
            "Often includes `markPrice` and `indexPrice` as they are used to calculate the rate.",
            "Updates frequently (often every few seconds) to reflect market conditions."
          ],
          "related": ["useFundingDetails", "useFundingRateHistory", "usePosition"]
        },
        {
          "name": "useFundingRates",
          "description": "Hook for useFundingRates",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Fetches the current funding rates for all perpetual markets in a single call. This is optimized for dashboard views showing multiple assets.",
          "example": "import { useFundingRates } from '@orderly.network/hooks';\n\nexport const AllMarketsFunding = () => {\n  // Fetches current funding rates for all available markets\n  const fundingRates = useFundingRates();\n\n  return (\n    <div className=\"p-4\">\n      <h2 className=\"text-xl font-bold mb-4\">Market Overview</h2>\n      <div className=\"grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4\">\n        {Object.entries(fundingRates).map(([symbol, data]) => (\n          <div key={symbol} className=\"border p-3 rounded hover:shadow-md transition-shadow\">\n            <div className=\"flex justify-between items-center mb-2\">\n              <span className=\"font-bold\">{symbol}</span>\n              <span className={`text-xs px-2 py-1 rounded ${data.rate > 0 ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800'}`}>\n                {(data.rate * 100).toFixed(3)}%\n              </span>\n            </div>\n            <div className=\"text-xs text-gray-500\">\n              Mark Price: ${data.markPrice}\n            </div>\n          </div>\n        ))}\n      </div>\n    </div>\n  );\n};",
          "notes": [
            "Returns an object or array mapping symbols to their current funding data.",
            "More efficient than calling `useFundingRate` multiple times for different symbols.",
            "Data is usually aggregated, making it perfect for overview lists."
          ],
          "related": ["useFutures", "useFundingRateHistory", "useMarkets"]
        },
        {
          "name": "useFundingRatesStore",
          "description": "Hook for useFundingRatesStore",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Provides direct access to the global funding rates state store (Zustand). This is useful for accessing cached data without triggering network requests or for creating highly optimized components.",
          "example": "import { useFundingRatesStore } from '@orderly.network/hooks';\n\nexport const FundingTicker = ({ symbol }: { symbol: string }) => {\n  // Accesses the global Zustand store for funding rates\n  // This allows for high-performance updates without re-fetching\n  const getRate = useFundingRatesStore((state) => state.getRate);\n  const rates = useFundingRatesStore((state) => state.rates);\n\n  const currentRate = getRate ? getRate(symbol) : rates[symbol];\n\n  return (\n    <div className=\"inline-block px-3 py-1 bg-gray-800 text-white rounded-full text-xs font-mono\">\n      {symbol}: {currentRate ? `${(currentRate * 100).toFixed(4)}%` : 'Loading...'}\n    </div>\n  );\n};",
          "notes": [
            "Reads from a global state store, ensuring instant UI updates when data changes elsewhere.",
            "Does not typically trigger its own fetch; relies on other hooks (like `useFundingRates`) to populate the store.",
            "Best used for performance-critical components like tickers or real-time headers."
          ],
          "related": ["useFundingRates", "useOrderbookStore", "useMarketsStore"]
        },
        {
          "name": "useFutures",
          "description": "Hook for useFutures",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Retrieves the list of all available futures/perpetual markets along with their static configuration (min order size, tick size) and dynamic data (mark price, volume).",
          "example": "import { useFutures } from '@orderly.network/hooks';\n\nexport const FuturesMarketList = () => {\n  // Fetches configuration and data for all futures/perpetual markets\n  const { data } = useFutures();\n\n  return (\n    <div className=\"container mx-auto p-4\">\n      <h1 className=\"text-2xl font-bold mb-6\">Perpetual Markets</h1>\n      \n      <div className=\"grid gap-4\">\n        {data?.map((market) => (\n          <div key={market.symbol} className=\"flex items-center justify-between p-4 border rounded-lg hover:bg-gray-50\">\n            <div>\n              <h3 className=\"font-bold text-lg\">{market.symbol}</h3>\n              <p className=\"text-sm text-gray-500\">\n                Min Size: {market.min_order_size} | Tick Size: {market.tick_size}\n              </p>\n            </div>\n            <div className=\"text-right\">\n              <div className=\"text-lg font-mono\">{market.mark_price}</div>\n              <div className=\"text-sm text-gray-500\">24h Vol: {market.volume_24h}</div>\n            </div>\n          </div>\n        ))}\n      </div>\n    </div>\n  );\n};",
          "notes": [
            "The primary source of truth for which symbols are available to trade.",
            "Contains validation parameters (e.g., `min_order_size`) required for order forms.",
            "Returns a comprehensive array suitable for building market selection tables."
          ],
          "related": ["useMarket", "useOrderbook", "useTrades"]
        },
        {
          "name": "useInternalTransfer",
          "description": "Hook for useInternalTransfer",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Handles the logic for transferring assets between the user's connected wallet and their Orderly trading account (or between sub-accounts). It manages the transaction state and execution.",
          "example": "import { useInternalTransfer } from \"@orderly.network/hooks\";\nimport { useState } from \"react\";\n\nexport const TransferModal = () => {\n  const [amount, setAmount] = useState(\"\");\n  // Hook to handle transfers between Wallet and Orderly Account\n  const { transfer, isLoading } = useInternalTransfer();\n\n  const handleDeposit = async () => {\n    try {\n      // Execute transfer (e.g., from Wallet to Trading Account)\n      await transfer({\n        token: \"USDC\",\n        amount: parseFloat(amount),\n        // direction depends on specific SDK implementation, usually implied by context or explicit param\n      });\n      alert(\"Transfer successful!\");\n      setAmount(\"\");\n    } catch (e) {\n      console.error(\"Transfer failed\", e);\n    }\n  };\n\n  return (\n    <div className=\"flex gap-2\">\n      <input \n        type=\"number\" \n        value={amount} \n        onChange={(e) => setAmount(e.target.value)} \n        placeholder=\"Amount USDC\"\n        className=\"border p-2 rounded\"\n      />\n      <button \n        onClick={handleDeposit} \n        disabled={isLoading}\n        className=\"bg-blue-500 text-white px-4 py-2 rounded disabled:opacity-50\"\n      >\n        {isLoading ? \"Processing...\" : \"Deposit\"}\n      </button>\n    </div>\n  );\n};",
          "notes": [
            "Usually requires the user to have approved the Orderly contract to spend tokens (allowance) if transferring from the wallet.",
            "The `isLoading` state is critical for UI feedback to prevent double-spending or user confusion during blockchain confirmation.",
            "Always wrap the transfer call in a try/catch block to handle network errors or insufficient balance scenarios."
          ],
          "related": ["useHoldingStream", "useAccount", "useAllowance"]
        },
        {
          "name": "useMaintenanceStatus",
          "description": "Hook for useMaintenanceStatus",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Checks the operational status of the Orderly Network. It returns a boolean (or status object) indicating if the system is under maintenance, allowing developers to disable trading features gracefully.",
          "example": "import { useMaintenanceStatus } from \"@orderly.network/hooks\";\n\nexport const TradingInterface = () => {\n  // Checks if the system is currently under maintenance\n  const isMaintenance = useMaintenanceStatus();\n\n  if (isMaintenance) {\n    return (\n      <div className=\"flex h-screen items-center justify-center bg-yellow-50 text-yellow-800\">\n        <div className=\"text-center\">\n          <h1 className=\"text-2xl font-bold mb-2\">System Maintenance</h1>\n          <p>Trading is currently disabled. Please check back later.</p>\n        </div>\n      </div>\n    );\n  }\n\n  return (\n    <div className=\"p-4\">\n      <h1 className=\"text-2xl font-bold mb-4\">Order Book</h1>\n      {/* Main trading UI components go here */}\n      <div>Normal Trading View...</div>\n    </div>\n  );\n};",
          "notes": [
            "Essential for UX; prevents users from placing orders that will fail during scheduled maintenance windows.",
            "Can be used to disable specific actions (like deposits) while allowing read-only data (like market history) if the API permits.",
            "Combine with a polling interval or refresh mechanism if the app stays open for long periods."
          ],
          "related": ["useOrderSubmit", "useAccount", "useMarketList"]
        },
        {
          "name": "useOdosQuote",
          "description": "Hook for useOdosQuote",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Fetches a swap quote using the Odos aggregator integration. This allows you to get the best price execution path for token swaps within the Orderly ecosystem.",
          "example": "import { useOdosQuote } from '@orderly.network/hooks';\n\nexport const SwapQuote = () => {\n  const fromToken = 'USDC';\n  const toToken = 'ETH';\n  const amount = '1000';\n\n  // Fetches a swap quote from the Odos aggregator\n  const { data: quote, isLoading } = useOdosQuote({\n    fromToken,\n    toToken,\n    amount\n  });\n\n  return (\n    <div className=\"bg-blue-50 p-4 rounded\">\n      <h3>Swap Quote</h3>\n      {isLoading ? (\n        <p>Fetching best route...</p>\n      ) : (\n        <div>\n          <p>Input: {amount} {fromToken}</p>\n          <p>Output: <span className=\"font-bold\">{quote?.outAmount} {toToken}</span></p>\n          <p className=\"text-xs text-gray-500\">Gas: {quote?.gasEstimate}</p>\n        </div>\n      )}\n    </div>\n  );\n};",
          "notes": [
            "Used for pre-transaction estimation; does not execute the swap itself.",
            "Returns detailed routing information, including output amount and gas estimates.",
            "Essential for building a 'Swap' interface UI."
          ],
          "related": ["useOdosSwap", "useTokenBalance", "useAllowance"]
        },
        {
          "name": "useOpenInterest",
          "description": "Hook for useOpenInterest",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Fetches Open Interest data for a specific market. Open Interest represents the total number of outstanding derivative contracts (longs + shorts) that are held by market participants.",
          "example": "import { useOpenInterest } from '@orderly.network/hooks';\n\nexport const OpenInterestChart = ({ symbol }: { symbol: string }) => {\n  // Fetches historical or current open interest data for a market\n  const { data: oiData, isLoading } = useOpenInterest(symbol);\n\n  if (isLoading) return <div>Loading OI data...</div>;\n\n  return (\n    <div className=\"chart-container\">\n      <h4>Open Interest: {symbol}</h4>\n      {/* Visualization logic would go here */}\n      <div className=\"h-40 flex items-end space-x-1\">\n        {oiData?.slice(-10).map((point, i) => (\n          <div \n            key={i} \n            className=\"bg-purple-500 w-full\" \n            style={{ height: `${(point.sum / 1000000) * 100}%` }}\n            title={`OI: ${point.sum}`}\n          />\n        ))}\n      </div>\n    </div>\n  );\n};",
          "notes": [
            "Often returns an array of historical data points for charting.",
            "A key indicator of market liquidity and sentiment.",
            "Data is usually aggregated over time intervals (e.g., hourly or daily)."
          ],
          "related": ["useMarket", "useFundingRate", "useMarketsStream"]
        },
        {
          "name": "useSettleSubscription",
          "description": "Hook for useSettleSubscription",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Subscribes to settlement events from the exchange. This is crucial for tracking when positions are settled, funding fees are charged/credited, or PnL is realized.",
          "example": "import { useSettleSubscription } from '@orderly.network/hooks';\nimport { useEffect } from 'react';\n\nexport const SettlementMonitor = () => {\n  // Subscribes to settlement events (e.g., funding fees, PnL settlement)\n  const settleData = useSettleSubscription();\n\n  useEffect(() => {\n    if (settleData) {\n      // Handle settlement logic, e.g., updating local balance or notifying user\n      console.log('Settlement Event:', settleData);\n      // Trigger a toast notification or balance refresh\n    }\n  }, [settleData]);\n\n  return (\n    <div className=\"text-xs text-gray-400\">\n      Monitoring Settlement Events...\n    </div>\n  );\n};",
          "notes": [
            "Settlement events directly impact the user's available balance and position value.",
            "This hook ensures the UI reflects these changes immediately without requiring a page refresh.",
            "Essential for accurate accounting in perpetual futures trading."
          ],
          "related": ["usePositionStream", "useAccount", "usePrivateDataObserver"]
        },
        {
          "name": "useStorageLedgerAddress",
          "description": "Hook for useStorageLedgerAddress",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Retrieves the Orderly Ledger Address associated with the current session from storage. This is the specific address used for trading on the Orderly network, which may differ from the user's wallet address.",
          "example": "import { useStorageLedgerAddress } from '@orderly.network/hooks';\n\nexport const WalletDisplay = () => {\n  // Retrieves the Orderly Ledger Address stored in local storage/browser storage\n  const ledgerAddress = useStorageLedgerAddress();\n\n  return (\n    <div className=\"flex items-center gap-2 p-2 bg-gray-100 rounded\">\n      <span className=\"text-gray-600 text-sm\">Ledger:</span>\n      {ledgerAddress ? (\n        <code className=\"text-xs bg-white px-2 py-1 rounded border\">\n          {ledgerAddress.slice(0, 6)}...{ledgerAddress.slice(-4)}\n        </code>\n      ) : (\n        <span className=\"text-xs text-red-500\">Not Connected</span>\n      )}\n    </div>\n  );\n};",
          "notes": [
            "Useful for debugging connection issues or displaying the specific trading account ID.",
            "Reads from persistent storage, so it can survive page refreshes.",
            "Returns null or undefined if the user has not yet connected/registered a wallet."
          ],
          "related": ["useAccount", "useWallet", "useOrderlyContext"]
        },
        {
          "name": "useTokensInfo",
          "description": "Hook for useTokensInfo",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Retrieves a list of all supported tokens with their metadata, such as display precision (decimals), token logos, and which blockchain networks they support.",
          "example": "import { useTokensInfo } from '@orderly.network/hooks';\n\nexport const AssetList = () => {\n  // Fetches metadata for all supported tokens (chains, decimals, logos)\n  const { tokens, isLoading } = useTokensInfo();\n\n  return (\n    <div className=\"asset-grid\">\n      {tokens?.map((token) => (\n        <div key={token.token_id} className=\"asset-card\">\n          <img src={token.logo} alt={token.symbol} />\n          <h4>{token.symbol}</h4>\n          <p>Decimals: {token.decimals}</p>\n          <div className=\"chains\">\n            {token.chains.map((chain) => (\n              <span key={chain.chain_id} className=\"chain-tag\">\n                {chain.network}\n              </span>\n            ))}\n          </div>\n        </div>\n      ))}\n    </div>\n  );\n};",
          "notes": [
            "Essential for rendering deposit/withdraw forms where the user needs to select a specific network (e.g., Arbitrum vs Optimism).",
            "The `decimals` field here is crucial for formatting user balances correctly in the UI.",
            "Data is usually fetched once and cached, as it doesn't change often."
          ],
          "related": ["useTokenInfo", "useBalance", "useDeposit"]
        },
        {
          "name": "useTokenInfo",
          "description": "Hook for useTokenInfo",
          "installation": "npm install @orderly.network/hooks",
          "usage": "A focused hook to retrieve metadata for a single token. Useful when displaying details for a specific asset without loading the entire token list.",
          "example": "import { useTokenInfo } from '@orderly.network/hooks';\n\nexport const TokenHeader = ({ tokenSymbol }: { tokenSymbol: string }) => {\n  // Fetches metadata for a single token by its symbol\n  const { token, isLoading } = useTokenInfo(tokenSymbol);\n\n  if (isLoading) return <div>Loading token info...</div>;\n  if (!token) return <div>Token not found</div>;\n\n  return (\n    <div className=\"token-header\">\n      <img src={token.logo} alt={token.symbol} style={{ width: '24px', height: '24px' }} />\n      <span>{token.name} ({token.symbol})</span>\n    </div>\n  );\n};",
          "notes": [
            "More efficient than `useTokensInfo` if you only need data for one specific asset.",
            "Returns `null` or `undefined` if the token symbol is not supported by Orderly.",
            "Use this to dynamically determine decimal places for input fields based on the selected asset."
          ],
          "related": ["useTokensInfo", "useTokenBalance", "useWithdraw"]
        },
        {
          "name": "useTransfer",
          "description": "Hook for useTransfer",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Handles the movement of funds between the user's Wallet Account (off-chain/chain balance) and their Trading Account (collateral account) on the Orderly Network.",
          "example": "import { useTransfer } from '@orderly.network/hooks';\nimport { useState } from 'react';\n\nexport const TransferFunds = () => {\n  const { transfer, isLoading } = useTransfer();\n  const [amount, setAmount] = useState('');\n\n  const handleTransfer = async () => {\n    try {\n      // Transfers funds between Trading Account and Wallet Account\n      await transfer({\n        amount: parseFloat(amount),\n        token: 'USDC',\n        type: 'TRADING_TO_WALLET' // or 'WALLET_TO_TRADING'\n      });\n      alert('Transfer successful!');\n    } catch (err) {\n      console.error('Transfer failed', err);\n    }\n  };\n\n  return (\n    <div>\n      <input \n        type=\"number\" \n        value={amount} \n        onChange={(e) => setAmount(e.target.value)} \n        placeholder=\"Amount\" \n      />\n      <button onClick={handleTransfer} disabled={isLoading}>\n        {isLoading ? 'Transferring...' : 'Transfer to Wallet'}\n      </button>\n    </div>\n  );\n};",
          "notes": [
            "Ensure you check the user's available balance before initiating a transfer to prevent UI errors.",
            "The `type` parameter determines the direction: `WALLET_TO_TRADING` adds margin, `TRADING_TO_WALLET` removes it.",
            "Transfers usually require a signature from the connected wallet."
          ],
          "related": ["useBalance", "useWalletAccount", "useAccountInfo"]
        },
        {
          "name": "useBalanceTopic",
          "description": "Hook for useBalanceTopic",
          "installation": "npm install @orderly.network/hooks",
          "usage": "A utility hook that returns the specific topic string required to subscribe to balance updates via the Orderly Network WebSocket API. Useful for custom WebSocket integrations.",
          "example": "import { useBalanceTopic } from '@orderly.network/hooks';\nimport { useEffect } from 'react';\n\nexport const BalanceLogger = () => {\n  // Generates the specific WebSocket topic string for balance updates\n  const topic = useBalanceTopic();\n\n  useEffect(() => {\n    if (topic) {\n      console.log(`Subscribed to topic: ${topic}`);\n      // You can pass this topic to a custom WS client if not using the high-level hooks\n    }\n  }, [topic]);\n\n  return (\n    <div>\n      <p>Current Balance Topic: <code>{topic}</code></p>\n      <p>Use this topic to filter balance updates in the WebSocket stream.</p>\n    </div>\n  );\n};",
          "notes": [
            "This is typically used internally by higher-level hooks like `useWalletSubscription`, but is exposed for advanced usage.",
            "The topic string usually follows a format like `balance` or includes account identifiers.",
            "Useful if you are building a custom data fetching layer and need to know exactly which channel to subscribe to."
          ],
          "related": ["useWalletSubscription", "useAccountInfo", "usePrivateTopic"]
        }
      ]
    },
    {
      "name": "Market Data",
      "patterns": [
        {
          "name": "useFundingRateBySymbol",
          "description": "Hook for useFundingRateBySymbol",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Fetches the funding rate data for a specific trading symbol. This is vital for perpetual futures traders to understand the cost of holding positions.",
          "example": "import { useFundingRateBySymbol } from '@orderly.network/hooks';\n\nexport const FundingRateTicker = ({ symbol }) => {\n  // Fetches the current and predicted funding rate for a specific symbol\n  const { \n    fundingRate, \n    predictedFundingRate, \n    nextFundingTime \n  } = useFundingRateBySymbol(symbol);\n\n  // Helper to format percentage\n  const formatRate = (rate) => {\n    if (!rate) return '-';\n    const percentage = (rate * 100).toFixed(4);\n    return rate >= 0 ? `+${percentage}%` : `${percentage}%`;\n  };\n\n  return (\n    <div className=\"flex flex-col items-end p-2\">\n      <span className=\"text-xs text-gray-500\">Funding / Predicted</span>\n      <div className=\"flex gap-2 text-sm font-mono\">\n        <span className={fundingRate >= 0 ? 'text-green-500' : 'text-red-500'}>\n          {formatRate(fundingRate)}\n        </span>\n        <span className=\"text-gray-400\">/</span>\n        <span className={predictedFundingRate >= 0 ? 'text-green-500' : 'text-red-500'}>\n          {formatRate(predictedFundingRate)}\n        </span>\n      </div>\n      {nextFundingTime && (\n        <span className=\"text-[10px] text-gray-400\">\n          Next: {new Date(nextFundingTime).toLocaleTimeString()}\n        </span>\n      )}\n    </div>\n  );\n};",
          "notes": [
            "Funding rates are typically returned as decimals (e.g., 0.0001), so you must multiply by 100 to display as a percentage.",
            "The 'predicted' rate helps traders anticipate future costs, while the current rate is used for settlement.",
            "Display the nextFundingTime to inform users when the next fee/reward will be applied to their position."
          ],
          "related": ["useOrderEntity", "useOrderEntryFromStore", "useAccountInfo"]
        },
        {
          "name": "useIndexPrice",
          "description": "Hook for useIndexPrice",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Retrieves the Index Price for a specific trading pair. The Index Price is the fair value of the underlying asset, usually derived from an average of spot prices on various exchanges, used to calculate mark prices.",
          "example": "import { useIndexPrice } from \"@orderly.network/hooks\";\n\nexport const PerpPriceDisplay = ({ symbol }: { symbol: string }) => {\n  // Fetches the index price for a specific derivative symbol\n  const { data: indexPrice } = useIndexPrice(symbol);\n\n  return (\n    <div className=\"flex items-center gap-2\">\n      <span className=\"text-gray-500\">Index Price:</span>\n      <span className=\"font-mono font-bold\">\n        {indexPrice ? parseFloat(indexPrice).toFixed(2) : \"---\"}\n      </span>\n    </div>\n  );\n};",
          "notes": [
            "Crucial for derivatives trading to understand the fair value of the contract distinct from the last traded price.",
            "Returns a single value (number or string) representing the price at the time of the fetch or update.",
            "Use this to display the 'Oracle' price to users, helping them understand funding rate calculations."
          ],
          "related": ["useMarkPrice", "useMarkPricesStream", "useFundingRate"]
        },
        {
          "name": "useIndexPricesStream",
          "description": "Hook for useIndexPricesStream",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Streams index prices for multiple symbols simultaneously. It returns a dataset of all prices and a helper function `getIndexPrice` to efficiently query a specific symbol's price from the stream.",
          "example": "import { useIndexPricesStream } from \"@orderly.network/hooks\";\n\nexport const MarketTicker = () => {\n  // Streams index prices for all available symbols\n  const { data, getIndexPrice } = useIndexPricesStream();\n\n  return (\n    <div className=\"grid grid-cols-2 gap-4\">\n      <div>\n        <h4>BTC Price</h4>\n        <p>${getIndexPrice(\"PERP_BTC_USDC\")?.toFixed(2) || \"Loading...\"}</p>\n      </div>\n      <div>\n        <h4>ETH Price</h4>\n        <p>${getIndexPrice(\"PERP_ETH_USDC\")?.toFixed(2) || \"Loading...\"}</p>\n      </div>\n    </div>\n  );\n};",
          "notes": [
            "More efficient than calling `useIndexPrice` multiple times if you need prices for several pairs on one screen.",
            "The `getIndexPrice` helper allows for quick lookups without manually iterating through the `data` object.",
            "Data updates automatically via WebSocket, providing a live ticker tape experience."
          ],
          "related": ["useMarkPricesStream", "useMarketList", "useIndexPrice"]
        },
        {
          "name": "useLeverageBySymbol",
          "description": "Hook for useLeverageBySymbol",
          "installation": "npm install @orderly.network/hooks",
          "usage": "A lightweight hook specifically for retrieving the current leverage setting for a given symbol. Unlike `useLeverage`, it is read-only and does not provide update functions.",
          "example": "import { useLeverageBySymbol } from \"@orderly.network/hooks\";\n\nexport const PositionHeader = ({ symbol }: { symbol: string }) => {\n  // Simple read-only hook to get leverage for a specific symbol\n  const leverage = useLeverageBySymbol(symbol);\n\n  return (\n    <div className=\"flex justify-between items-center border-b pb-2 mb-2\">\n      <h2 className=\"text-xl font-bold\">{symbol}</h2>\n      <div className=\"bg-gray-200 px-3 py-1 rounded text-sm\">\n        Leverage: {leverage || 0}x\n      </div>\n    </div>\n  );\n};",
          "notes": [
            "Ideal for display components (like tables or headers) where you need to show leverage but don't need controls to change it.",
            "Returns `undefined` or `null` if the symbol is invalid or data hasn't loaded yet.",
            "Useful for calculating margin requirements or liquidation prices in read-only views."
          ],
          "related": ["useLeverage", "usePosition", "useMarkPrice"]
        },
        {
          "name": "useMarkPrice",
          "description": "Hook for useMarkPrice",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Retrieves the Mark Price for a specific symbol. The Mark Price is used to determine the unrealized PnL and liquidation threshold of positions, distinct from the Index Price and Last Traded Price.",
          "example": "import { useMarkPrice } from \"@orderly.network/hooks\";\n\nexport const LiquidationWarning = ({ symbol }: { symbol: string }) => {\n  // Fetches the Mark Price used for liquidation calculations\n  const { data: markPrice } = useMarkPrice(symbol);\n\n  return (\n    <div className=\"text-xs text-gray-500 mt-1\">\n      Mark Price: {markPrice ? parseFloat(markPrice).toFixed(2) : \"Loading...\"}\n    </div>\n  );\n};",
          "notes": [
            "Mark Price is typically calculated as a weighted average of the Index Price and moving average of the Mid-Price to prevent manipulation.",
            "This is the price you should use when calculating user equity and margin ratio for liquidation warnings.",
            "Updates frequently; ensure your display components handle rapid number changes gracefully."
          ],
          "related": ["useIndexPrice", "usePosition", "useMarkPricesStream"]
        },
        {
          "name": "useMarkPricesStream",
          "description": "Hook for useMarkPricesStream",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Streams Mark Prices for all available trading pairs. This is the most efficient way to monitor the fair value of multiple positions simultaneously for real-time PnL calculation.",
          "example": "import { useMarkPricesStream } from \"@orderly.network/hooks\";\n\nexport const PortfolioSummary = () => {\n  // Streams mark prices for all symbols to calculate portfolio value in real-time\n  const markPrices = useMarkPricesStream();\n\n  // Example: Calculate total value assuming a hypothetical position map\n  // In a real app, you would combine this with usePositionsStream\n  const calculatePortfolioValue = () => {\n    if (!markPrices) return 0;\n    // Logic to sum up (quantity * markPrice) for all positions\n    return Object.values(markPrices).reduce((acc, price) => acc + price, 0); \n  };\n\n  return (\n    <div className=\"p-4 bg-white shadow rounded\">\n      <h3 className=\"text-gray-500 text-sm\">Total Portfolio Value (Est.)</h3>\n      <p className=\"text-2xl font-bold text-green-600\">\n        ${calculatePortfolioValue().toFixed(2)}\n      </p>\n      <p className=\"text-xs text-gray-400 mt-1\">Updated via Mark Price Stream</p>\n    </div>\n  );\n};",
          "notes": [
            "Crucial for building a real-time 'Portfolio' or 'Positions' tab where values change every second.",
            "Returns an object/map keyed by symbol, allowing O(1) lookup time.",
            "Since this is a stream, ensure your component is optimized to handle re-renders, or memoize the calculated values."
          ],
          "related": ["usePositionsStream", "useIndexPricesStream", "useHoldingStream"]
        },
        {
          "name": "useMarketList",
          "description": "Hook for useMarketList",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Fetches the comprehensive list of supported markets (trading pairs). This includes metadata such as symbol, display name, minimum order size, tick size, and market type (Spot/Perp).",
          "example": "import { useMarketList } from \"@orderly.network/hooks\";\n\nexport const MarketSelector = ({ onSelect }: { onSelect: (symbol: string) => void }) => {\n  // Fetches the list of all available trading pairs and their configs\n  const markets = useMarketList();\n\n  if (!markets || markets.length === 0) return <div>Loading markets...</div>;\n\n  return (\n    <select \n      onChange={(e) => onSelect(e.target.value)}\n      className=\"w-full p-2 border border-gray-300 rounded bg-white\"\n    >\n      <option value=\"\">Select a Market</option>\n      {markets.map((market) => (\n        <option key={market.symbol} value={market.symbol}>\n          {market.display_name || market.symbol} \n          {market.type === 'PERP' ? ' (Perp)' : ' (Spot)'}\n        </option>\n      ))}\n    </select>\n  );\n};",
          "notes": [
            "This is the primary source of truth for populating dropdowns and market selectors in your app.",
            "Contains vital validation data like `min_order_qty` and `tick_size` which must be used in order forms to prevent API rejections.",
            "The data is relatively static, so it's often cached or fetched once on app mount."
          ],
          "related": ["useSymbolInfo", "useOrderBook", "useTrades"]
        },
        {
          "name": "useMarketMap",
          "description": "Hook for useMarketMap",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Retrieves the global map of all available markets from the Orderly store. This is useful for populating dropdown selectors, displaying asset lists, or looking up static market metadata like tick size or step size without making individual API calls.",
          "example": "import { useMarketMap } from '@orderly.network/hooks';\n\nexport const MarketList = () => {\n  // Accesses the global store containing all market configurations (symbols, tickers, etc.)\n  const marketMap = useMarketMap();\n\n  return (\n    <div>\n      <h3>Available Markets</h3>\n      <ul>\n        {Object.values(marketMap).map((market) => (\n          <li key={market.symbol}>\n            {market.symbol} - {market.display_name}\n          </li>\n        ))}\n      </ul>\n    </div>\n  );\n};",
          "notes": [
            "Returns a key-value object where keys are symbols (e.g., 'PERP_ETH_USDC') and values are market info objects.",
            "Data is usually cached in the Zustand store, making access synchronous and extremely fast.",
            "Use this to derive lists of symbols for UI selectors rather than hardcoding them."
          ],
          "related": ["useMarket", "useMarketsStream", "useMarketsStore"]
        },
        {
          "name": "useMarket",
          "description": "Hook for useMarket",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Fetches detailed static and semi-static data for a specific trading pair. It returns metadata such as decimal places (`quote_dp`), creation timestamp, and current open interest figures.",
          "example": "import { useMarket } from '@orderly.network/hooks';\n\nexport const MarketInfo = ({ symbol }: { symbol: string }) => {\n  // Fetches specific details for a single market\n  const { quote_dp, created_time, openInterest } = useMarket(symbol);\n\n  if (!quote_dp) return <div>Loading market info...</div>;\n\n  return (\n    <div className=\"p-4 border rounded\">\n      <h2>{symbol}</h2>\n      <p>Decimals: {quote_dp}</p>\n      <p>Created: {new Date(created_time).toLocaleDateString()}</p>\n      <p>Open Interest: {openInterest}</p>\n    </div>\n  );\n};",
          "notes": [
            "Requires a `symbol` string (e.g., 'PERP_BTC_USDC') as an argument.",
            "Useful for determining precision (dp) for formatting price inputs or displays.",
            "Combines static config data with some dynamic data like Open Interest."
          ],
          "related": ["useMarketMap", "useOpenInterest", "useMarketsStream"]
        },
        {
          "name": "useMarketsStore",
          "description": "Hook for useMarketsStore",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Provides access to the global markets state store, specifically for interacting with user preferences like favorited markets. It exposes actions to modify the state (e.g., `updateFavorites`).",
          "example": "import { useMarketsStore } from '@orderly.network/hooks';\n\nexport const MarketFavoriteToggle = ({ symbol }: { symbol: string }) => {\n  // Access the Zustand store to manage user favorites\n  const { updateFavorites, favorites } = useMarketsStore();\n\n  const isFavorite = favorites.includes(symbol);\n\n  const handleToggle = () => {\n    // Toggle the symbol in the favorites list\n    updateFavorites(symbol);\n  };\n\n  return (\n    <button \n      onClick={handleToggle}\n      className={isFavorite ? 'text-yellow-500' : 'text-gray-400'}\n    >\n      {isFavorite ? '★' : '☆'}\n    </button>\n  );\n};",
          "notes": [
            "This is a state management hook (Zustand), not a data fetching hook.",
            "Allows you to persist user preferences (like favorite trading pairs) locally.",
            "Changes made here reflect immediately across all components consuming the store."
          ],
          "related": ["useMarketMap", "useMarketsStream", "useMarkets"]
        },
        {
          "name": "useMarketsStream",
          "description": "Hook for useMarketsStream",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Subscribes to the public ticker stream for all supported markets. It returns real-time data including the last price, 24h volume, and 24h price change for every market.",
          "example": "import { useMarketsStream } from '@orderly.network/hooks';\n\nexport const MarketTickerTable = () => {\n  // Subscribes to ticker updates for all markets\n  const { data } = useMarketsStream();\n\n  return (\n    <div className=\"overflow-x-auto\">\n      <table className=\"w-full text-sm\">\n        <thead>\n          <tr>\n            <th>Symbol</th>\n            <th>Last Price</th>\n            <th>24h Change</th>\n          </tr>\n        </thead>\n        <tbody>\n          {data?.map((ticker) => (\n            <tr key={ticker.symbol}>\n              <td>{ticker.symbol}</td>\n              <td>{parseFloat(ticker.last_price).toFixed(2)}</td>\n              <td className={parseFloat(ticker.change_24h) >= 0 ? 'text-green-500' : 'text-red-500'}>\n                {ticker.change_24h}%\n              </td>\n            </tr>\n          ))}\n        </tbody>\n      </table>\n    </div>\n  );\n};",
          "notes": [
            "Provides a bulk update of market data, ideal for a 'Market Overview' or 'All Markets' page.",
            "Data updates frequently via WebSocket; ensure your UI handles re-renders efficiently.",
            "The `data` array contains objects keyed by market symbol."
          ],
          "related": ["useMarketTradeStream", "useMarketMap", "useMarket"]
        },
        {
          "name": "useRwaSymbolsInfo",
          "description": "Hook for useRwaSymbolsInfo",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Retrieves metadata for Real World Asset (RWA) symbols, specifically focusing on trading hours. It determines if the market is currently open and when the next session begins or ends.",
          "example": "import { useRwaSymbolsInfo } from '@orderly.network/hooks';\n\nexport const MarketStatus = ({ symbol }: { symbol: string }) => {\n  // Fetches specific info for Real World Assets (stocks, forex, etc.)\n  const { isRwa, open, nextOpen, nextClose } = useRwaSymbolsInfo(symbol);\n\n  if (!isRwa) {\n    return <div className=\"text-sm text-gray-500\">Crypto Market (24/7)</div>;\n  }\n\n  return (\n    <div className={`p-3 rounded border ${open ? 'border-green-200 bg-green-50' : 'border-red-200 bg-red-50'}`}>\n      <div className=\"flex items-center gap-2 mb-1\">\n        <span className={`w-2 h-2 rounded-full ${open ? 'bg-green-500' : 'bg-red-500'}`} />\n        <span className=\"font-bold text-sm\">\n          {open ? 'Market Open' : 'Market Closed'}\n        </span>\n      </div>\n      \n      {!open && (\n        <div className=\"text-xs text-gray-600\">\n          <p>Next Open: {new Date(nextOpen).toLocaleString()}</p>\n          <p>Next Close: {new Date(nextClose).toLocaleString()}</p>\n        </div>\n      )}\n    </div>\n  );\n};",
          "notes": [
            "Essential for RWA markets (e.g., US Stocks) which do not trade 24/7 like crypto.",
            "Helps prevent order placement errors by checking the 'open' boolean before allowing trades.",
            "Returns ISO timestamps for scheduling purposes."
          ],
          "related": ["useRwaSymbolsInfoStore", "useInitRwaSymbolsRuntime", "useSymbolInfo"]
        },
        {
          "name": "useRwaSymbolsInfoStore",
          "description": "Hook for useRwaSymbolsInfoStore",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Provides direct access to the RWA symbols info state store. This is useful for accessing cached market hours data without triggering re-fetches or for accessing the state globally across the application.",
          "example": "import { useRwaSymbolsInfoStore } from '@orderly.network/hooks';\n\nexport const RwaMarketList = () => {\n  // Accesses the global store of RWA symbol info (Zustand pattern)\n  // Useful for getting status of multiple symbols without individual fetches\n  const { isRwa, open, nextOpen } = useRwaSymbolsInfoStore();\n\n  // Example: Check a specific symbol from the store if the store supports lookup\n  // or simply display global RWA status if applicable.\n  // Note: Usage depends on store structure, often used to read cached data.\n\n  return (\n    <div className=\"bg-gray-50 p-4 rounded\">\n      <h3 className=\"font-bold mb-2\">RWA Market Status</h3>\n      <div className=\"text-sm\">\n        <p>Global RWA Active: {isRwa ? 'Yes' : 'No'}</p>\n        <p>Next Session Open: {nextOpen ? new Date(nextOpen).toLocaleTimeString() : 'N/A'}</p>\n      </div>\n    </div>\n  );\n};",
          "notes": [
            "Reads from the global state (likely Zustand) initialized by other hooks.",
            "More performant than `useRwaSymbolsInfo` if you just need to read data without side effects.",
            "Ensure `useInitRwaSymbolsRuntime` has been called to populate this store."
          ],
          "related": ["useInitRwaSymbolsRuntime", "useRwaSymbolsInfo", "useSymbolInfo"]
        },
        {
          "name": "useInitRwaSymbolsRuntime",
          "description": "Hook for useInitRwaSymbolsRuntime",
          "installation": "npm install @orderly.network/hooks",
          "usage": "An initialization hook that fetches and sets up the runtime data for RWA (Real World Asset) symbols. It ensures that trading hours and market status information are available globally for the application.",
          "example": "import { useEffect } from 'react';\nimport { useInitRwaSymbolsRuntime } from '@orderly.network/hooks';\n\n// This hook is typically used in a root layout or provider component\nexport const RwaProvider = () => {\n  // Initializes the RWA symbols runtime, fetching trading hours and schedules\n  const { isRwa, open, nextOpen } = useInitRwaSymbolsRuntime();\n\n  useEffect(() => {\n    if (isRwa) {\n      console.log('RWA Runtime initialized. Next market open:', nextOpen);\n    }\n  }, [isRwa, nextOpen]);\n\n  // This component usually doesn't render UI itself, just ensures data is loaded\n  return null;\n};\n\n// Usage in App.tsx\n// <RwaProvider />\n// <RestOfApp />",
          "notes": [
            "Should be called once at the root level of the application (e.g., in App.tsx or a custom Provider).",
            "It populates the store used by `useRwaSymbolsInfoStore`.",
            "Necessary for any RWA-related logic to function correctly within the SDK."
          ],
          "related": ["useRwaSymbolsInfoStore", "useRwaSymbolsInfo", "useSymbolInfo"]
        },
        {
          "name": "useSymbolInfo",
          "description": "Hook for useSymbolInfo",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Retrieves detailed metadata for a specific trading symbol. This includes trading rules like minimum order size, tick size (price precision), and maximum leverage.",
          "example": "import { useSymbolInfo } from '@orderly.network/hooks';\n\nexport const TradingRules = ({ symbol }: { symbol: string }) => {\n  // Fetches static or semi-static information about a trading symbol\n  const symbolInfo = useSymbolInfo(symbol);\n\n  if (!symbolInfo) return <div>Loading symbol info...</div>;\n\n  return (\n    <div className=\"text-sm space-y-2\">\n      <div className=\"flex justify-between border-b pb-1\">\n        <span className=\"text-gray-500\">Min Order Size</span>\n        <span className=\"font-medium\">{symbolInfo.min_order_size}</span>\n      </div>\n      <div className=\"flex justify-between border-b pb-1\">\n        <span className=\"text-gray-500\">Tick Size (Precision)</span>\n        <span className=\"font-medium\">{symbolInfo.tick_size}</span>\n      </div>\n      <div className=\"flex justify-between border-b pb-1\">\n        <span className=\"text-gray-500\">Max Leverage</span>\n        <span className=\"font-medium\">{symbolInfo.max_leverage}x</span>\n      </div>\n    </div>\n  );\n};",
          "notes": [
            "Critical for form validation (e.g., ensuring user input respects min_order_size and tick_size).",
            "Data is often fetched once and cached, but should be checked before allowing order submission.",
            "Helps in dynamically configuring input fields (step amounts) based on the symbol."
          ],
          "related": ["useOrderbookStream", "useRwaSymbolsInfo", "useCreateOrder"]
        },
        {
          "name": "useSymbolLeverage",
          "description": "Hook for useSymbolLeverage",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Fetches the current leverage setting for a specific trading symbol and allows you to update it. It handles the API interaction to modify margin requirements.",
          "example": "import { useSymbolLeverage } from '@orderly.network/hooks';\n\nexport const LeverageControl = ({ symbol }: { symbol: string }) => {\n  // Fetches current leverage and provides a method to update it\n  const { leverage, maxLeverage, updateLeverage, isLoading } = useSymbolLeverage(symbol);\n\n  const handleLeverageChange = async (e: React.ChangeEvent<HTMLInputElement>) => {\n    const newVal = parseInt(e.target.value);\n    if (newVal > 0) {\n      await updateLeverage(newVal);\n    }\n  };\n\n  if (isLoading) return <div>Loading leverage settings...</div>;\n\n  return (\n    <div className=\"leverage-control\">\n      <label>Current Leverage: {leverage}x</label>\n      <input \n        type=\"range\" \n        min=\"1\" \n        max={maxLeverage} \n        value={leverage} \n        onChange={handleLeverageChange} \n      />\n      <span>Max: {maxLeverage}x</span>\n    </div>\n  );\n};",
          "notes": [
            "Always check the `maxLeverage` returned by the hook before allowing user input to prevent API errors.",
            "The `updateLeverage` function is asynchronous; ensure you handle loading states to prevent double-submissions.",
            "Changing leverage may affect your existing position's liquidation price immediately."
          ],
          "related": ["usePosition", "useAccountInfo", "useSymbolInfo"]
        },
        {
          "name": "useSymbolsInfo",
          "description": "Hook for useSymbolsInfo",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Retrieves static information for all trading symbols, such as tick size, lot size, minimum order size, and asset pairs. Essential for building order forms and market lists.",
          "example": "import { useSymbolsInfo } from '@orderly.network/hooks';\n\nexport const MarketSelector = () => {\n  // Fetches comprehensive metadata for all available trading pairs\n  const { data: symbols, isLoading, error } = useSymbolsInfo();\n\n  if (isLoading) return <div>Loading markets...</div>;\n  if (error) return <div>Failed to load markets.</div>;\n\n  return (\n    <select className=\"market-select\">\n      {symbols?.map((symbol) => (\n        <option key={symbol.symbol_id} value={symbol.symbol_id}>\n          {symbol.base_asset}/{symbol.quote_asset} ({symbol.type})\n        </option>\n      ))}\n    </select>\n  );\n};",
          "notes": [
            "This data is relatively static but should be fetched on app mount to ensure trading rules are up to date.",
            "Use the `min_order_qty` and `tick_size` fields from this data to validate user order inputs before submission.",
            "The return type is usually an array of objects containing detailed symbol configurations."
          ],
          "related": ["useSymbolInfo", "useTokenInfo", "useOrderBook"]
        },
        {
          "name": "useSymbolsInfoStore",
          "description": "Hook for useSymbolsInfoStore",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Provides direct access to the Zustand store managing symbol information. This is ideal for performance optimization, avoiding redundant network requests when multiple components need symbol data.",
          "example": "import { useSymbolsInfoStore } from '@orderly.network/hooks';\n\nexport const SymbolPriceDisplay = ({ symbolId }: { symbolId: string }) => {\n  // Access the global Zustand store for symbol data without fetching directly\n  // Useful for accessing data already fetched by other components\n  const getSymbol = useSymbolsInfoStore((state) => state.getSymbol);\n  \n  const symbolData = getSymbol(symbolId);\n\n  if (!symbolData) {\n    return <span>Symbol {symbolId} not found</span>;\n  }\n\n  return (\n    <div>\n      <h3>{symbolData.base_asset}/{symbolData.quote_asset}</h3>\n      <p>Min Order: {symbolData.min_order_qty}</p>\n    </div>\n  );\n};",
          "notes": [
            "This relies on the store being populated, usually by a parent component using `useSymbolsInfo` or a provider.",
            "Use selectors (e.g., `state => state.symbols`) to prevent unnecessary re-renders of components.",
            "It provides helper functions like `getSymbol(id)` for quick lookups."
          ],
          "related": ["useSymbolsInfo", "useTokenInfoStore", "useOrderlyConfig"]
        }
      ]
    },
    {
      "name": "WebSocket & Streaming",
      "patterns": [
        {
          "name": "useWSObserver",
          "description": "Hook for useWSObserver",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Monitors the health and state of the Orderly Network WebSocket connection. It is useful for displaying real-time connection status to the user or debugging connectivity issues.",
          "example": "import { useWSObserver } from \"@orderly.network/hooks\";\n\nexport const ConnectionStatus = () => {\n  // Observes the underlying WebSocket connection state\n  // Returns the current status of the socket connection\n  const { status } = useWSObserver();\n\n  const getStatusColor = () => {\n    switch (status) {\n      case \"connected\": return \"text-green-500\";\n      case \"connecting\": return \"text-yellow-500\";\n      case \"disconnected\": return \"text-red-500\";\n      default: return \"text-gray-500\";\n    }\n  };\n\n  return (\n    <div className=\"flex items-center gap-2\">\n      <span className={`w-2 h-2 rounded-full ${getStatusColor()}`} />\n      <span className=\"text-sm font-medium capitalize\">{status}</span>\n    </div>\n  );\n};",
          "notes": [
            "This is an internal hook primarily used to track the lifecycle of the socket (connecting, open, closed).",
            "It does not return message data; use specific subscription hooks (like useBalanceSubscription) for data.",
            "Status changes are reactive, allowing UI components to update instantly when connection drops or recovers."
          ],
          "related": [
            "useBalanceSubscription",
            "useOrderbookSubscription",
            "useAccountSubscription"
          ]
        },
        {
          "name": "useHoldingStream",
          "description": "Hook for useHoldingStream",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Streams the user's account holdings (balances) and frozen assets (locked in open orders) in real-time. This is essential for displaying up-to-date wallet information without manual refreshing.",
          "example": "import { useHoldingStream } from \"@orderly.network/hooks\";\n\nexport const WalletBalance = () => {\n  // Subscribes to real-time balance updates via WebSocket\n  const { holding, frozen } = useHoldingStream();\n\n  if (!holding) return <div>Loading balances...</div>;\n\n  return (\n    <div className=\"p-4 border rounded\">\n      <h3 className=\"font-bold mb-2\">Asset Overview</h3>\n      <ul>\n        {Object.entries(holding).map(([token, amount]) => {\n          const frozenAmount = frozen?.[token] || 0;\n          const available = amount - frozenAmount;\n\n          return (\n            <li key={token} className=\"flex justify-between py-1\">\n              <span>{token}</span>\n              <span>\n                Available: {available.toFixed(4)} \n                {frozenAmount > 0 && <span className=\"text-red-500\"> (Frozen: {frozenAmount})</span>}\n              </span>\n            </li>\n          );\n        })}\n      </ul>\n    </div>\n  );\n};",
          "notes": [
            "Provides real-time updates via WebSocket, ensuring the UI reflects balance changes immediately after fills or deposits.",
            "The 'frozen' return value represents funds locked in open orders; subtract this from 'holding' to get available balance.",
            "Handles connection lifecycle automatically, but you should still check if the data exists before rendering."
          ],
          "related": ["useAccount", "useInternalTransfer", "useOrderStream"]
        },
        {
          "name": "usePrivateDataObserver",
          "description": "Hook for usePrivateDataObserver",
          "installation": "npm install @orderly.network/hooks",
          "usage": "A generic observer for private user data streams. It is often used to watch for specific data types like order history, trade history, or liquidation events, returning structured rows for tabular display.",
          "example": "import { usePrivateDataObserver } from '@orderly.network/hooks';\n\nexport const OrderHistory = () => {\n  // Observes private data streams like orders, fills, or positions\n  // 'rows' typically contains the array of data, 'row' might be the latest update\n  const { rows } = usePrivateDataObserver('ORDER'); // Assuming 'ORDER' as a type filter\n\n  return (\n    <div className=\"overflow-x-auto\">\n      <table className=\"min-w-full text-left text-sm whitespace-nowrap\">\n        <thead className=\"uppercase tracking-wider border-b bg-gray-50\">\n          <tr>\n            <th className=\"px-6 py-3\">Symbol</th>\n            <th className=\"px-6 py-3\">Side</th>\n            <th className=\"px-6 py-3\">Quantity</th>\n            <th className=\"px-6 py-3\">Status</th>\n          </tr>\n        </thead>\n        <tbody>\n          {rows && rows.length > 0 ? (\n            rows.map((order: any, index: number) => (\n              <tr key={index} className=\"border-b hover:bg-gray-50\">\n                <td className=\"px-6 py-4\">{order.symbol}</td>\n                <td className={`px-6 py-4 ${order.side === 'BUY' ? 'text-green-500' : 'text-red-500'}`}>\n                  {order.side}\n                </td>\n                <td className=\"px-6 py-4\">{order.quantity}</td>\n                <td className=\"px-6 py-4\">{order.status}</td>\n              </tr>\n            ))\n          ) : (\n            <tr>\n              <td colSpan={4} className=\"px-6 py-4 text-center text-gray-400\">\n                No orders found\n              </td>\n            </tr>\n          )}\n        </tbody>\n      </table>\n    </div>\n  );\n};",
          "notes": [
            "This hook is highly efficient for rendering lists (tables) of user activity.",
            "It abstracts the websocket connection logic, providing direct access to data arrays.",
            "Ensure you handle loading states or empty arrays before rendering to avoid UI errors."
          ],
          "related": ["usePositionStream", "useAccount", "useOrderStream"]
        },
        {
          "name": "usePublicDataObserver",
          "description": "Hook for usePublicDataObserver",
          "installation": "npm install @orderly.network/hooks",
          "usage": "A generic hook for observing public market data streams via websocket. It can be configured to watch for trades, tickers, or candle updates depending on the implementation parameters.",
          "example": "import { usePublicDataObserver } from '@orderly.network/hooks';\n\nexport const RecentTrades = ({ symbol }: { symbol: string }) => {\n  // Observes public data streams such as recent trades or tickers\n  const data = usePublicDataObserver('TRADE', symbol);\n\n  return (\n    <div className=\"h-64 overflow-y-auto\">\n      <h3 className=\"text-sm font-semibold mb-2\">Recent Trades</h3>\n      <ul className=\"space-y-1\">\n        {data?.map((trade: any, i: number) => (\n          <li key={i} className=\"flex justify-between text-xs px-2 py-1 hover:bg-gray-100 rounded\">\n            <span className={trade.side === 'BUY' ? 'text-green-600' : 'text-red-600'}>\n              {trade.price}\n            </span>\n            <span className=\"text-gray-500\">{trade.size}</span>\n            <span className=\"text-gray-400\">\n              {new Date(trade.timestamp).toLocaleTimeString()}\n            </span>\n          </li>\n        ))}\n      </ul>\n    </div>\n  );\n};",
          "notes": [
            "Provides real-time updates without the need for HTTP polling.",
            "Useful for building components like 'Recent Trades', 'Market Tickers', or 'Price Tickers'.",
            "Data structure depends on the specific topic (e.g., 'TRADE' vs 'TICKER') requested."
          ],
          "related": ["useOrderbookStream", "useSymbolInfo", "useCandlestickStream"]
        },
        {
          "name": "useTickerStream",
          "description": "Hook for useTickerStream",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Establishes a WebSocket connection to receive real-time market data updates, including last price, 24h volume, and price change percentages for all or specific symbols.",
          "example": "import { useTickerStream } from '@orderly.network/hooks';\n\nexport const TickerList = () => {\n  // Subscribes to a WebSocket stream for real-time 24hr ticker data\n  const { data, isLoading } = useTickerStream();\n\n  if (isLoading) return <div>Connecting to market feed...</div>;\n\n  return (\n    <table className=\"ticker-table\">\n      <thead>\n        <tr>\n          <th>Symbol</th>\n          <th>Last Price</th>\n          <th>24h Change</th>\n        </tr>\n      </thead>\n      <tbody>\n        {data?.map((ticker) => (\n          <tr key={ticker.symbol}>\n            <td>{ticker.symbol}</td>\n            <td>{parseFloat(ticker.last_price).toFixed(4)}</td>\n            <td style={{ color: parseFloat(ticker.change_24h) >= 0 ? 'green' : 'red' }}>\n              {parseFloat(ticker.change_24h).toFixed(2)}%\n            </td>\n          </tr>\n        ))}\n      </tbody>\n    </table>\n  );\n};",
          "notes": [
            "This hook manages the WebSocket lifecycle automatically (connecting and disconnecting based on component mount).",
            "Data updates frequently; ensure your UI rendering is optimized to handle rapid prop changes.",
            "Use this for displaying live market overviews rather than single-order execution data."
          ],
          "related": ["useOrderBookStream", "useTradeStream", "useCandlestickStream"]
        },
        {
          "name": "useWS",
          "description": "Hook for useWS",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Manages the WebSocket connection to the Orderly Network for real-time data. It provides methods to subscribe to topics, send messages, and listen for incoming data streams.",
          "example": "import { useWS } from '@orderly.network/hooks';\nimport { useEffect, useState } from 'react';\n\nexport const OrderBook = () => {\n  // useWS typically returns a function to subscribe or a socket context\n  const { on, off, send } = useWS();\n  const [bids, setBids] = useState([]);\n\n  useEffect(() => {\n    // Define event handler for orderbook updates\n    const handleOrderBook = (data) => {\n      if (data.topic === 'orderbook') {\n        setBids(data.data.bids);\n      }\n    };\n\n    // Subscribe to the event\n    on('orderbook', handleOrderBook);\n\n    // Subscribe to the channel via WebSocket message\n    send({\n      id: 1,\n      event: 'subscribe',\n      topic: 'orderbook',\n      params: { symbol: 'PERP_ETH_USDC' }\n    });\n\n    // Cleanup on unmount\n    return () => {\n      off('orderbook', handleOrderBook);\n    };\n  }, [on, off, send]);\n\n  return (\n    <div>\n      <h3>Order Book Bids</h3>\n      <ul>\n        {bids.slice(0, 5).map((bid, index) => (\n          <li key={index}>{bid[0]} : {bid[1]}</li>\n        ))}\n      </ul>\n    </div>\n  );\n};",
          "notes": [
            "Essential for building high-frequency trading interfaces or real-time charting.",
            "Always clean up subscriptions (using `off`) in the useEffect cleanup to prevent memory leaks.",
            "Handles the connection lifecycle (connect/disconnect/reconnect) internally."
          ],
          "related": ["useWsStatus", "useAccount"]
        },
        {
          "name": "useWsStatus",
          "description": "Hook for useWsStatus",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Monitors and returns the real-time status of the WebSocket connection. This is vital for providing feedback to users about whether they are receiving live market data.",
          "example": "import { useWsStatus } from '@orderly.network/hooks';\n\nexport const ConnectionIndicator = () => {\n  // Returns the current status of the WebSocket connection\n  const status = useWsStatus();\n\n  const getStatusColor = () => {\n    switch (status) {\n      case 'connected': return 'green';\n      case 'connecting': return 'yellow';\n      case 'disconnected': return 'red';\n      default: return 'gray';\n    }\n  };\n\n  return (\n    <div style={{ display: 'flex', alignItems: 'center', gap: '8px' }}>\n      <div \n        style={{ \n          width: '10px', \n          height: '10px', \n          borderRadius: '50%', \n          backgroundColor: getStatusColor() \n        }} \n      />\n      <span>WS Status: {status}</span>\n    </div>\n  );\n};",
          "notes": [
            "Status typically includes 'connected', 'connecting', 'disconnected', and 'error'.",
            "Use this to trigger UI alerts or toasts when the connection is lost.",
            "Helps users distinguish between a frozen market and a connection issue."
          ],
          "related": ["useWS", "useNetworkInfo"]
        }
      ]
    },
    {
      "name": "Analytics",
      "patterns": [
        {
          "name": "useAssetsHistory",
          "description": "Hook for useAssetsHistory",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Retrieves the historical value of the user's assets or portfolio equity. This is essential for rendering PnL charts or account growth over time.",
          "example": "import { useAssetsHistory } from \"@orderly.network/hooks\";\n\nexport const AssetHistoryChart = () => {\n  // Fetches historical asset/portfolio value over time\n  const { data, isLoading, error } = useAssetsHistory({\n    // Optional: pass pagination or time range params if supported by the SDK version\n  });\n\n  if (isLoading) return <div>Loading history...</div>;\n  if (error) return <div>Error loading history</div>;\n\n  return (\n    <div className=\"p-4 border rounded\">\n      <h3 className=\"font-bold mb-2\">Portfolio History</h3>\n      <ul>\n        {data?.map((entry, index) => (\n          <li key={index} className=\"flex justify-between text-sm\">\n            <span>{new Date(entry.timestamp).toLocaleDateString()}</span>\n            <span>{entry.total_equity} USD</span>\n          </li>\n        ))}\n      </ul>\n    </div>\n  );\n};",
          "notes": [
            "Returns an array of historical data points, usually containing timestamps and equity values.",
            "Useful for visualizing trading performance or account balance trends.",
            "May require the user to be authenticated (wallet connected) to fetch specific account data."
          ],
          "related": ["useStatisticsDaily", "useAccount", "useBalanceSubscription"]
        },
        {
          "name": "useDistributionHistory",
          "description": "Hook for useDistributionHistory",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Fetches the history of reward distributions (e.g., trading fee rebates or staking rewards) paid to the user. It often includes summary metrics like the current annual rate.",
          "example": "import { useDistributionHistory } from \"@orderly.network/hooks\";\n\nexport const RewardsList = () => {\n  // Fetches history of fee distributions or staking rewards\n  // Returns data including annual_rate and specific distribution events\n  const { data } = useDistributionHistory();\n\n  return (\n    <div className=\"bg-gray-50 p-4 rounded\">\n      <h3 className=\"font-semibold mb-4\">Distribution History</h3>\n      \n      {/* Displaying the aggregate annual rate if available in the summary */}\n      {data?.summary && (\n        <div className=\"mb-4 p-2 bg-blue-100 rounded\">\n          Current Annual Rate: <strong>{data.summary.annual_rate}%</strong>\n        </div>\n      )}\n\n      <table className=\"w-full text-sm\">\n        <thead>\n          <tr>\n            <th className=\"text-left\">Date</th>\n            <th className=\"text-left\">Amount</th>\n            <th className=\"text-left\">Asset</th>\n          </tr>\n        </thead>\n        <tbody>\n          {data?.history?.map((item, i) => (\n            <tr key={i}>\n              <td>{new Date(item.time).toLocaleDateString()}</td>\n              <td>{item.amount}</td>\n              <td>{item.token}</td>\n            </tr>\n          ))}\n        </tbody>\n      </table>\n    </div>\n  );\n};",
          "notes": [
            "The return value includes `annual_rate`, which represents the estimated yield based on recent distributions.",
            "Data is useful for calculating ROI on staked or provided liquidity.",
            "Ensure the user has connected their wallet to view personal distribution history."
          ],
          "related": ["useFundingFeeHistory", "useStatisticsDaily", "useVaultsHistory"]
        },
        {
          "name": "useFundingFeeHistory",
          "description": "Hook for useFundingFeeHistory",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Retrieves the history of funding fees for perpetual trading positions. This helps users track the cost or profit derived from the funding rate mechanism over time.",
          "example": "import { useFundingFeeHistory } from \"@orderly.network/hooks\";\n\nexport const FundingFeeTable = () => {\n  // Fetches history of funding fees paid or received for perpetual futures\n  // Returns data including annual_rate summaries\n  const { data, isLoading } = useFundingFeeHistory();\n\n  if (isLoading) return <div>Loading fees...</div>;\n\n  return (\n    <div className=\"w-full\">\n      <h3 className=\"text-lg font-bold mb-3\">Funding Fees</h3>\n      \n      {data && data.length > 0 ? (\n        <div className=\"overflow-x-auto\">\n          <table className=\"min-w-full text-sm border\">\n            <thead className=\"bg-gray-100\">\n              <tr>\n                <th className=\"p-2 text-left\">Symbol</th>\n                <th className=\"p-2 text-left\">Time</th>\n                <th className=\"p-2 text-right\">Amount</th>\n                <th className=\"p-2 text-right\">Rate</th>\n              </tr>\n            </thead>\n            <tbody>\n              {data.map((fee, index) => (\n                <tr key={index} className=\"border-t\">\n                  <td className=\"p-2\">fee.symbol}</td>\n                  <td className=\"p-2\">{new Date(fee.timestamp).toLocaleString()}</td>\n                  <td className={`p-2 text-right ${fee.amount > 0 ? 'text-green-500' : 'text-red-500'}`}>\n                    {fee.amount}\n                  </td>\n                  <td className=\"p-2 text-right\">\n                    {fee.annual_rate ? `${fee.annual_rate}%` : '-'}\n                  </td>\n                </tr>\n              ))}\n            </tbody>\n          </table>\n        </div>\n      ) : (\n        <p>No funding fee history found.</p>\n      )}\n    </div>\n  );\n};",
          "notes": [
            "Returns `annual_rate` which can be used to project future costs/revenues.",
            "Distinguish between positive (received) and negative (paid) amounts in the UI.",
            "Critical for perps traders to understand the 'carry cost' of their positions."
          ],
          "related": ["useDistributionHistory", "usePosition", "useAccount"]
        },
        {
          "name": "useTransferHistory",
          "description": "Hook for useTransferHistory",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Retrieves a log of funds moving in and out of the account (deposits and withdrawals). This is essential for the 'Wallet' or 'Assets' section of an application.",
          "example": "import { useTransferHistory } from \"@orderly.network/hooks\";\n\nexport const TransferLog = () => {\n  // Fetches the history of deposits and withdrawals\n  const { data, isLoading } = useTransferHistory({\n    // Optional parameters might include pagination (page, size)\n    // or type filtering (deposit, withdrawal)\n  });\n\n  return (\n    <div className=\"max-w-md mx-auto mt-8\">\n      <h2 className=\"text-xl font-bold mb-4\">Transfers</h2>\n      <div className=\"space-y-3\">\n        {isLoading ? (\n          <p>Loading...</p>\n        ) : (\n          data?.map((transfer) => (\n            <div key={transfer.id} className=\"flex justify-between items-center p-3 border rounded\">\n              <div>\n                <p className=\"font-medium capitalize\">{transfer.type}</p>\n                <p className=\"text-xs text-gray-500\">{new Date(transfer.created_time).toLocaleString()}</p>\n              </div>\n              <div className=\"text-right\">\n                <p className={`font-bold ${transfer.status === 'completed' ? 'text-green-600' : 'text-yellow-600'}`}>\n                  {transfer.amount} {transfer.token}\n                </p>\n                <p className=\"text-xs text-gray-400 capitalize\">{transfer.status}</p>\n              </div>\n            </div>\n          ))\n        )}\n      </div>\n    </div>\n  );\n};",
          "notes": [
            "Returns an array of transfer objects containing status, amount, token, and timestamp.",
            "Use this to track the state of pending deposits or withdrawals.",
            "Combine with `useChainInfo` to generate transaction explorer links for on-chain transfers."
          ],
          "related": ["useChainInfo", "useBalanceSubscription", "useAccount"]
        },
        {
          "name": "useFundingRateHistory",
          "description": "Hook for useFundingRateHistory",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Retrieves a historical log of funding rates for a specific market. Useful for displaying charts or tables of past funding payments.",
          "example": "import { useFundingRateHistory } from '@orderly.network/hooks';\n\nexport const FundingHistoryChart = ({ symbol }: { symbol: string }) => {\n  // Fetches historical funding rates data\n  const { data, isLoading } = useFundingRateHistory(symbol);\n\n  if (isLoading) return <div>Loading history...</div>;\n\n  return (\n    <div className=\"w-full\">\n      <h3 className=\"font-bold mb-4\">Funding Rate History: {symbol}</h3>\n      \n      <div className=\"overflow-x-auto\">\n        <table className=\"min-w-full text-sm text-left\">\n          <thead className=\"bg-gray-100 text-gray-600\">\n            <tr>\n              <th className=\"p-2\">Timestamp</th>\n              <th className=\"p-2\">Rate</th>\n              <th className=\"p-2\">Price</th>\n            </tr>\n          </thead>\n          <tbody>\n            {data?.map((entry, index) => (\n              <tr key={index} className=\"border-b\">\n                <td className=\"p-2\">\n                  {new Date(entry.timestamp).toLocaleDateString()}\n                </td>\n                <td className={`p-2 ${entry.rate > 0 ? 'text-green-500' : 'text-red-500'}`}>\n                  {(entry.rate * 100).toFixed(4)}%\n                </td>\n                <td className=\"p-2\">${entry.price.toFixed(2)}</td>\n              </tr>\n            ))}\n          </tbody>\n        </table>\n      </div>\n    </div>\n  );\n};",
          "notes": [
            "Returns an array of data points containing timestamps and rates.",
            "Includes an `isLoading` state to handle async data fetching gracefully.",
            "Ideal for integration with charting libraries like Recharts or Chart.js."
          ],
          "related": ["useFundingRate", "useMarketData", "useTrades"]
        }
      ]
    },
    {
      "name": "Assets & Vault",
      "patterns": [
        {
          "name": "useVaultsHistory",
          "description": "Hook for useVaultsHistory",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Accesses historical data for liquidity vaults or strategy vaults. This shows user interactions with specific vault products over time.",
          "example": "import { useVaultsHistory } from \"@orderly.network/hooks\";\n\nexport const VaultPerformance = () => {\n  // Fetches history related to vault interactions (deposits/withdrawals/performance)\n  const { data } = useVaultsHistory();\n\n  return (\n    <div className=\"bg-white p-6 rounded-lg shadow\">\n      <h2 className=\"text-lg font-semibold mb-4\">Vault Activity</h2>\n      \n      {data && data.length > 0 ? (\n        <div className=\"space-y-4\">\n          {data.map((vaultRecord, idx) => (\n            <div key={idx} className=\"flex items-center justify-between border-b pb-2\">\n              <div>\n                <span className=\"font-medium\">{vaultRecord.vault_name}</span>\n                <span className=\"text-xs text-gray-500 ml-2\">\n                  {new Date(vaultRecord.timestamp).toDateString()}\n                </span>\n              </div>\n              <div className=\"text-right\">\n                <div className=\"font-bold\">{vaultRecord.amount} USD</div>\n                <div className=\"text-xs text-gray-500 capitalize\">{vaultRecord.action}</div>\n              </div>\n            </div>\n          ))}\n        </div>\n      ) : (\n        <p className=\"text-gray-500\">No vault history available.</p>\n      )}\n    </div>\n  );\n};",
          "notes": [
            "Useful for displaying a user's investment journey in specific Orderly vaults.",
            "Can include data on yield farming performance or liquidity provision history.",
            "Ensure the UI handles cases where the user has not interacted with any vaults."
          ],
          "related": ["useDistributionHistory", "useAssetsHistory", "useAccount"]
        },
        {
          "name": "useDeposit",
          "description": "Hook for useDeposit",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Retrieves the specific deposit address and metadata required to fund a trading account with a specific token on a specific chain.",
          "example": "import { useDeposit } from '@orderly.network/hooks';\n\nexport const DepositAddressDisplay = ({ symbol }: { symbol: string }) => {\n  // Fetches the deposit address configuration for a specific token\n  const { symbol: tokenSymbol, address, decimals, chainId, network } = useDeposit(symbol);\n\n  if (!address) return <div>Loading deposit info...</div>;\n\n  return (\n    <div className=\"bg-gray-50 p-4 rounded border\">\n      <h3 className=\"font-bold mb-2\">Deposit Funds</h3>\n      \n      <div className=\"space-y-2 text-sm\">\n        <div className=\"flex justify-between\">\n          <span className=\"text-gray-500\">Asset:</span>\n          <span className=\"font-medium\">{tokenSymbol}</span>\n </div>\n        <div className=\"flex justify-between\">\n          <span className=\"text-gray-500\">Network:</span>\n          <span className=\"font-medium\">{network}</span>\n        </div>\n        <div className=\"flex justify-between\">\n          <span className=\"text-gray-500\">Chain ID:</span>\n          <span className=\"font-medium\">{chainId}</span>\n        </div>\n        <div className=\"flex justify-between\">\n          <span className=\"text-gray-500\">Decimals:</span>\n          <span className=\"font-medium\">{decimals}</span>\n        </div>\n      </div>\n\n      <div className=\"mt-4\">\n        <p className=\"text-xs text-gray-400 mb-1\">Send only {tokenSymbol} to this address:</p>\n        <div className=\"bg-white border p-2 rounded font-mono text-xs break-all select-all\">\n          {address}\n        </div>\n      </div>\n    </div>\n  );\n};",
          "notes": [
            "Returns the smart contract or wallet address where funds should be sent.",
            "Includes `decimals` to ensure the UI displays amounts correctly.",
            "Crucial for preventing user error (sending tokens to the wrong chain)."
          ],
          "related": ["useWithdraw", "useBalance", "useAccount"]
        },
        {
          "name": "useMaxWithdrawal",
          "description": "Hook for useMaxWithdrawal",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Calculates and retrieves the maximum amount of a specific token that can be withdrawn from the account, taking into account account balance, margin requirements, and pending orders.",
          "example": "import { useMaxWithdrawal } from '@orderly.network/hooks';\n\nexport const WithdrawalForm = () => {\n  const token = 'USDC';\n  // Fetches the maximum amount the user can withdraw right now\n  const { data: maxWithdraw, isLoading } = useMaxWithdrawal(token);\n\n  return (\n    <div className=\"p-4 bg-white rounded shadow\">\n      <h3>Withdraw {token}</h3>\n      {isLoading ? (\n        <p>Checking limits...</p>\n      ) : (\n        <>\n          <p>Available to withdraw: <strong>{maxWithdraw} {token}</strong></p>\n          <input \n            type=\"number\" \n            max={maxWithdraw} \n            placeholder=\"Enter amount\" \n            className=\"border p-2 w-full\"\n          />\n        </>\n      )}\n    </div>\n  );\n};",
          "notes": [
            "Requires the user to be authenticated/wallet connected.",
            "The value changes dynamically based on open positions and order collateral.",
            "Useful for validating user input in withdrawal forms to prevent API errors."
          ],
          "related": ["useAccount", "useBalance", "usePositionStream"]
        },
        {
          "name": "useWithdraw",
          "description": "Hook for useWithdraw",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Manages the withdrawal of funds from the Orderly Network to an external blockchain address. It returns specific token and chain metadata required to format the transaction.",
          "example": "import { useWithdraw } from '@orderly.network/hooks';\nimport { useState } from 'react';\n\nexport const WithdrawForm = () => {\n  // Returns configuration and function to execute on-chain withdrawal\n  const { withdraw, isLoading, symbol, decimals, chainId, network } = useWithdraw();\n  const [amount, setAmount] = useState('');\n\n  const handleWithdraw = async () => {\n    try {\n      await withdraw({\n        amount: parseFloat(amount),\n        // destination address is usually handled by the hook context or passed here\n      });\n      alert('Withdrawal initiated!');\n    } catch (err) {\n      console.error(err);\n    }\n  };\n\n  return (\n    <div className=\"withdraw-card\">\n      <h3>Withdraw {symbol}</h3>\n      <p>Network: {network} (Chain ID: {chainId})</p>\n      <p>Decimals: {decimals}</p>\n      \n      <input \n        type=\"number\" \n        step={`1e-${decimals}`}\n        value={amount} \n        onChange={(e) => setAmount(e.target.value)} \n        placeholder={`Amount in ${symbol}`} \n      />\n      \n      <button onClick={handleWithdraw} disabled={isLoading}>\n        {isLoading ? 'Processing...' : 'Withdraw'}\n      </button>\n    </div>\n  );\n};",
          "notes": [
            "The hook returns `decimals` specifically so you can format the input field precision correctly for the selected token.",
            "Withdrawals involve on-chain transactions and may take time depending on the network congestion.",
            "Ensure the user has sufficient balance in their *Trading Account* (and has transferred to Wallet if necessary) before withdrawing."
          ],
          "related": ["useTransfer", "useTokenInfo", "useWalletAccount"]
        }
      ]
    },
    {
      "name": "Risk Management",
      "patterns": [
        {
          "name": "useLeverage",
          "description": "Hook for useLeverage",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Manages the leverage setting for a specific trading symbol. It provides the current leverage, maximum allowed leverage, predefined leverage steps (levers), and a function to update the leverage.",
          "example": "import { useLeverage } from \"@orderly.network/hooks\";\nimport { useState } from \"react\";\n\nexport const LeverageControl = ({ symbol }: { symbol: string }) => {\n  const [localLeverage, setLocalLeverage] = useState(1);\n  \n  // Fetches current leverage and provides function to update it\n  const { update, isLoading, curLeverage, maxLeverage, leverageLevers } = useLeverage(symbol);\n\n  const handleUpdate = async () => {\n    await update(localLeverage);\n  };\n\n  return (\n    <div className=\"bg-gray-100 p-4 rounded\">\n      <div className=\"flex justify-between mb-2\">\n        <span>Current Leverage: <b>{curLeverage}x</b></span>\n        <span>Max: {maxLeverage}x</span>\n      </div>\n      \n      <div className=\"flex gap-2 items-center\">\n        <input \n          type=\"range\" \n          min=\"1\" \n          max={maxLeverage} \n          step=\"1\" \n          value={localLeverage}\n          onChange={(e) => setLocalLeverage(Number(e.target.value))}\n          className=\"w-full\"\n        />\n        <span className=\"font-mono w-12 text-right\">{localLeverage}x</span>\n      </div>\n\n      <button \n        onClick={handleUpdate}\n        disabled={isLoading || localLeverage === curLeverage}\n        className=\"mt-3 w-full bg-indigo-600 text-white py-2 rounded disabled:bg-gray-400\"\n      >\n        {isLoading ? \"Updating...\" : \"Confirm Leverage\"}\n      </button>\n    </div>\n  );\n};",
          "notes": [
            "Changing leverage often requires it to apply to future positions only, or it might affect existing position margin depending on the broker's configuration.",
            "`leverageLevers` typically provides an array of safe leverage increments (e.g., [1, 2, 5, 10, 20]) to use in UI dropdowns.",
            "Always respect `maxLeverage` in your UI validation to prevent API errors."
          ],
          "related": ["usePosition", "useLeverageBySymbol", "useAccount"]
        }
      ]
    },
    {
      "name": "Referral & Rewards",
      "patterns": [
        {
          "name": "useReferralRebateSummary",
          "description": "Hook for useReferralRebateSummary",
          "installation": "npm install @orderly.network/hooks",
          "usage": "Provides a financial summary of the rewards earned through the referral program, distinguishing between claimed (total) and pending amounts.",
          "example": "import { useReferralRebateSummary } from '@orderly.network/hooks';\n\nexport const RebateDashboard = () => {\n  // Fetches a summary of rebates earned from the referral program.\n  const { data, isLoading } = useReferralRebateSummary();\n\n  if (isLoading) return <div>Loading rebate summary...</div>;\n\n  return (\n    <div className=\"grid grid-cols-2 gap-4\">\n      <div className=\"card p-4\">\n        <h4>Total Rebates</h4>\n        <p className=\"text-xl font-bold\">\n          {data?.total_rebate || '0.00'} USDC\n        </p>\n      </div>\n      <div className=\"card p-4\">\n        <h4>Pending Rebates</h4>\n        <p className=\"text-xl font-bold\">\n          {data?.pending_rebate || '0.00'} USDC\n        </p>\n      </div>\n    </div>\n  );\n};",
          "notes": [
            "Crucial for displaying the user's earnings from their referral activity.",
            "Data is typically aggregated over time (e.g., lifetime earnings).",
            "Ensure you handle cases where rebate data might be undefined or zero."
          ],
          "related": ["useRefereeInfo", "useCommission"]
        }
      ]
    },
    {
      "name": "Data Fetching",
      "patterns": [
        {
          "name": "useMediaQuery",
          "description": "Hook for useMediaQuery",
          "installation": "npm install @orderly.network/hooks",
          "usage": "A responsive design hook that returns a boolean indicating whether the current viewport matches the provided CSS media query string.",
          "example": "import { useMediaQuery } from '@orderly.network/hooks';\n\nexport const ResponsiveLayout = () => {\n  // A utility hook to check if the viewport matches a specific breakpoint\n  const isMobile = useMediaQuery('(max-width: 768px)');\n  const isDesktop = useMediaQuery('(min-width: 1024px)');\n\n  return (\n    <div className=\"layout\">\n      <header>\n        {isMobile ? 'Mobile Header' : 'Desktop Header'}\n      </header>\n      \n      <main>\n        {isDesktop && <aside className=\"sidebar\">Sidebar</aside>}\n        <div className=\"content\">Main Content</div>\n      </main>\n    </div>\n  );\n};",
          "notes": [
            "Useful for toggling UI components like sidebars, tabs, or changing table layouts.",
            "Relies on window.matchMedia and updates automatically on resize.",
            "Prefer this over CSS-only solutions when you need to conditionally render entire React components."
          ],
          "related": ["useConfig", "useAccount"]
        }
      ]
    },
    {
      "name": "UI Components",
      "patterns": [
        {
          "name": "CheckboxIcon",
          "description": "A visual icon component representing a checked state. It is typically used inside custom checkbox containers or buttons to indicate selection.",
          "installation": "npm install @orderly.network/ui",
          "usage": "Use this icon when building custom form controls or toggle buttons where you need to visually represent a 'checked' boolean state. It is often paired with a container div that handles the background color.",
          "example": "import React from \"react\";\nimport { CheckboxIcon } from \"@orderly.network/ui\";\nimport { cnBase } from \"@orderly.network/ui\";\n\nexport const CheckboxIconExample = () => {\n  return (\n    <div className=\"flex gap-4 items-center\">\n      {/* Primary Color Checkbox */}\n      <div className=\"flex items-center gap-2\">\n        <div className=\"w-5 h-5 rounded bg-primary flex items-center justify-center\">\n          <CheckboxIcon className=\"oui-text-white\" />\n        </div>\n        <span>Checked (Primary)</span>\n      </div>\n\n      {/* White/Custom Color Checkbox */}\n      <div className=\"flex items-center gap-2\">\n        <div className=\"w-5 h-5 rounded border border-gray-500 flex items-center justify-center\">\n          <CheckboxIcon className=\"oui-text-white\" />\n        </div>\n        <span>Checked (White)</span>\n      </div>\n    </div>\n  );\n};",
          "notes": [
            "The icon itself does not handle state (checked/unchecked); it is purely visual.",
            "Commonly styled with utility classes like `oui-text-primary` or `oui-text-white` depending on the background contrast."
          ],
          "related": ["Checkbox", "Radio", "Icon"]
        },
        {
          "name": "EmptyStateIcon",
          "description": "A graphical placeholder icon used to indicate that a list, table, or section has no data to display.",
          "installation": "npm install @orderly.network/ui",
          "usage": "Render this component inside tables, lists, or card containers when the data array is empty or when a search yields no results.",
          "example": "import React from \"react\";\nimport { EmptyStateIcon } from \"@orderly.network/ui\";\n\nexport const EmptyStateExample = () => {\n  return (\n    <div className=\"flex flex-col items-center justify-center p-8 text-gray-400\">\n      <div className=\"mb-4 opacity-50\">\n        <EmptyStateIcon />\n      </div>\n      <p className=\"text-sm\">No data found</p>\n    </div>\n  );\n};",
          "notes": [
            "This is a static SVG component.",
            "Often wrapped in a container with reduced opacity to soften the visual impact."
          ],
          "related": ["Table", "Card", "StatusIndicator"]
        },
        {
          "name": "InputPrefix",
          "description": "A wrapper component used to display content (like currency symbols or icons) immediately before the input text cursor.",
          "installation": "npm install @orderly.network/ui",
          "usage": "Use this to prepend fixed text or icons to an input field, such as a currency symbol ($), a network identifier, or a search icon.",
          "example": "import React from \"react\";\nimport { InputPrefix } from \"@orderly.network/ui\";\n\nexport const InputPrefixExample = () => {\n  return (\n    <div className=\"flex items-center border border-gray-700 rounded px-3 bg-gray-800\">\n      <InputPrefix \n        id=\"amount-input\" \n        prefix={<span className=\"text-gray-400 font-bold\">$</span>} \n      />\n      <input \n        id=\"amount-input\"\n        type=\"number\" \n        className=\"bg-transparent border-none outline-none text-white py-2 w-full\"\n        placeholder=\"0.00\"\n      />\n    </div>\n  );\n};",
          "notes": [
            "The `prefix` prop accepts a React Node (string, number, JSX element).",
            "Ensure the `id` passed to InputPrefix matches the `id` of the input element for accessibility."
          ],
          "related": ["InputSuffix", "TextField", "Input"]
        },
        {
          "name": "InputSuffix",
          "description": "A wrapper component used to display content (like units, currency codes, or action buttons) immediately after the input text.",
          "installation": "npm install @orderly.network/ui",
          "usage": "Use this to append fixed text or icons to an input field, such as a currency ticker (USDT), a percentage sign (%), or a clear button.",
          "example": "import React from \"react\";\nimport { InputSuffix } from \"@orderly.network/ui\";\n\nexport const InputSuffixExample = () => {\n  return (\n    <div className=\"flex items-center border border-gray-700 rounded px-3 bg-gray-800\">\n      <input \n        id=\"leverage-input\"\n        type=\"number\" \n        className=\"bg-transparent border-none outline-none text-white py-2 w-full\"\n        placeholder=\"1x\"\n      />\n      <InputSuffix \n        id=\"leverage-input\"\n        suffix={<span className=\"text-gray-400 text-sm\">USD</span>} \n      />\n    </div>\n  );\n};",
          "notes": [
            "The `suffix` prop accepts a React Node.",
            "Useful for maintaining alignment of input text while keeping labels fixed to the right."
          ],
          "related": ["InputPrefix", "TextField", "Input"]
        },
        {
          "name": "TextField",
          "description": "A standard text input wrapper that includes label, placeholder, and error handling styling. It supports controlled component patterns.",
          "installation": "npm install @orderly.network/ui",
          "usage": "Use for form inputs requiring user text entry, such as referral codes, account names, or search queries.",
          "example": "import React, { useState } from \"react\";\nimport { TextField } from \"@orderly.network/ui\";\n\nexport const TextFieldExample = () => {\n  const [referralCode, setReferralCode] = useState(\"\");\n\n  return (\n    <div className=\"w-64 space-y-4\">\n      {/* Standard Full Width Input */}\n      <TextField\n        label=\"Referral Code\"\n        placeholder=\"Enter code...\"\n        value={referralCode}\n        onChange={(e) => setReferralCode(e.target.value)}\n        fullWidth\n      />\n\n      {/* Input with dynamic placeholder */}\n      <TextField\n        label=\"Sub-account Name\"\n        placeholder=\"Account 1\"\n        fullWidth\n      />\n    </div>\n  );\n};",
          "notes": [
            "The `fullWidth` prop makes the input take 100% width of its parent container.",
            "It functions as a controlled input; ensure you manage `value` and `onChange` state."
          ],
          "related": ["InputPrefix", "InputSuffix", "Input"]
        },
        {
          "name": "ExtensionProvider",
          "description": "A Context Provider that enables the plugin/extension system within the application tree. It makes registered extensions available to `ExtensionSlot` components.",
          "installation": "npm install @orderly.network/ui",
          "usage": "Wrap your application root or a specific layout section with this provider to allow dynamic injection of UI components via the extension system.",
          "example": "import React from \"react\";\nimport { ExtensionProvider } from \"@orderly.network/ui\";\n\n// Define a mock extension\nconst myCustomExtension = {\n  name: \"custom-widget\",\n  component: () => <div>Custom Widget Content</div>\n};\n\nexport const ProviderExample = ({ children }) => {\n  return (\n    <ExtensionProvider extensions={[myCustomExtension]}>\n      {children}\n    </ExtensionProvider>\n  );\n};",
          "notes": [
            "Must be placed higher in the component tree than any `ExtensionSlot` that intends to use its extensions.",
            "Accepts an array of extension objects containing configuration and render logic."
          ],
          "related": ["ExtensionSlot", "PluginContext", "useExtensions"]
        },
        {
          "name": "ExtensionSlot",
          "description": "A placeholder component that renders specific UI extensions based on the `position` prop. It acts as a mount point for dynamic content.",
          "installation": "npm install @orderly.network/ui",
          "usage": "Place this component in layouts where you want third-party plugins or internal modules to inject UI elements (e.g., adding a button to the mobile menu or a custom empty state).",
          "example": "import React from \"react\";\nimport { ExtensionSlot, ExtensionPositionEnum } from \"@orderly.network/ui\";\n\nexport const SlotExample = () => {\n  return (\n    <div className=\"p-4 border border-gray-700 rounded\">\n      <h3 className=\"mb-2\">Account Menu Area</h3>\n      \n      {/* This slot will render any extension registered for MobileAccountMenu */}\n      <div className=\"bg-gray-800 p-2 rounded\">\n        <ExtensionSlot position={ExtensionPositionEnum.MobileAccountMenu} />\n      </div>\n\n      <h3 className=\"mt-4 mb-2\">Empty Data Area</h3>\n      <div className=\"min-h-[100px] flex items-center justify-center bg-gray-800 p-2 rounded\">\n        <ExtensionSlot position={ExtensionPositionEnum.EmptyDataIdentifier} />\n      </div>\n    </div>\n  );\n};",
          "notes": [
            "Requires an `ExtensionProvider` to be ancestor in the tree.",
            "Uses `ExtensionPositionEnum` to define standard locations (like `MobileAccountMenu` or `EmptyDataIdentifier`)."
          ],
          "related": ["ExtensionProvider", "ExtensionPositionEnum", "PluginContext"]
        },
        {
          "name": "CombineSelect",
          "description": "A compact select dropdown component that combines a trigger button with a selection menu. It supports various sizes and option configurations.",
          "installation": "npm install @orderly.network/ui",
          "usage": "Ideal for selecting order types (Limit/Market), timeframes, or other discrete settings where space is limited (e.g., trading headers or toolbars).",
          "example": "import React, { useState } from \"react\";\nimport { CombineSelect } from \"@orderly.network/ui\";\n\nexport const CombineSelectExample = () => {\n  const [value, setValue] = useState(\"option-1\");\n\n  const options = [\n    { label: \"Limit Order\", value: \"limit\" },\n    { label: \"Market Order\", value: \"market\" },\n    { label: \"Stop Limit\", value: \"stop-limit\" },\n  ];\n\n  return (\n    <div className=\"w-48\">\n      <CombineSelect\n        size=\"xs\"\n        options={options}\n        value={value}\n        onValueChange={(val) => setValue(val)}\n        placeholder=\"Select type\"\n      />\n    </div>\n  );\n};",
          "notes": [
            "The `size` prop accepts values like 'xs' for compact layouts.",
            "Uses `onValueChange` for handling updates rather than standard DOM events.",
            "Accepts `options` prop which is an array of objects with label and value."
          ],
          "related": ["Select", "Popover", "TextField"]
        },
        {
          "name": "TokenSelect",
          "description": "A dropdown component specifically designed for selecting cryptocurrency tokens. It typically handles token display, including icons and symbols, and manages the selection state.",
          "installation": "npm install @orderly.network/ui",
          "usage": "Use in trading forms or asset transfer modals where the user needs to specify which token they are interacting with.",
          "example": "import { TokenSelect } from \"@orderly.network/ui\";\n\nconst MyTokenPicker = () => {\n  const tokens = [\n    { symbol: \"ETH\", name: \"Ethereum\", logo: \"/eth.png\" },\n    { symbol: \"USDC\", name: \"USD Coin\", logo: \"/usdc.png\" },\n    { symbol: \"PERP\", name: \"Perpetual Protocol\", logo: \"/perp.png\" },\n  ];\n\n  return (\n    <div className=\"w-64\">\n      <TokenSelect\n        tokens={tokens}\n        value=\"ETH\"\n        onSelect={(token) => console.log(\"Selected:\", token)}\n        disabled={false}\n      />\n    </div>\n  );\n};",
          "notes": [
            "Ensure the tokens array includes 'symbol' and 'logo' properties for proper rendering.",
            "The 'onSelect' callback returns the full token object or symbol depending on implementation."
          ],
          "related": ["Input", "Button", "TokenBalance"]
        },
        {
          "name": "SliderTip",
          "description": "A visual indicator (tooltip) that displays the current value of a slider interaction. It is often used to overlay the value on top of a slider track or thumb.",
          "installation": "npm install @orderly.network/ui",
          "usage": "Integrate with range sliders to provide real-time feedback on the percentage or absolute value selected by the user.",
          "example": "import { SliderTip } from \"@orderly.network/ui\";\n\nconst MySliderTip = () => {\n  // Simulating a slider value\n  const currentValue = 75;\n  const maxValue = 100;\n\n  return (\n    <div className=\"relative w-full h-12 flex items-center justify-center\">\n      <SliderTip\n        value={currentValue}\n        max={maxValue}\n        className=\"text-green-500 font-bold\"\n      />\n    </div>\n  );\n};",
          "notes": [
            "Requires a 'value' prop to determine what text to display.",
            "The 'max' prop helps calculate percentage positioning if the component handles internal positioning."
          ],
          "related": ["Slider", "Input", "Progress"]
        },
        {
          "name": "DataFilter",
          "description": "A component used to filter datasets, often appearing as a segmented control or a tab group above tables or lists.",
          "installation": "npm install @orderly.network/ui",
          "usage": "Place above a `DataTable` or list view to allow users to toggle between different data states (e.g., Open Orders vs. History).",
          "example": "import { DataFilter } from \"@orderly.network/ui\";\n\nconst MyDataFilter = () => {\n  const filterItems = [\n    { label: \"Open\", value: \"open\" },\n    { label: \"Filled\", value: \"filled\" },\n    { label: \"Cancelled\", value: \"cancelled\" },\n  ];\n\n  const handleFilterChange = (value: string) => {\n    console.log(\"Filter selected:\", value);\n  };\n\n  return (\n    <DataFilter\n      items={filterItems}\n      onFilter={handleFilterChange}\n      defaultValue=\"open\"\n      className=\"oui-border-none oui-py-0\"\n    />\n  );\n};",
          "notes": [
            "The 'items' prop expects an array of objects with 'label' and 'value'.",
            "It supports custom className for styling overrides, specifically useful for removing borders or padding."
          ],
          "related": ["DataTable", "Tabs", "StatusBadge"]
        },
        {
          "name": "DataTable",
          "description": "A comprehensive table component for displaying structured data. It handles loading states, empty states, and custom styling via class names.",
          "installation": "npm install @orderly.network/ui",
          "usage": "The primary component for displaying order history, trade history, or position data in a trading interface.",
          "example": "import { DataTable } from \"@orderly.network/ui\";\n\nconst MyTable = () => {\n  const columns = [\n    { key: \"market\", title: \"Market\" },\n    { key: \"size\", title: \"Size\" },\n    { key: \"price\", title: \"Price\" },\n  ];\n\n  const data = [\n    { id: 1, market: \"PERP-ETH\", size: \"1.5\", price: \"2000\" },\n    { id: 2, market: \"PERP-BTC\", size: \"0.5\", price: \"35000\" },\n  ];\n\n  return (\n    <DataTable\n      dataSource={data}\n      columns={columns}\n      loading={false}\n      classNames={{\n        root: \"oui-bg-base-7 oui-rounded-lg\",\n        header: \"oui-text-base-contrast\",\n      }}\n      emptyView={<div className=\"p-4 text-center\">No data found</div>}\n    />\n  );\n};",
          "notes": [
            "Use 'dataSource' to pass the array of data objects.",
            "'classNames' prop allows deep styling customization for root, header, body, etc.",
            "Provide an 'emptyView' component to handle cases with no data."
          ],
          "related": ["DataFilter", "TableBody", "TableCell"]
        },
        {
          "name": "TableBody",
          "description": "The container component for the rows of a table. It renders the `<tbody>` and handles the iteration over row data.",
          "installation": "npm install @orderly.network/ui",
          "usage": "Used when constructing a custom table layout or when extending the default `DataTable` with specific row rendering logic.",
          "example": "import { TableBody } from \"@orderly.network/ui\";\n\nconst MyCustomTableBody = () => {\n  const rows = [\n    { id: 1, cells: [\"PERP-ETH\", \"1.5\", \"2000\"] },\n    { id: 2, cells: [\"PERP-BTC\", \"0.5\", \"35000\"] },\n  ];\n\n  return (\n    <table className=\"w-full\">\n      <TableBody\n        rows={rows}\n        bordered={true}\n        className=\"oui-text-sm\"\n      />\n    </table>\n  );\n};",
          "notes": [
            "The 'rows' prop typically contains the data structure to be iterated over.",
            "The 'bordered' prop toggles borders between rows/cells."
          ],
          "related": ["DataTable", "TableCell", "TableHeader"]
        },
        {
          "name": "TableCell",
          "description": "A component responsible for rendering a single cell within a table row. It handles alignment and basic formatting based on the cell data.",
          "installation": "npm install @orderly.network/ui",
          "usage": "Used inside `TableBody` or `DataTable` to standardize how individual data points are displayed.",
          "example": "import { TableCell } from \"@orderly.network/ui\";\n\nconst MyCustomCell = () => {\n  // Represents a single cell's data structure\n  const cellData = {\n    value: \"2000.00\",\n    align: \"right\",\n    type: \"number\"\n  };\n\n  return (\n    <TableCell\n      cell={cellData}\n      className=\"oui-text-right oui-font-mono\"\n    />\n  );\n};",
          "notes": [
            "The 'cell' prop usually contains the raw value and metadata (like alignment).",
            "Supports className overrides for specific cell styling."
          ],
          "related": ["TableBody", "DataTable", "NumType"]
        },
        {
          "name": "NumTypeRoi",
          "description": "A typography component specifically for formatting Return on Investment (ROI) figures. It automatically applies color coding (green for positive, red for negative).",
          "installation": "npm install @orderly.network/ui",
          "usage": "Ideal for portfolio summaries, strategy performance cards, or history tables displaying percentage gains/losses.",
          "example": "import { NumTypeRoi } from \"@orderly.network/ui\";\n\nconst RoiDisplay = () => {\n  return (\n    <div className=\"flex gap-4\">\n      <div>\n        <p>Positive ROI</p>\n        <NumTypeRoi value={12.5} suffix=\"%\" />\n      </div>\n      <div>\n        <p>Negative ROI</p>\n        <NumTypeRoi value={-5.2} suffix=\"%\" />\n      </div>\n    </div>\n  );\n};",
          "notes": [
            "Automatically handles color based on the sign of the value.",
            "Accepts a 'suffix' prop (e.g., '%') to append units."
          ],
          "related": ["NumTypePnl", "Percent", "StatusBadge"]
        },
        {
          "name": "NumTypePnl",
          "description": "A typography component for formatting Profit and Loss (PnL) values. It ensures consistent decimal formatting and color indication for financial data.",
          "installation": "npm install @orderly.network/ui",
          "usage": "Used in position rows, account balances, and trade history to clearly show profit or loss amounts.",
          "example": "import { NumTypePnl } from \"@orderly.network/ui\";\n\nconst PnlDisplay = () => {\n  return (\n    <div className=\"flex gap-4\">\n      <div>\n        <p>Realized PnL</p>\n        <NumTypePnl value={150.25} decimals={2} />\n      </div>\n      <div>\n        <p>Unrealized Loss</p>\n        <NumTypePnl value={-42.10} decimals={2} />\n      </div>\n    </div>\n  );\n};",
          "notes": [
            "Use 'decimals' to control precision.",
            "Colors typically default to green for positive and red for negative values."
          ],
          "related": ["NumTypeRoi", "TokenBalance", "Decimal"]
        },
        {
          "name": "NumTypeNotional",
          "description": "A typography component specifically designed to display the notional value of trading positions. It handles decimal formatting appropriate for contract sizes and total position values.",
          "installation": "npm install @orderly.network/ui",
          "usage": "Use in position tables, portfolio summaries, or trade confirmations where the total dollar value of a position needs to be displayed clearly.",
          "example": "import { NumTypeNotional } from \"@orderly.network/ui\";\n\nexport const PositionNotional = () => {\n  return (\n    <div className=\"flex flex-col gap-2\">\n      {/* Standard usage */}\n      <NumTypeNotional value={12500.54321} />\n      \n      {/* With custom decimals and symbol */}\n      <NumTypeNotional \n        value={12500.54321} \n        decimals={2} \n        showSymbol={true} \n      />\n    </div>\n  );\n};",
          "notes": [
            "Automatically formats large numbers with commas or abbreviations depending on the internal configuration.",
            "Typically inherits color themes from the parent theme (e.g., red/green for PnL contexts if applicable)."
          ],
          "related": ["NumTypeAssetValue", "NumTypeCollateral", "Decimal"]
        },
        {
          "name": "NumTypeAssetValue",
          "description": "A typography component for displaying the value of a specific asset. It ensures consistent decimal precision and symbol placement across the application.",
          "installation": "npm install @orderly.network/ui",
          "usage": "Ideal for displaying wallet balances, asset allocation breakdowns, or individual coin holdings.",
          "example": "import { NumTypeAssetValue } from \"@orderly.network/ui\";\n\nexport const WalletBalance = () => {\n  return (\n    <div className=\"p-4 border rounded\">\n      <h3>Wallet Balance</h3>\n      <div className=\"text-2xl font-bold\">\n        <NumTypeAssetValue \n          value={4500.12345678} \n          symbol=\"USDT\" \n        />\n      </div>\n      <div className=\"text-sm text-gray-500\">\n        <NumTypeAssetValue \n          value={0.45001234} \n          symbol=\"ETH\" \n          decimals={4}\n        />\n      </div>\n    </div>\n  );\n};",
          "notes": [
            "Use the `symbol` prop to append currency tickers (e.g., USD, ETH).",
            "Handles precision scaling automatically based on the asset type if not explicitly overridden."
          ],
          "related": ["NumTypeNotional", "NumTypeCollateral", "TokenIcon"]
        },
        {
          "name": "NumTypeCollateral",
          "description": "A specialized typography component for displaying collateral values. It formats numbers specifically for margin requirements and account health metrics.",
          "installation": "npm install @orderly.network/ui",
          "usage": "Use in the account overview bar, margin calculator, or liquidation risk panels.",
          "example": "import { NumTypeCollateral } from \"@orderly.network/ui\";\n\nexport const MarginInfo = () => {\n  return (\n    <div className=\"flex justify-between p-3 bg-gray-50 rounded\">\n      <span>Total Collateral</span>\n      <span className=\"font-mono\">\n        <NumTypeCollateral value={10234.56789} />\n      </span>\n    </div>\n  );\n};",
          "notes": [
            "Ensures high precision is maintained where required for margin calculations.",
            "Often used in conjunction with health bars or risk meters."
          ],
          "related": ["NumTypeAssetValue", "AccountHealthBar", "MarginRatio"]
        },
        {
          "name": "AuthGuard",
          "description": "A conditional wrapper that checks authentication status. If the user is not connected or does not have an Orderly account, it renders a call-to-action button to trigger the connection flow. Otherwise, it renders the children components.",
          "installation": "npm install @orderly.network/ui-connector",
          "usage": "Wrap critical trading actions (Place Order, Withdraw, Create API Key) to ensure only authenticated users can interact with them.",
          "example": "import { AuthGuard } from \"@orderly.network/ui-connector\";\n\nexport const TradeAction = () => {\n  return (\n    <div className=\"p-6\">\n      <h2>Place Order</h2>\n      \n      {/* If user is not authenticated, this renders a Connect/Create Account button.\n           If authenticated, it renders the children (the Place Order button). */}\n      <AuthGuard \n        networkId={orderlyNetworkId}\n        buttonProps={{\n          size: \"md\",\n          fullWidth: true,\n          className: \"mt-4\"\n        }}\n      >\n        <button className=\"w-full bg-blue-600 text-white py-2 rounded hover:bg-blue-700\">\n          Place Long Order\n        </button>\n      </AuthGuard>\n    </div>\n  );\n};",
          "notes": [
            "Accepts `buttonProps` to customize the appearance of the 'Connect' button that appears when unauthenticated.",
            "Requires `OrderlyContext` to be available in the component tree."
          ],
          "related": ["AuthGuardEmpty", "WalletConnectorWidget", "useAccount"]
        },
        {
          "name": "AuthGuardEmpty",
          "description": "A silent authentication guard. It renders its children only if the user is authenticated. If the user is not authenticated, it renders nothing (null), effectively hiding the protected UI elements without prompting.",
          "installation": "npm install @orderly.network/ui-connector",
          "usage": "Use for dashboard sections, private navigation links, or data tables that should simply disappear for guest users rather than showing a login prompt.",
          "example": "import { AuthGuardEmpty } from \"@orderly.network/ui-connector\";\n\nexport const PrivateDataView = () => {\n  return (\n    <AuthGuardEmpty>\n      {/* This content is only visible to authenticated users.\n           If unauthenticated, nothing is rendered (Empty state). */}\n      <div className=\"grid grid-cols-2 gap-4\">\n        <div className=\"card\">Portfolio Analytics</div>\n        <div className=\"card\">Order History</div>\n      </div>\n    </AuthGuardEmpty>\n  );\n};",
          "notes": [
            "Does not render any UI feedback for unauthenticated users.",
            "Useful for 'clean' UI layouts where you don't want empty placeholders or login buttons cluttering the view."
          ],
          "related": ["AuthGuard", "useAccount", "WalletConnectorWidget"]
        },
        {
          "name": "WalletConnectorWidget",
          "description": "A standalone, all-in-one component that handles the wallet connection UI. It displays a 'Connect Wallet' button when disconnected, and manages the account dropdown/modal when connected.",
          "installation": "npm install @orderly.network/ui-connector",
          "usage": "Place in the application header (navbar) as the primary entry point for wallet connection and account management.",
          "example": "import { WalletConnectorWidget } from \"@orderly.network/ui-connector\";\n\nexport const Header = () => {\n  return (\n    <header className=\"flex justify-between items-center p-4 border-b\">\n      <div className=\"logo\">My DEX</div>\n      \n      {/* Renders a button that opens the connection modal.\n           Once connected, it typically transforms into a wallet address display or dropdown. */}\n      <WalletConnectorWidget />\n    </header>\n  );\n};",
          "notes": [
            "Handles the connection flow logic internally via the Orderly Connector context.",
            "Usually supports switching networks and disconnecting within its dropdown menu."
          ],
          "related": ["AuthGuard", "useWallet", "AccountInfo"]
        },
        {
          "name": "LeverageInput",
          "description": "An input component specifically designed for entering leverage values. It handles validation (min/max limits) and ensures the input is treated as a numerical multiplier.",
          "installation": "npm install @orderly.network/ui-leverage",
          "usage": "Use in trading settings panels or order forms where users need to manually type or fine-tune their leverage multiplier.",
          "example": "import { LeverageInput } from \"@orderly.network/ui-leverage\";\nimport { useState } from \"react\";\n\nexport const LeverageSettings = () => {\n  const [leverage, setLeverage] = useState(5);\n\n  return (\n    <div className=\"p-4 border rounded\">\n      <label className=\"block text-sm font-medium mb-2\">Adjust Leverage</label>\n      \n      <LeverageInput \n        value={leverage}\n        onChange={(val) => setLeverage(val)}\n        max={20}\n        step={0.1}\n      />\n      \n      <p className=\"text-xs text-gray-500 mt-2\">\n        Current: {leverage}x\n      </p>\n    </div>\n  );\n};",
          "notes": [
            "Often includes visual indicators for max leverage limits.",
            "Prevents invalid inputs (like negative numbers or text)."
          ],
          "related": ["LeverageSelector", "useLeverage", "OrderEntry"]
        },
        {
          "name": "LeverageSelector",
          "description": "A UI component for selecting leverage, typically featuring a slider or preset buttons. It offers a more visual and constrained way to adjust leverage compared to a text input.",
          "installation": "npm install @orderly.network/ui-leverage",
          "usage": "Best for user-friendly trading interfaces where users want to quickly slide or click to set common leverage levels (1x, 10x, 20x, etc.).",
          "example": "import { LeverageSelector } from \"@orderly.network/ui-leverage\";\nimport { useState } from \"react\";\n\nexport const QuickLeverage = () => {\n  const [leverage, setLeverage] = useState(10);\n\n  return (\n    <div className=\"flex flex-col gap-2\">\n      <span className=\"text-sm\">Select Leverage</span>\n      \n      {/* Provides a quick selection interface (slider or presets) \n           rather than a raw text input. */}\n      <LeverageSelector \n        value={leverage}\n        onChange={setLeverage}\n        max={50}\n        marks={[1, 5, 10, 20, 50]}\n      />\n    </div>\n  );\n};",
          "notes": [
            "The `marks` prop allows you to define specific leverage steps available to the user.",
            "Provides immediate visual feedback on the risk level associated with the leverage."
          ],
          "related": ["LeverageInput", "Slider", "useLeverage"]
        },
        {
          "name": "LTVRiskTooltipUI",
          "description": "A tooltip component designed to display Loan-to-Value (LTV) risk information. It helps users understand their margin usage and liquidation risk at a glance.",
          "installation": "npm install @orderly.network/ui-order-entry",
          "usage": "Use this in account summary sections or order entry forms where users need to be aware of their leverage risk relative to their collateral.",
          "example": "import React from \"react\";\nimport { LTVRiskTooltipUI } from \"@orderly.network/ui-order-entry\";\n\nexport const LTVExample = () => {\n  // Represents the current Loan-to-Value ratio (e.g., 80%)\n  const currentLTV = 0.8;\n\n  return (\n    <div style={{ padding: \"20px\" }}>\n      <h3>Margin Health</h3>\n      <LTVRiskTooltipUI \n        ltv={currentLTV} \n        // Optional: Customize trigger element if needed, though usually defaults to an icon\n      />\n    </div>\n  );\n};",
          "notes": [
            "Typically appears as an icon (info/warning) that expands on hover.",
            "Ensure the `ltv` prop is a decimal (e.g., 0.5 for 50%)."
          ],
          "related": ["AssetInfo", "AccountBalance", "OrderEntry"]
        },
        {
          "name": "AdditionalConfigButton",
          "description": "A toggle button component used to reveal or hide advanced configuration options within the order entry interface, such as Take Profit/Stop Loss settings or Post-Only modes.",
          "installation": "npm install @orderly.network/ui-order-entry",
          "usage": "Place this below the main price/quantity inputs to keep the UI clean while allowing power users to access advanced order types.",
          "example": "import React, { useState } from \"react\";\nimport { AdditionalConfigButton } from \"@orderly.network/ui-order-entry\";\n\nexport const ConfigButtonExample = () => {\n  const [isExpanded, setIsExpanded] = useState(false);\n\n  return (\n    <div style={{ display: \"flex\", flexDirection: \"column\", gap: \"10px\" }}>\n      <AdditionalConfigButton \n        active={isExpanded}\n        onClick={() => setIsExpanded(!isExpanded)}\n      >\n        Advanced Settings\n      </AdditionalConfigButton>\n      \n      {isExpanded && (\n        <div style={{ padding: \"10px\", border: \"1px solid #333\" }}>\n          {/* Advanced options like TP/SL or Post-Only would go here */}\n          <p>TP/SL Configuration Panel</p>\n        </div>\n      )}\n    </div>\n  );\n};",
          "notes": [
            "The `active` prop usually changes the visual style (e.g., background color) to indicate the panel is open.",
            "It accepts children to customize the button label."
          ],
          "related": ["AdvancedTPSLResult", "OrderEntry", "TPSLInput"]
        },
        {
          "name": "AdvancedTPSLResult",
          "description": "Displays the calculated result of an order that includes Advanced Take Profit (TP) and Stop Loss (SL) parameters. It validates and shows the trigger prices and expected outcomes.",
          "installation": "npm install @orderly.network/ui-order-entry",
          "usage": "Render this inside the order confirmation modal or the advanced settings panel to show the user exactly what their TP/SL settings will execute at.",
          "example": "import React from \"react\";\nimport { AdvancedTPSLResult } from \"@orderly.network/ui-order-entry\";\n\nexport const TPSLResultExample = () => {\n  // Mock data representing a calculated order with TP/SL\n  const formattedOrder = {\n    trigger_price: \"2500.50\",\n    tp_trigger_price: \"2600.00\",\n    sl_trigger_price: \"2400.00\",\n    quantity: \"0.1\",\n    side: \"BUY\"\n  };\n\n  const symbolInfo = {\n    symbol: \"PERP_ETH_USDC\",\n    quote_precision: 2,\n    base_precision: 3\n  };\n\n  const errors = {}; // Empty object implies no validation errors\n\n  return (\n    <div style={{ border: \"1px solid #444\", padding: \"16px\", borderRadius: \"8px\" }}>\n      <h4>Order Preview</h4>\n      <AdvancedTPSLResult \n        order={formattedOrder}\n        symbolInfo={symbolInfo}\n        errors={errors}\n      />\n    </div>\n  );\n};",
          "notes": [
            "Requires `symbolInfo` to format prices correctly according to the market's precision.",
            "Passing an `errors` object with keys will display validation messages inline."
          ],
          "related": ["AdditionalConfigButton", "OrderSummary", "TPSLInput"]
        },
        {
          "name": "AssetInfo",
          "description": "Displays critical information about the trading asset, including the quote currency, estimated liquidation price, and current mark price. It also indicates if the asset is tradeable.",
          "installation": "npm install @orderly.network/ui-order-entry",
          "usage": "Ideal for the header of the order entry box or the account overview section to give users context on their position's health.",
          "example": "import React from \"react\";\nimport { AssetInfo } from \"@orderly.network/ui-order-entry\";\n\nexport const AssetInfoExample = () => {\n  return (\n    <div style={{ backgroundColor: \"#1a1a1a\", padding: \"15px\" }}>\n      <AssetInfo \n        canTrade={true}\n        quote=\"USDC\"\n        estLiqPrice=\"1850.25\"\n        markPrice=\"1950.00\"\n      />\n    </div>\n  );\n};",
          "notes": [
            "If `canTrade` is false, the component might visually disable itself or show a warning.",
            "Liquidation price is a key metric for risk management."
          ],
          "related": ["LTVRiskTooltipUI", "PositionTable", "AccountBalance"]
        },
        {
          "name": "EffectiveFeeUI",
          "description": "A presentational component that renders the effective fee rate (after volume discounts or token rebates) for either Maker or Taker orders.",
          "installation": "npm install @orderly.network/ui-order-entry",
          "usage": "Use this in fee breakdown sections or tooltips where you need to show the specific rate the user is currently paying.",
          "example": "import React from \"react\";\nimport { EffectiveFeeUI } from \"@orderly.network/ui-order-entry\";\n\nexport const EffectiveFeeUIExample = () => {\n  return (\n    <div style={{ display: \"flex\", gap: \"20px\" }}>\n      <EffectiveFeeUI \n        type=\"maker\"\n        value={0.0002} // 0.02%\n        currency=\"USDC\"\n      />\n      <EffectiveFeeUI \n        type=\"taker\"\n        value={0.0005} // 0.05%\n        currency=\"USDC\"\n      />\n    </div>\n  );\n};",
          "notes": [
            "The `value` prop is typically a decimal number (e.g., 0.001 for 0.1%).",
            "It usually handles formatting (percentage display) internally."
          ],
          "related": ["EffectiveFeesWidget", "RegularFeesUI", "FeesWidget"]
        },
        {
          "name": "EffectiveFeesWidget",
          "description": "A composite widget that displays both Maker and Taker effective fees. It is designed to show the user's actual trading costs based on their current tier.",
          "installation": "npm install @orderly.network/ui-order-entry",
          "usage": "Place this in the order entry footer or account settings to inform users of their discounted fee rates.",
          "example": "import React from \"react\";\nimport { EffectiveFeesWidget } from \"@orderly.network/ui-order-entry\";\n\nexport const EffectiveFeesWidgetExample = () => {\n  // Example rates after VIP/discount calculation\n  const makerFee = -0.0001; // Negative implies rebate (0.01% rebate)\n  const takerFee = 0.0003;   // 0.03% fee\n\n  return (\n    <div style={{ border: \"1px dashed #555\", padding: \"10px\" }}>\n      <h4>Your Current Rates</h4>\n      <EffectiveFeesWidget \n        maker={makerFee}\n        taker={takerFee}\n      />\n    </div>\n  );\n};",
          "notes": [
            "Handles negative values (rebates) gracefully, often displaying them in green.",
            "Requires both `maker` and `taker` props to render fully."
          ],
          "related": ["EffectiveFeeUI", "RegularFeesUI", "AccountSummary"]
        },
        {
          "name": "FeesWidget",
          "description": "A widget that fetches and displays fee information for a specific trading symbol. It abstracts the logic of looking up fee tiers for the given market.",
          "installation": "npm install @orderly.network/ui-order-entry",
          "usage": "Useful when you want to display standard fee rates for a specific market without manually calculating effective rates.",
          "example": "import React from \"react\";\nimport { FeesWidget } from \"@orderly.network/ui-order-entry\";\n\nexport const FeesWidgetExample = () => {\n  return (\n    <div>\n      <FeesWidget \n        symbol=\"PERP_BTC_USDC\" \n      />\n    </div>\n  );\n};",
          "notes": [
            "It likely fetches data from the Orderly SDK context or API based on the `symbol` prop.",
            "Ensure the symbol string matches the Orderly format (e.g., PERP_ETH_USDC)."
          ],
          "related": ["EffectiveFeesWidget", "SymbolSelect", "OrderEntry"]
        },
        {
          "name": "RegularFeesUI",
          "description": "Displays the standard (base) fee rates before any discounts or rebates are applied. This serves as a reference point for users to see their savings.",
          "installation": "npm install @orderly.network/ui-order-entry",
          "usage": "Best used in comparison views or tooltips showing 'Standard Rate' vs 'Your Rate'.",
          "example": "import React from \"react\";\nimport { RegularFeesUI } from \"@orderly.network/ui-order-entry\";\n\nexport const RegularFeesUIExample = () => {\n  return (\n    <div style={{ display: \"flex\", flexDirection: \"column\", gap: \"5px\" }}>\n      <h5>Base Fee Rates</h5>\n      <RegularFeesUI \n        maker={0.0002}\n        taker={0.0005}\n      />\n    </div>\n  );\n};",
          "notes": [
            "Unlike `EffectiveFeesWidget`, this is purely presentational and expects raw number props.",
            "Helpful in pricing pages or FAQ sections regarding fees."
          ],
          "related": ["EffectiveFeeUI", "EffectiveFeesWidget", "FeesWidget"]
        },
        {
          "name": "RegularFeesWidget",
          "description": "A display component that renders the Maker and Taker fee rates for a specific market or asset class. It is often used in the footer or header of an order entry form to inform users of trading costs before execution.",
          "installation": "npm install @orderly.network/ui-order-entry",
          "usage": "Install: npm install @orderly.network/ui-order-entry\n\nUse this component when you need to display fee statictics. It accepts string or number props for `taker` and `maker` fees.",
          "example": "import React from \"react\";\nimport { RegularFeesWidget } from \"@orderly.network/ui-order-entry\";\n\nexport const FeeDisplayExample = () => {\n  // Example: Conditional logic for RWA (Real World Assets) vs Standard assets\n  const isRwa = false; \n  const standardFees = { taker: \"0.05\", maker: \"0.02\" };\n  const rwaFees = { taker: \"0.1\", maker: \"0.05\" };\n\n  return (\n    <div className=\"fee-container\">\n      <RegularFeesWidget \n        taker={isRwa ? rwaFees.taker : standardFees.taker}\n        maker={isRwa ? rwaFees.maker : standardFees.maker}\n      />\n    </div>\n  );\n};",
          "notes": [
            "taker (string | number): The fee rate for taker orders (e.g., '0.05').",
            "maker (string | number): The fee rate for maker orders (e.g., '0.02')."
          ],
          "related": ["OrderEntryWidget", "OrderEntry", "FeeSummary"]
        },
        {
          "name": "OrderEntryHeader",
          "description": "The top navigation bar for the Order Entry interface. It displays the trading pair symbol, allows switching between Buy/Sell sides, and indicates the current order type (Limit/Market).",
          "installation": "npm install @orderly.network/ui-order-entry",
          "usage": "Install: npm install @orderly.network/ui-order-entry\n\nPlace this at the top of your custom order form. It handles the visual state of the trading direction.",
          "example": "import React, { useState } from \"react\";\nimport { OrderEntryHeader } from \"@orderly.network/ui-order-entry\";\n\nexport const HeaderExample = () => {\n  const [side, setSide] = useState<\"buy\" | \"sell\">(\"buy\");\n\n  return (\n    <OrderEntryHeader\n      symbol=\"PERP_ETH_USDC\"\n      canTrade={true}\n      side={side}\n      order_type=\"limit\"\n      onSideChange={(newSide) => setSide(newSide)}\n    />\n  );\n};",
          "notes": [
            "symbol (string): The trading pair symbol (e.g., 'PERP_ETH_USDC').",
            "canTrade (boolean): Disables interaction if trading is halted for the symbol.",
            "side ('buy' | 'sell'): The current selected side.",
            "order_type ('limit' | 'market'): The current selected order type."
          ],
          "related": ["OrderEntry", "OrderEntryWidget", "SymbolInfo"]
        },
        {
          "name": "OrderInput",
          "description": "A compound input component handling Price and Amount entry. It manages formatting, decimal precision based on the symbol's tick size, and layout constraints.",
          "installation": "npm install @orderly.network/ui-order-entry",
          "usage": "Install: npm install @orderly.network/ui-order-entry\n\nThis is a controlled component. Pass the current form state via `values` and update it via `onValuesChange`.",
          "example": "import React, { useState } from \"react\";\nimport { OrderInput } from \"@orderly.network/ui-order-entry\";\n\nexport const InputExample = () => {\n  const [values, setValues] = useState({\n    price: \"2000.00\",\n    amount: \"0.5\"\n  });\n\n  return (\n    <div className=\"order-inputs\">\n      <OrderInput \n        values={values}\n        onValuesChange={setValues}\n        priceInputContainerWidth=\"100%\"\n        // Additional props for validation or decimals can be passed here\n      />\n    </div>\n  );\n};",
          "notes": [
            "values (object): Contains { price: string, amount: string }.",
            "onValuesChange (function): Callback when inputs change.",
            "priceInputContainerWidth (string): CSS width for the price input container."
          ],
          "related": ["OrderEntry", "PercentButton", "OrderEntryWidget"]
        },
        {
          "name": "PnlInputProvider",
          "description": "A provider component that wraps the logic and UI for Stop Loss (SL) or Take Profit (TP) inputs. It calculates the trigger price and size based on the parent order context.",
          "installation": "npm install @orderly.network/ui-order-entry",
          "usage": "Install: npm install @orderly.network/ui-order-entry\n\nWrap this around your advanced order settings. The `type` prop determines if it renders Stop Loss or Take Profit fields.",
          "example": "import React from \"react\";\nimport { PnlInputProvider } from \"@orderly.network/ui-order-entry\";\n\nexport const StopLossExample = () => {\n  const stopLossValues = {\n    trigger_price: \"1900.00\",\n    tp_sl_price: \"1950.00\"\n  };\n\n  return (\n    <PnlInputProvider \n      values={stopLossValues} \n      type={\"SL\"} \n      onValuesChange={(vals) => console.log(\"SL Updated:\", vals)}\n    >\n      {/* Internal inputs for Stop Loss are rendered here */}\n    </PnlInputProvider>\n  );\n};",
          "notes": [
            "values (object): The current state of the TP/SL configuration.",
            "type ('SL' | 'TP'): Specifies if this is a Stop Loss or Take Profit input.",
            "onValuesChange (function): Handler for updating TP/SL state."
          ],
          "related": ["OrderEntry", "ReduceOnlySwitch", "OrderEntryWidget"]
        },
        {
          "name": "ReduceOnlySwitch",
          "description": "A toggle switch component specifically for the 'Reduce Only' order flag. When enabled, the order will only close existing positions and not open new ones.",
          "installation": "npm install @orderly.network/ui-order-entry",
          "usage": "Install: npm install @orderly.network/ui-order-entry\n\nUse this in the order configuration section. It is a controlled boolean component.",
          "example": "import React, { useState } from \"react\";\nimport { ReduceOnlySwitch } from \"@orderly.network/ui-order-entry\";\n\nexport const ReduceOnlyExample = () => {\n  const [isReduceOnly, setIsReduceOnly] = useState(false);\n\n  return (\n    <div className=\"flex items-center gap-2\">\n      <ReduceOnlySwitch \n        checked={isReduceOnly} \n        onCheckedChange={setIsReduceOnly} \n      />\n      <span className=\"text-sm\">Reduce Only Position</span>\n    </div>\n  );\n};",
          "notes": [
            "checked (boolean): Whether the switch is active.",
            "onCheckedChange (function): Callback returning the new boolean state."
          ],
          "related": ["OrderEntry", "PostOnlySwitch", "OrderEntryWidget"]
        },
        {
          "name": "OrderEntry",
          "description": "The main container component for the order entry interface. It aggregates the header, inputs, buttons, and fee widgets into a cohesive unit. It handles the business logic for order creation.",
          "installation": "npm install @orderly.network/ui-order-entry",
          "usage": "Install: npm install @orderly.network/ui-order-entry\n\nUse this if you need the full order entry functionality but perhaps want to wrap it in a custom layout or container.",
          "example": "import React from \"react\";\nimport { OrderEntry } from \"@orderly.network/ui-order-entry\";\n\nexport const OrderEntryContainerExample = () => {\n  // This component represents the core logic container\n  // It usually requires a context provider to be higher up in the tree\n  return (\n    <div className=\"order-entry-panel\">\n      <OrderEntry \n        symbol=\"PERP_ETH_USDC\"\n        disableFeatures={{\n          reduceOnly: false,\n          postOnly: false\n        }}\n        onOrderSubmit={(order) => console.log(\"Order Placed:\", order)}\n      />\n    </div>\n  );\n};",
          "notes": [
            "symbol (string): The trading pair symbol.",
            "disableFeatures (object): Object to hide specific features like 'reduceOnly' or 'postOnly'.",
            "containerRef (RefObject): Reference to the container DOM element."
          ],
          "related": ["OrderEntryWidget", "OrderEntryHeader", "OrderInput"]
        },
        {
          "name": "OrderEntryWidget",
          "description": "A high-level, plug-and-play widget that encapsulates the entire Order Entry experience. It includes the header, price/size inputs, order type selection, and submission buttons.",
          "installation": "npm install @orderly.network/ui-order-entry",
          "usage": "Install: npm install @orderly.network/ui-order-entry\n\nThis is the easiest way to add trading functionality to your app. Simply pass the symbol and a container ref.",
          "example": "import React, { useRef } from \"react\";\nimport { OrderEntryWidget } from \"@orderly.network/ui-order-entry\";\n\nexport const WidgetExample = () => {\n  const containerRef = useRef<HTMLDivElement>(null);\n\n  return (\n    <div ref={containerRef} className=\"h-[600px] w-full\">\n      <OrderEntryWidget \n        symbol=\"PERP_BTC_USDC\" \n        containerRef={containerRef}\n        disableFeatures={{\n          // Example: Hiding the TP/SL feature\n          tpSl: true \n        }}\n      />\n    </div>\n  );\n};",
          "notes": [
            "symbol (string): Required symbol for the trading pair.",
            "containerRef (RefObject): Required ref to the parent div for portal/modal positioning.",
            "disableFeatures (object): Allows granular control over which order features are available."
          ],
          "related": ["OrderEntry", "AccountBalance", "MobileOrderListWidget"]
        },
        {
          "name": "MobileOrderListWidget",
          "description": "A responsive widget designed specifically for mobile screens to display a list of open, filled, or historical orders. It supports filtering by symbol.",
          "installation": "npm install @orderly.network/ui-orders",
          "usage": "Install: npm install @orderly.network/ui-orders\n\nIdeal for mobile-first trading interfaces. Pass `undefined` to the symbol prop to show a global order list across all markets.",
          "example": "import React from \"react\";\nimport { MobileOrderListWidget } from \"@orderly.network/ui-orders\";\n\nexport const MobileOrdersExample = () => {\n  return (\n    <div className=\"mobile-orders-container\">\n      {/* If symbol is undefined, it shows orders for all symbols */}\n      <MobileOrderListWidget \n        symbol={\"PERP_ETH_USDC\"} \n        onSymbolChange={(newSymbol) => console.log(\"Filtering by:\", newSymbol)}\n      />\n    </div>\n  );\n};",
          "notes": [
            "symbol (string | undefined): The specific symbol to filter orders. Pass undefined for all symbols.",
            "onSymbolChange (function): Callback when the user changes the symbol filter."
          ],
          "related": ["OrderEntryWidget", "OrderListWidget", "PositionListWidget"]
        },
        {
          "name": "Orders",
          "description": "A comprehensive table component for displaying user orders. It supports different states (open, filled, history) and handles the display of order details like price, size, and status.",
          "installation": "npm install @orderly.network/ui-orders",
          "usage": "Use this component to render the main order management interface in your trading dashboard. It automatically fetches data based on the provided props.",
          "example": "import { Orders } from \"@orderly.network/ui-orders\";\n\nexport const OrdersTable = () => {\n  return (\n    <div className=\"orderly-orders-container\">\n      {/* Display open orders for a specific symbol */}\n      <Orders \n        status=\"open\" \n        symbol=\"PERP_ETH_USDC\" \n      />\n    </div>\n  );\n};",
          "notes": [
            "Ensure the app is wrapped in OrderlyProvider.",
            "Install: npm install @orderly.network/ui-orders"
          ],
          "related": ["OrderlyProvider", "useOrders", "CancelOrderButton"]
        },
        {
          "name": "ShareButtonWidget",
          "description": "A button widget that triggers a modal allowing users to share their Profit and Loss (PnL) or specific trade details, often for social sharing or community engagement.",
          "installation": "npm install @orderly.network/ui-orders",
          "usage": "Place this button next to specific order or position rows in a table to allow users to share their performance.",
          "example": "import { ShareButtonWidget } from \"@orderly.network/ui-orders\";\n\n// Mock order object\nconst mockOrder = {\n  order_id: 12345,\n  symbol: \"PERP_BTC_USDC\",\n  side: \"BUY\",\n  price: 50000,\n  quantity: 0.1\n};\n\nexport const TradeShareButton = () => {\n  return (\n    <ShareButtonWidget \n      order={mockOrder} \n      sharePnLConfig={{\n        // Configuration for the PnL sharing modal\n        title: \"Check out my trade!\",\n        showChart: true\n      }}\n      modalId=\"share-pnl-modal\"\n    />\n  );\n};",
          "notes": [
            "Can accept either an 'order' or 'position' prop.",
            "Requires a 'sharePnLConfig' object to customize the shareable content."
          ],
          "related": ["SharePnLModal", "Positions", "usePositions"]
        },
        {
          "name": "CloseAllPositionsWidget",
          "description": "A widget (typically a button or small control panel) that allows users to liquidate all open positions for a specific symbol or for the entire account.",
          "installation": "npm install @orderly.network/ui-positions",
          "usage": "Useful in risk management sections or at the top of a positions table to allow users to exit markets quickly.",
          "example": "import { CloseAllPositionsWidget } from \"@orderly.network/ui-positions\";\n\nexport const CloseAllPositions = () => {\n  return (\n    <div className=\"risk-controls\">\n      <h3>Position Management</h3>\n      {/* Closes all positions for PERP_ETH_USDC */}\n      <CloseAllPositionsWidget symbol=\"PERP_ETH_USDC\" />\n      \n      {/* Omit symbol to close all positions across all markets */}\n      {/* <CloseAllPositionsWidget /> */}\n    </div>\n  );\n};",
          "notes": [
            "Install: npm install @orderly.network/ui-positions",
            "Passing 'undefined' or omitting the symbol prop usually implies closing all positions globally."
          ],
          "related": ["Positions", "usePositions", "ReduceOnlyOrder"]
        },
        {
          "name": "LiquidationWidget",
          "description": "A data table component displaying historical liquidation events. It shows details such as the time of liquidation, price, and size.",
          "installation": "npm install @orderly.network/ui-positions",
          "usage": "Implement in the 'History' or 'Reports' tab of your application to show users past liquidation events.",
          "example": "import { LiquidationWidget } from \"@orderly.network/ui-positions\";\n\nexport const LiquidationHistory = () => {\n  return (\n    <div className=\"history-section\">\n      <h2>Liquidation History</h2>\n      {/* Shows liquidation history for a specific symbol */}\n      <LiquidationWidget symbol=\"PERP_SOL_USDC\" />\n    </div>\n  );\n};",
          "notes": [
            "If the 'symbol' prop is undefined, it typically shows liquidations for all symbols."
          ],
          "related": ["MobileLiquidationWidget", "PositionHistoryWidget", "useAccountHistory"]
        },
        {
          "name": "MobileLiquidationWidget",
          "description": "A mobile-optimized version of the LiquidationWidget. It presents liquidation records in a card or list format suitable for smaller screens rather than a wide table.",
          "installation": "npm install @orderly.network/ui-positions",
          "usage": "Use this within your mobile-specific layout routes or responsive breakpoints where the desktop table would not fit.",
          "example": "import { MobileLiquidationWidget } from \"@orderly.network/ui-positions\";\n\nexport const MobileLiquidationList = () => {\n  return (\n    <div className=\"mobile-view\">\n      <MobileLiquidationWidget \n        enableLoadMore \n        symbol={undefined} // undefined shows all symbols\n        classNames={{\n          cell: \"text-sm text-gray-400\",\n          row: \"border-b border-gray-800\"\n        }}\n      />\n    </div>\n  );\n};",
          "notes": [
            "Supports custom class names for styling cells and rows to match mobile themes.",
            "Includes built-in load more functionality via 'enableLoadMore'."
          ],
          "related": ["LiquidationWidget", "MobilePositionHistoryWidget", "useBreakpoints"]
        },
        {
          "name": "PositionHistoryWidget",
          "description": "Displays a table of closed positions. It calculates and shows the realized PnL for positions that have been exited.",
          "installation": "npm install @orderly.network/ui-positions",
          "usage": "Essential for the 'History' section, allowing users to review their past trading performance on specific assets.",
          "example": "import { PositionHistoryWidget } from \"@orderly.network/ui-positions\";\n\nexport const ClosedPositions = () => {\n  return (\n    <PositionHistoryWidget \n      symbol=\"PERP_ETH_USDC\"\n      pnlNotionalDecimalPrecision={2} \n    />\n  );\n};",
          "notes": [
            "Use 'pnlNotionalDecimalPrecision' to control how many decimal places are shown for PnL values."
          ],
          "related": ["MobilePositionHistoryWidget", "Positions", "useAccountHistory"]
        },
        {
          "name": "MobilePositionHistoryWidget",
          "description": "A mobile-friendly card list view of closed position history. It allows users to browse their past trades on mobile devices.",
          "installation": "npm install @orderly.network/ui-positions",
          "usage": "Deploy this in your mobile trading interface to replace the standard desktop history table.",
          "example": "import { MobilePositionHistoryWidget } from \"@orderly.network/ui-positions\";\n\nexport const MobileClosedPositions = () => {\n  const handleSymbolChange = (symbol: string) => {\n    console.log(\"Filtering by symbol:\", symbol);\n  };\n\n  return (\n    <MobilePositionHistoryWidget \n      symbol={undefined} // Shows all symbols initially\n      onSymbolChange={handleSymbolChange}\n    />\n  );\n};",
          "notes": [
            "Provides an 'onSymbolChange' callback to handle filtering logic when a user selects a specific symbol from the history."
          ],
          "related": ["PositionHistoryWidget", "MobileLiquidationWidget", "useSymbolInfo"]
        },
        {
          "name": "NumeralWithCtx",
          "description": "A utility component to format numerical values. It automatically determines decimal precision based on the current trading symbol's context (defined in OrderlyConfig).",
          "installation": "npm install @orderly.network/ui-positions",
          "usage": "Use this whenever displaying prices, sizes, or notional values to ensure they adhere to the specific exchange rules for that asset.",
          "example": "import { NumeralWithCtx } from \"@orderly.network/ui-positions\";\nimport Decimal from \"decimal.js\";\n\nexport const LiquidationPriceDisplay = ({ price }: { price: number }) => {\n  return (\n    <div className=\"price-info\">\n      <span>Liq Price: </span>\n      {/* Formats the number based on the symbol's context/precision */}\n      <NumeralWithCtx \n        value={price} \n        rm={Decimal.ROUND_DOWN} // Rounding mode\n        className=\"text-red-500 font-mono\"\n      />\n    </div>\n  );\n};",
          "notes": [
            "Accepts a 'rm' (rounding mode) prop from the Decimal.js library (e.g., ROUND_UP, ROUND_DOWN).",
            "Requires context to know which symbol's precision to apply."
          ],
          "related": ["useSymbolInfo", "Decimal", "FormatNumber"]
        },
        {
          "name": "Positions",
          "description": "A core UI component that renders a table of the user's current trading positions. It handles the display of standard position data such as symbol, size, entry price, and mark price.",
          "installation": "npm install @orderly.network/ui-positions",
          "usage": "Use this when you need a custom layout or want to embed the positions table within your own UI container (e.g., inside a dashboard grid) without the default widget wrapper.",
          "example": "import { Positions } from \"@orderly.network/ui-positions\";\n\nexport const MyPositionsTable = () => {\n  return (\n    <div className=\"orderly-positions-container\">\n      <Positions \n        onRowClick={(position) => console.log(\"Clicked position:\", position)}\n        showHeader={true}\n      />\n    </div>\n  );\n};",
          "notes": [
            "Requires an OrderlyProvider context to fetch position data.",
            "This component focuses purely on the data table; it does not include the outer card styling or headers found in the Widget version."
          ],
          "related": ["PositionsWidget", "MobilePositions", "usePositions"]
        },
        {
          "name": "MobilePositions",
          "description": "A mobile-optimized list view for displaying positions. Instead of a dense table, it typically renders position data in a vertical card or expanded list format suitable for touch interfaces.",
          "installation": "npm install @orderly.network/ui-positions",
          "usage": "Ideal for mobile-first applications or responsive layouts where the standard desktop table is too cramped.",
          "example": "import { MobilePositions } from \"@orderly.network/ui-positions\";\n\nexport const MyMobilePositions = () => {\n  return (\n    <div className=\"orderly-mobile-positions\">\n      <MobilePositions \n        onSymbolClick={(symbol) => console.log(\"Navigating to:\", symbol)}\n      />\n    </div>\n  );\n};",
          "notes": [
            "Designed for smaller viewports.",
            "Interactions are tailored for touch (e.g., larger tap targets)."
          ],
          "related": ["Positions", "MobilePositionsWidget", "usePositions"]
        },
        {
          "name": "CombinePositions",
          "description": "Displays positions in a combined or aggregated format. This is often used to show net positions or group positions by underlying asset in cross-margin modes.",
          "installation": "npm install @orderly.network/ui-positions",
          "usage": "Use this when you want to present a simplified view of risk exposure by combining similar positions rather than listing every individual order.",
          "example": "import { CombinePositions } from \"@orderly.network/ui-positions\";\n\nexport const CombinedView = () => {\n  return (\n    <CombinePositions \n      // Props typically align with Positions but may aggregate data differently\n      onRowClick={(data) => console.log(\"Combined data:\", data)} \n    />\n  );\n};",
          "notes": [
            "Useful for portfolio overview dashboards.",
            "Data aggregation logic is handled internally based on the account mode."
          ],
          "related": ["Positions", "CombinePositionsWidget", "AccountSummary"]
        },
        {
          "name": "PositionsWidget",
          "description": "A complete, self-contained widget that includes the header, filters, and the positions table. It is the standard 'drop-in' solution for displaying positions on a desktop trading interface.",
          "installation": "npm install @orderly.network/ui-positions",
          "usage": "The default choice for most desktop trading interfaces. It manages its own layout and state presentation.",
          "example": "import { PositionsWidget } from \"@orderly.network/ui-positions\";\n\nexport const PositionPanel = () => {\n  return (\n    <PositionsWidget \n      // Filter to a specific symbol, or undefined for all\n      symbol=\"PERP_ETH_USDC\" \n      // Customize decimal precision for PnL display\n      pnlNotionalDecimalPrecision={2}\n      onSymbolClick={(symbol) => console.log(\"Switch to:\", symbol)}\n    />\n  );\n};",
          "notes": [
            "Internal usage indicates it supports filtering by `symbol`.",
            "Includes built-in UI for headers and action buttons."
          ],
          "related": ["Positions", "MobilePositionsWidget", "AccountSummary"]
        },
        {
          "name": "MobilePositionsWidget",
          "description": "The mobile counterpart to the PositionsWidget. It wraps the mobile-optimized list view with appropriate headers and navigation controls for a mobile app experience.",
          "installation": "npm install @orderly.network/ui-positions",
          "usage": "Best for full-screen mobile views or bottom sheets in trading apps.",
          "example": "import { MobilePositionsWidget } from \"@orderly.network/ui-positions\";\n\nexport const MobilePositionPanel = () => {\n  return (\n    <MobilePositionsWidget \n      symbol=\"PERP_BTC_USDC\"\n      onSymbolChange={(newSymbol) => console.log(\"Symbol changed:\", newSymbol)}\n    />\n  );\n};",
          "notes": [
            "Handles symbol switching logic via `onSymbolChange`.",
            "Responsive design adapts to mobile screen widths automatically."
          ],
          "related": ["MobilePositions", "PositionsWidget", "usePositions"]
        },
        {
          "name": "CombinePositionsWidget",
          "description": "A widget wrapper for the CombinePositions component. It provides the aggregated position view within a styled container suitable for a dashboard.",
          "installation": "npm install @orderly.network/ui-positions",
          "usage": "Use when you need the 'Combine' logic but want the consistent look and feel of other Orderly widgets.",
          "example": "import { CombinePositionsWidget } from \"@orderly.network/ui-positions\";\n\nexport const CombinedWidget = () => {\n  return (\n    <CombinePositionsWidget \n      // Displays aggregated positions in a widget container\n      showAllSymbol={true}\n    />\n  );\n};",
          "notes": [
            "Useful for risk management views where individual order details are less important than total exposure."
          ],
          "related": ["CombinePositions", "PositionsWidget", "AccountSummary"]
        },
        {
          "name": "ReversePositionWidget",
          "description": "A specialized widget that allows users to quickly reverse their position (e.g., closing a Long and opening a Short of the same size) in a single action.",
          "installation": "npm install @orderly.network/ui-positions",
          "usage": "Ideal for advanced trading interfaces where users frequently flip their market sentiment on a specific asset.",
          "example": "import { ReversePositionWidget } from \"@orderly.network/ui-positions\";\n\nexport const ReversePositionTool = () => {\n  return (\n    <ReversePositionWidget \n      symbol=\"PERP_SOL_USDC\"\n      onClose={() => console.log(\"Widget closed\")}\n    />\n  );\n};",
          "notes": [
            "This is a high-level trading action component.",
            "Requires valid position data to function effectively."
          ],
          "related": ["PositionsWidget", "usePositions", "OrderlyProvider"]
        },
        {
          "name": "AccountSummary",
          "description": "A flexible component that renders a summary of the user's account state, such as equity, margin ratio, and PnL. It allows customization of which fields are displayed via `elementKeys`.",
          "installation": "npm install @orderly.network/ui-scaffold",
          "usage": "Perfect for the top bar of a trading app or a dashboard overview card to give users a quick health check of their account.",
          "example": "import { AccountSummary } from \"@orderly.network/ui-scaffold\";\n\n// Define which keys you want to display\nconst summaryKeys = {\n  total_equity: \"Total Equity\",\n  unrealized_pnl: \"Unrealized PnL\",\n  available_balance: \"Available Balance\"\n};\n\nexport const MyAccountSummary = () => {\n  return (\n    <AccountSummary \n      elementKeys={summaryKeys}\n      // Additional state props can be passed if needed\n    />\n  );\n};",
          "notes": [
            "Found in `@orderly.network/ui-scaffold`.",
            "The `elementKeys` prop maps internal data keys to display labels or determines visibility."
          ],
          "related": ["PositionsWidget", "useAccount", "OrderlyProvider"]
        },
        {
          "name": "BottomNav",
          "description": "A mobile-first bottom navigation bar component used to switch between primary views in the application.",
          "installation": "npm install @orderly.network/ui-scaffold",
          "usage": "Use this component to render the main navigation menu on mobile devices. It accepts an array of menu objects defining the label, icons, and click handlers.",
          "example": "import React from \"react\";\nimport { BottomNav } from \"@orderly.network/ui-scaffold\";\nimport { HomeIcon, ChartIcon } from \"@orderly.network/icons\"; // Assuming icon usage\n\nconst App = () => {\n  const mainMenus = [\n    {\n      name: \"Market\",\n      icon: <HomeIcon />,\n      activeIcon: <HomeIcon className=\"text-blue-500\" />,\n      onClick: () => console.log(\"Navigate to Market\"),\n    },\n    {\n      name: \"Trade\",\n      icon: <ChartIcon />,\n      activeIcon: <ChartIcon className=\"text-blue-500\" />,\n      onClick: () => console.log(\"Navigate to Trade\"),\n    },\n  ];\n\n  return (\n    <div className=\"h-screen\">\n      {/* Main Content */}\n      <div className=\"pb-20\">Content goes here</div>\n      \n      {/* Bottom Navigation */}\n      <BottomNav mainMenus={mainMenus} />\n    </div>\n  );\n};",
          "notes": [
            "Typically fixed to the bottom of the viewport on mobile screens.",
            "Ensure icons provided are valid React elements.",
            "The 'mainMenus' prop is required and expects an array of menu configuration objects."
          ],
          "related": ["BottomNavTradingMenu", "MobileScaffold", "BottomNavWidget"]
        },
        {
          "name": "BottomNavWidget",
          "description": "A widget wrapper for the BottomNav component, likely providing pre-configured state or context integration.",
          "installation": "npm install @orderly.network/ui-scaffold",
          "usage": "Use this when you need a drop-in navigation solution that handles internal state or routing logic automatically without manually defining menu items.",
          "example": "import React from \"react\";\nimport { BottomNavWidget } from \"@orderly.network/ui-scaffold\";\n\nconst App = () => {\n  return (\n    <div className=\"relative h-full\">\n      {/* Page Content */}\n      <main className=\"p-4\">Trading View</main>\n\n      {/* Widget Wrapper for Bottom Nav */}\n      <BottomNavWidget \n        // Props may vary based on internal widget configuration\n        // Often wraps the UI component with additional logic or context\n      />\n    </div>\n  );\n};",
          "notes": [
            "This is the 'Widget' variant, implying it may connect to the Orderly store or router automatically.",
            "Check specific SDK version for default routing behavior if props are omitted."
          ],
          "related": ["BottomNav", "MainNavWidget", "MobileScaffold"]
        },
        {
          "name": "BottomNavTradingMenu",
          "description": "A specific menu item component designed for trading-related navigation within the bottom bar.",
          "installation": "npm install @orderly.network/ui-scaffold",
          "usage": "Use this to render individual tabs, specifically highlighting trading states. It handles the visual distinction between active and inactive states.",
          "example": "import React from \"react\";\nimport { BottomNavTradingMenu } from \"@orderly.network/ui-scaffold\";\nimport { TradeIcon, TradeActiveIcon } from \"./icons\";\n\nconst TradingTab = () => {\n  const isActive = true; // Derived from router state\n\n  return (\n    <BottomNavTradingMenu\n      label=\"Perpetual\"\n      active={isActive}\n      icon={<TradeIcon />}\n      activeIcon={<TradeActiveIcon />}\n      onClick={() => console.log(\"Trading tab clicked\")}\n    />\n  );\n};",
          "notes": [
            "Requires 'active' prop to toggle styling.",
            "Accepts separate 'icon' and 'activeIcon' props for visual feedback.",
            "Commonly used inside the 'mainMenus' array of the BottomNav."
          ],
          "related": ["BottomNav", "CloseIcon"]
        },
        {
          "name": "FooterWidget",
          "description": "A footer component, typically used to display legal links, social icons, or application version information.",
          "installation": "npm install @orderly.network/ui-scaffold",
          "usage": "Place this at the bottom of your application layout or specific pages to provide standard footer information.",
          "example": "import React from \"react\";\nimport { FooterWidget } from \"@orderly.network/ui-scaffold\";\n\nconst PageLayout = () => {\n  return (\n    <div className=\"flex flex-col min-h-screen\">\n      <main className=\"flex-grow\">Main Content</main>\n      \n      <FooterWidget \n        // May accept props for links, version info, or custom text depending on implementation\n        className=\"border-t border-gray-800\"\n      />\n    </div>\n  );\n};",
          "notes": [
            "Often styled with a dark theme or border separator.",
            "Content may be static or configurable via props."
          ],
          "related": ["MobileScaffold", "MainNavWidget"]
        },
        {
          "name": "CloseIcon",
          "description": "A simple, reusable 'X' icon component used for closing modals, drawers, or dismissing alerts.",
          "installation": "npm install @orderly.network/ui-scaffold",
          "usage": "Implement this anywhere a dismiss action is required. It supports standard SVG props like size and opacity.",
          "example": "import React from \"react\";\nimport { CloseIcon } from \"@orderly.network/ui-scaffold\";\n\nconst ModalHeader = ({ onClose }) => {\n  return (\n    <div className=\"flex justify-between items-center p-4\">\n      <h2>Settings</h2>\n      <CloseIcon \n        size={20} \n        className=\"oui-text-base-contrast-80 oui-cursor-pointer hover:opacity-80\" \n        opacity={1} \n        onClick={onClose}\n      />\n    </div>\n  );\n};",
          "notes": [
            "Supports 'size' prop to scale the icon.",
            "Commonly used with 'className' for cursor pointer and hover effects.",
            "Internal usage suggests 'opacity' is a controlled prop."
          ],
          "related": ["BottomNav", "OfflineInfoWidget"]
        },
        {
          "name": "MainNavWidget",
          "description": "A navigation widget intended for desktop or main layouts, likely appearing as a sidebar or top header.",
          "installation": "npm install @orderly.network/ui-scaffold",
          "usage": "Use this in desktop views to provide the primary application navigation structure.",
          "example": "import React from \"react\";\nimport { MainNavWidget } from \"@orderly.network/ui-scaffold\";\n\nconst DesktopLayout = () => {\n  return (\n    <div className=\"flex h-screen\">\n      {/* Sidebar Navigation for Desktop */}\n      <div className=\"w-64 border-r border-gray-800\">\n        <MainNavWidget />\n      </div>\n      \n      <main className=\"flex-1 p-6\">\n        Dashboard Content\n      </main>\n    </div>\n  );\n};",
          "notes": [
            "Distinct from BottomNav, usually for larger viewports.",
            "May handle routing logic internally."
          ],
          "related": ["BottomNavWidget", "FooterWidget"]
        },
        {
          "name": "OfflineInfoWidget",
          "description": "A component that monitors network connectivity and displays an indicator or message when the application is offline.",
          "installation": "npm install @orderly.network/ui-scaffold",
          "usage": "Include this in your root layout to ensure users are aware of connectivity issues affecting trading data.",
          "example": "import React from \"react\";\nimport { OfflineInfoWidget } from \"@orderly.network/ui-scaffold\";\n\nconst AppRoot = () => {\n  return (\n    <div className=\"relative\">\n      <OfflineInfoWidget \n        // May customize the offline message or appearance\n      />\n      \n      {/* Rest of the application */}\n      <h1>Trading App</h1>\n    </div>\n  );\n};",
          "notes": [
            "Crucial for real-time trading applications.",
            "Likely listens to window 'online' and 'offline' events."
          ],
          "related": ["MobileScaffold", "FooterWidget"]
        },
        {
          "name": "MobileScaffold",
          "description": "A layout wrapper component specifically designed for mobile views, handling header, footer, and content area positioning.",
          "installation": "npm install @orderly.network/ui-scaffold",
          "usage": "Wrap your mobile pages with this component to ensure consistent layout structure, spacing, and scroll behavior.",
          "example": "import React from \"react\";\nimport { MobileScaffold } from \"@orderly.network/ui-scaffold\";\nimport { BottomNav } from \"./components/BottomNav\";\n\nconst MobilePage = () => {\n  return (\n    <MobileScaffold\n      header={\n        <div className=\"p-4 font-bold text-lg\">\n          Orderly App\n        </div>\n      }\n      footer={<BottomNav />}\n    >\n      <div className=\"p-4\">\n        <p>Scrollable content area goes here.</p>\n        <p>The scaffold handles the layout structure.</p>\n      </div>\n    </MobileScaffold>\n  );\n};",
          "notes": [
            "Accepts 'header' and 'footer' props to inject navigation bars.",
            "Manages the viewport height and scrolling internally.",
            "Essential for maintaining the 'app-like' feel on mobile browsers."
          ],
          "related": ["BottomNav", "MainNavWidget", "FooterWidget"]
        },
        {
          "name": "DesktopScaffold",
          "description": "A layout wrapper specifically designed for desktop views. It structures the application grid, handling the positioning of the sidebar, header, and main content area without requiring manual layout configuration.",
          "installation": "npm install @orderly.network/ui-scaffold",
          "usage": "Wrap your root application component or specific desktop-only routes with DesktopScaffold to ensure consistent layout structure.",
          "example": "import React from \"react\";\nimport { DesktopScaffold } from \"@orderly.network/ui-scaffold\";\n\nexport const AppLayout = () => {\n  return (\n    <DesktopScaffold>\n      {/* Main application content goes here */}\n      <div className=\"p-4\">\n        <h1>Trading Dashboard</h1>\n      </div>\n    </DesktopScaffold>\n  );\n};",
          "notes": [
            "Typically used internally by the main Scaffold component but can be used for custom desktop layouts.",
            "Ensure the parent container has a defined height if using in a specific viewport."
          ],
          "related": ["Scaffold", "ScaffoldProvider", "SideNavbarWidget"]
        },
        {
          "name": "Scaffold",
          "description": "The main application shell component that combines the sidebar, header, and footer. It provides a ready-to-use layout for decentralized exchange applications.",
          "installation": "npm install @orderly.network/ui-scaffold",
          "usage": "Use this as the outermost layout component in your application to provide consistent navigation and branding.",
          "example": "import React from \"react\";\nimport { Scaffold } from \"@orderly.network/ui-scaffold\";\n\nexport const AppShell = () => {\n  return (\n    <Scaffold\n      footerProps={{\n        telegramUrl: \"https://t.me/orderlynetwork\",\n        discordUrl: \"https://discord.gg/orderlynetwork\",\n        twitterUrl: \"https://twitter.com/orderlynetwork\",\n        trailing: (\n          <div className=\"text-xs text-gray-500\">\n            v1.0.0\n          </div>\n        )\n      }}\n    >\n      <div className=\"p-6\">\n        {/* Your page content */}\n      </div>\n    </Scaffold>\n  );\n};",
          "notes": [
            "Accepts `footerProps` to configure social media links and custom footer elements.",
            "Automatically integrates with the ScaffoldProvider for state management."
          ],
          "related": ["ScaffoldProvider", "DesktopScaffold", "SideNavbarWidget"]
        },
        {
          "name": "ScaffoldProvider",
          "description": "Context provider that manages the global state of the application scaffold, such as sidebar expansion state and routing integration.",
          "installation": "npm install @orderly.network/ui-scaffold",
          "usage": "Wrap your entire application (or the part containing the Scaffold) with this provider to enable navigation and layout state control.",
          "example": "import React, { useState } from \"react\";\nimport { ScaffoldProvider } from \"@orderly.network/ui-scaffold\";\n\n// Mock router adapter\nconst mockRouterAdapter = {\n  push: (path: string) => console.log(\"Navigating to\", path),\n  pathname: \"/trade\"\n};\n\nexport const AppProvider = ({ children }: { children: React.ReactNode }) => {\n  const [isSidebarExpanded, setSidebarExpanded] = useState(true);\n\n  return (\n    <ScaffoldProvider\n      routerAdapter={mockRouterAdapter}\n      expanded={isSidebarExpanded}\n      setExpand={setSidebarExpanded}\n    >\n      {children}\n    </ScaffoldProvider>\n  );\n};",
          "notes": [
            "Requires a `routerAdapter` prop to bridge the UI with your routing library (e.g., React Router).",
            "Controls the `expanded` state of the sidebar, allowing you to programmatically toggle it."
          ],
          "related": ["Scaffold", "SideNavbarWidget"]
        },
        {
          "name": "SideNavbarWidget",
          "description": "The sidebar navigation widget containing links to different sections of the app like Trade, Portfolio, and History.",
          "installation": "npm install @orderly.network/ui-scaffold",
          "usage": "Often used within the Scaffold layout, but can be extracted for custom layout implementations requiring a standalone sidebar.",
          "example": "import React from \"react\";\nimport { SideNavbarWidget } from \"@orderly.network/ui-scaffold\";\n\nexport const Sidebar = () => {\n  return (\n    <div className=\"w-64 h-full border-r border-gray-800\">\n      <SideNavbarWidget \n        // Props usually inferred from context or passed explicitly\n        // depending on specific implementation version\n      />\n    </div>\n  );\n};",
          "notes": [
            "Usually reads active route from the ScaffoldProvider context.",
            "Styling is typically handled by the parent container or the widget itself."
          ],
          "related": ["Scaffold", "ScaffoldProvider"]
        },
        {
          "name": "SubAccountUI",
          "description": "A UI component for managing and switching between sub-accounts. It renders a dropdown or modal interface triggered by a custom element.",
          "installation": "npm install @orderly.network/ui-scaffold",
          "usage": "Place in the header or settings area to allow users to select different trading sub-accounts.",
          "example": "import React from \"react\";\nimport { SubAccountUI } from \"@orderly.network/ui-scaffold\";\n\nexport const AccountSwitcher = () => {\n  return (\n    <div className=\"flex items-center justify-end p-4\">\n      <SubAccountUI \n        customTrigger={\n          <button className=\"px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700\">\n            Switch Account\n          </button>\n        }\n      />\n    </div>\n  );\n};",
          "notes": [
            "The `customTrigger` prop allows you to fully customize the button that opens the account selection menu.",
            "Requires account data context to function correctly."
          ],
          "related": ["Scaffold", "AccountInfo"]
        },
        {
          "name": "Dot",
          "description": "A simple indicator dot component, commonly used in carousels or pagination to show the currently active item.",
          "installation": "npm install @orderly.network/ui-share",
          "usage": "Map over an array of items to render a dot for each, passing the `active` boolean to highlight the current selection.",
          "example": "import React, { useState } from \"react\";\nimport { Dot } from \"@orderly.network/ui-share\";\n\nexport const CarouselIndicator = () => {\n  const [activeIndex, setActiveIndex] = useState(0);\n  const totalSlides = 5;\n\n  return (\n    <div className=\"flex gap-2\">\n      {Array.from({ length: totalSlides }).map((_, index) => (\n        <Dot\n          key={index}\n          index={index}\n          active={index === activeIndex}\n          onClick={() => setActiveIndex(index)}\n        />\n      ))}\n    </div>\n  );\n};",
          "notes": [
            "Accepts an `onClick` handler to make the dots interactive.",
            "Styling changes based on the `active` prop (e.g., filled vs. outline)."
          ],
          "related": ["Carousel", "SharePnL"]
        },
        {
          "name": "PriceInput",
          "description": "A specialized input field for entering order prices. It handles logic for both Market and Limit order types and displays Best Bid/Offer (BBO) data.",
          "installation": "npm install @orderly.network/ui-tpsl",
          "usage": "Use in order entry forms to capture the price from the user, providing context about the current market price.",
          "example": "import React from \"react\";\nimport { PriceInput } from \"@orderly.network/ui-tpsl\";\n\nexport const OrderPriceInput = () => {\n  return (\n    <div className=\"space-y-4\">\n      <PriceInput\n        order_type=\"LIMIT\"\n        order_price=\"100.50\"\n        bbo={{\n          bid: 100.00,\n          ask: 100.50,\n          timestamp: Date.now()\n        }}\n        onPriceChange={(price) => console.log(\"Price changed:\", price)}\n      />\n      \n      <PriceInput\n        order_type=\"MARKET\"\n        order_price=\"Market\"\n        bbo={{\n          bid: 100.00,\n          ask: 100.50,\n          timestamp: Date.now()\n        }}\n      />\n    </div>\n  );\n};",
          "notes": [
            "When `order_type` is 'MARKET', the input is typically disabled or displays 'Market'.",
            "The `bbo` prop is used to display price hints or spread information."
          ],
          "related": ["TPSL", "OrderEntry"]
        },
        {
          "name": "TPSL",
          "description": "A comprehensive UI component for setting Take Profit and Stop Loss orders. It provides inputs for trigger prices and calculates estimated returns.",
          "installation": "npm install @orderly.network/ui-tpsl",
          "usage": "Integrate into position management modals or trading views to allow users to set risk management parameters for their positions.",
          "example": "import React from \"react\";\nimport { TPSL } from \"@orderly.network/ui-tpsl\";\n\nexport const TakeProfitStopLoss = () => {\n  const handleComplete = (data: any) => {\n    console.log(\"TPSL Set:\", data);\n    // API call to set TP/SL\n  };\n\n  const handleCancel = () => {\n    console.log(\"TPSL Cancelled\");\n  };\n\n  return (\n    <div className=\"p-4 border rounded\">\n      <TPSL\n        symbol=\"BTC-USDT\"\n        side=\"LONG\"\n        positionSize={0.1}\n        entryPrice={40000}\n        onCancel={handleCancel}\n        onComplete={handleComplete}\n        close={() => console.log(\"Close modal\")}\n      />\n    </div>\n  );\n};",
          "notes": [
            "Requires position context (symbol, side, size) to calculate valid price ranges.",
            "The `onComplete` callback is triggered when the user confirms the TP/SL settings."
          ],
          "related": ["PriceInput", "PositionTable"]
        },
        {
          "name": "TPSLWidget",
          "description": "A widget component used to manage Take Profit (TP) and Stop Loss (SL) settings for a specific trading position. It typically handles the logic for calculating trigger prices and executing orders based on the position type (Long/Short).",
          "installation": "npm install @orderly.network/ui-tpsl",
          "usage": "Use this component in position management modals or sidebars to allow users to define risk management parameters for an existing trade.",
          "example": "import React from \"react\";\nimport { TPSLWidget } from \"@orderly.network/ui-tpsl\";\nimport { PositionType } from \"@orderly.network/types\";\n\nexport const TPSLExample = () => {\n  return (\n    <div className=\"orderly-tpsl-container\">\n      <TPSLWidget\n        symbol=\"BTC-PERP\"\n        positionType={PositionType.LONG}\n        // Additional props usually inferred from context or passed explicitly\n        // such as averageOpenPrice, size, etc.\n      />\n    </div>\n  );\n};",
          "notes": [
            "Requires position context (symbol, positionType) to function correctly.",
            "Often used within a larger position details view."
          ],
          "related": ["TPSLSimpleDialogWidget", "PositionTable", "usePosition"]
        },
        {
          "name": "TPSLSimpleDialogUI",
          "description": "The UI-only presentation layer for a simplified Take Profit/Stop Loss dialog. It renders the form fields and buttons but relies on parent components for state management and logic execution.",
          "installation": "npm install @orderly.network/ui-tpsl",
          "usage": "Useful when building a custom TP/SL flow where you need full control over the dialog state and logic, but want to reuse Orderly's standardized visual design.",
          "example": "import React from \"react\";\nimport { TPSLSimpleDialogUI } from \"@orderly.network/ui-tpsl\";\n\nexport const TPSLSimpleDialogExample = () => {\n  const handleClose = () => console.log(\"Dialog closed\");\n  const handleComplete = (data) => console.log(\"TPSL Set:\", data);\n\n  return (\n    <TPSLSimpleDialogUI\n      open={true}\n      close={handleClose}\n      onComplete={handleComplete}\n      showAdvancedTPSLDialog={() => console.log(\"Switch to advanced\")}\n      // Props representing form state\n      symbol=\"BTC-PERP\"\n      side=\"BUY\"\n    />\n  );\n};",
          "notes": [
            "This is a 'UI' component, meaning it is primarily presentational.",
            "Requires callbacks for `close`, `onComplete`, and navigation to advanced views."
          ],
          "related": ["TPSLSimpleDialogWidget", "TPSLWidget", "Dialog"]
        },
        {
          "name": "TPSLSimpleDialogWidget",
          "description": "A fully functional widget wrapper for the simplified TP/SL dialog. It encapsulates both the UI (TPSLSimpleDialogUI) and the business logic required to fetch position data and submit TP/SL orders.",
          "installation": "npm install @orderly.network/ui-tpsl",
          "usage": "Ideal for quick integration where a standard 'Set TP/SL' interaction is needed without building custom state logic.",
          "example": "import React from \"react\";\nimport { TPSLSimpleDialogWidget } from \"@orderly.network/ui-tpsl\";\n\nexport const TPSLSimpleWidgetExample = () => {\n  return (\n    <TPSLSimpleDialogWidget\n      symbol=\"ETH-PERP\"\n      // The widget typically handles internal state for the dialog\n      // and triggers submission to the Orderly API.\n    />\n  );\n};",
          "notes": [
            "No internal usages found in the provided context, suggesting it is a standalone entry point.",
            "Likely handles API calls internally via the Orderly provider."
          ],
          "related": ["TPSLSimpleDialogUI", "TPSLWidget", "useAlpacaOrderly"]
        },
        {
          "name": "DesktopDisplayControl",
          "description": "A desktop-specific control bar for the TradingView chart interface. It provides buttons to toggle various chart display settings like indicators, drawing tools, or chart types.",
          "installation": "npm install @orderly.network/ui-tradingview",
          "usage": "Place this in the header or toolbar of a desktop trading view to give users control over their chart environment.",
          "example": "import React from \"react\";\nimport { DesktopDisplayControl } from \"@orderly.network/ui-tradingview\";\n\nexport const DesktopDisplayControlExample = () => {\n  return (\n    <div className=\"flex justify-end p-2\">\n      <DesktopDisplayControl\n        // Props typically control visibility of indicators, drawing tools, etc.\n        onToggleIndicators={() => console.log(\"Toggle Indicators\")}\n      />\n    </div>\n  );\n};",
          "notes": [
            "Designed specifically for desktop layouts.",
            "Often paired with other chart controls like TimeInterval."
          ],
          "related": ["MobileDisplayControl", "TimeInterval", "TradingView"]
        },
        {
          "name": "MobileDisplayControl",
          "description": "A mobile-optimized version of the display control component. It adapts the chart settings functionality for smaller screens, often using icons or condensed menus.",
          "installation": "npm install @orderly.network/ui-tradingview",
          "usage": "Implement in the mobile view of your trading interface to save space while retaining chart customization capabilities.",
          "example": "import React from \"react\";\nimport { MobileDisplayControl } from \"@orderly.network/ui-tradingview\";\n\nexport const MobileDisplayControlExample = () => {\n  return (\n    <div className=\"flex justify-between p-2\">\n      <MobileDisplayControl\n        // Optimized for touch interactions and smaller screens\n        onSettingsClick={() => console.log(\"Open Settings\")}\n      />\n    </div>\n  );\n};",
          "notes": [
            "Ensure responsive layout handling when switching between Desktop and Mobile versions.",
            "Touch-friendly targets."
          ],
          "related": ["DesktopDisplayControl", "MobileTimeInterval", "TradingView"]
        },
        {
          "name": "TimeInterval",
          "description": "A component that allows users to select the time interval (timeframe) for the chart, such as 1 minute, 1 hour, or 1 day.",
          "installation": "npm install @orderly.network/ui-tradingview",
          "usage": "Standard component for the top toolbar of a trading chart to switch between different analysis timeframes.",
          "example": "import React, { useState } from \"react\";\nimport { TimeInterval } from \"@orderly.network/ui-tradingview\";\n\nexport const TimeIntervalExample = () => {\n  const [interval, setInterval] = useState(\"15\");\n\n  return (\n    <TimeInterval\n      value={interval}\n      onIntervalChange={(newInterval) => setInterval(newInterval)}\n      // Available intervals usually include '1', '5', '15', '60', '240', 'D'\n    />\n  );\n};",
          "notes": [
            "Usually syncs with the TradingView widget's chart resolution.",
            "Visual style typically matches the active theme."
          ],
          "related": ["MobileTimeInterval", "DesktopDisplayControl", "TradingView"]
        },
        {
          "name": "MobileTimeInterval",
          "description": "A mobile-adapted selector for chart time intervals. It may render as a horizontal scrollable list or a dropdown to accommodate mobile UI constraints.",
          "installation": "npm install @orderly.network/ui-tradingview",
          "usage": "Used in mobile trading views to allow timeframe switching without consuming excessive screen real estate.",
          "example": "import React, { useState } from \"react\";\nimport { MobileTimeInterval } from \"@orderly.network/ui-tradingview\";\n\nexport const MobileTimeIntervalExample = () => {\n  const [interval, setInterval] = useState(\"1H\");\n\n  return (\n    <MobileTimeInterval\n      value={interval}\n      onIntervalChange={(newInterval) => setInterval(newInterval)}\n    />\n  );\n};",
          "notes": [
            "Optimized for touch scrolling or tapping.",
            "Often displays fewer interval options than desktop by default."
          ],
          "related": ["TimeInterval", "MobileDisplayControl", "TradingView"]
        },
        {
          "name": "ZoomOutIcon",
          "description": "A simple icon component representing a 'Zoom Out' action. It is typically used within chart toolbars to trigger a zoom-out event on the TradingView chart.",
          "installation": "npm install @orderly.network/ui-tradingview",
          "usage": "Place inside a chart control bar. Attach an `onClick` handler that calls the TradingView widget's `execAction()` method or similar zoom logic.",
          "example": "import React from \"react\";\nimport { ZoomOutIcon } from \"@orderly.network/ui-tradingview\";\n\nexport const ZoomOutIconExample = () => {\n  return (\n    <div className=\"chart-toolbar\">\n      <ZoomOutIcon\n        className=\"text-gray-400 hover:text-white cursor-pointer transition-colors\"\n        onClick={() => console.log(\"Zoom Out\")}\n      />\n    </div>\n  );\n};",
          "notes": [
            "Internal usage shows it accepts standard className props for styling.",
            "Usually part of a set of zoom controls (ZoomIn, Reset)."
          ],
          "related": ["DesktopDisplayControl", "MobileDisplayControl", "TradingView"]
        },
        {
          "name": "ZoomInIcon",
          "description": "A presentational icon component used within trading chart interfaces to trigger a zoom-in action. It typically inherits text colors for styling and handles hover states.",
          "installation": "npm install @orderly.network/ui-tradingview",
          "usage": "Use this in chart toolbars or control panels where users need to magnify the view of a trading chart.",
          "example": "import React from \"react\";\nimport { ZoomInIcon } from \"@orderly.network/ui-tradingview\";\n\nexport const ChartControls = () => {\n  return (\n    <div className=\"flex items-center gap-2\">\n      <ZoomInIcon\n        className=\"oui-text-base-contrast-54 hover:oui-text-base-contrast oui-cursor-pointer transition-colors\"\n        onClick={() => console.log(\"Zoom In clicked\")}\n      />\n    </div>\n  );\n};",
          "notes": [
            "Accepts standard SVG/HTML attributes like className and onClick.",
            "Often paired with ZoomOutIcon.",
            "Uses Orderly utility classes (oui-*) for consistent theming."
          ],
          "related": ["ZoomOutIcon", "TradingView", "ChartToolbar"]
        },
        {
          "name": "LtvUI",
          "description": "A UI component designed to display the Loan-to-Value (LTV) ratio. It provides a visual representation of the user's current debt relative to their collateral.",
          "installation": "npm install @orderly.network/ui-transfer",
          "usage": "Ideal for dashboard cards or margin account summaries where users need a quick check of their liquidation risk.",
          "example": "import React from \"react\";\nimport { LtvUI } from \"@orderly.network/ui-transfer\";\n\nexport const MarginInfo = () => {\n  return (\n    <div className=\"p-4 border rounded\">\n      <h3 className=\"text-sm font-medium mb-2\">Loan to Value</h3>\n      {/* Displays the current LTV ratio visually or textually */}\n      <LtvUI \n        value={15.5} \n        max={20} \n        className=\"w-full\" \n      />\n    </div>\n  );\n};",
          "notes": [
            "Primarily a display component, not interactive.",
            "Props typically include the current value and the maximum safe threshold.",
            "May render a progress bar or colored text based on risk level."
          ],
          "related": ["LtvWidget", "CollateralRatioUI", "MarginHealthBar"]
        },
        {
          "name": "LtvWidget",
          "description": "An advanced widget for displaying Loan-to-Value that supports showing projections or differences (e.g., how LTV changes after a borrow or repay action).",
          "installation": "npm install @orderly.network/ui-transfer",
          "usage": "Use in modals or forms where the user is adjusting their position (borrowing/repaying) and needs to see the impact on their LTV in real-time.",
          "example": "import React, { useState } from \"react\";\nimport { LtvWidget } from \"@orderly.network/ui-transfer\";\n\nexport const LtvCalculator = () => {\n  const [quantity, setQuantity] = useState<string>(\"\");\n\n  return (\n    <div className=\"bg-slate-800 p-4 rounded-lg\">\n      <LtvWidget\n        // Shows the difference between current and projected LTV\n        showDiff={typeof quantity !== \"undefined\" && Number(quantity) > 0}\n        currentLtv={10.5}\n        projectedLtv={12.0}\n        maxLtv={20}\n      />\n    </div>\n  );\n};",
          "notes": [
            "The 'showDiff' prop controls whether the delta between current and new LTV is visible.",
            "Essential for risk management during transfer or trading operations.",
            "Often updates dynamically based on input fields."
          ],
          "related": ["LtvUI", "CollateralContribution", "BorrowRepayForm"]
        },
        {
          "name": "AccountSelect",
          "description": "A dropdown selector component allowing users to choose between different accounts (e.g., Trading Account, sub-accounts) for transfers or allocations.",
          "installation": "npm install @orderly.network/ui-transfer",
          "usage": "Used in transfer flows to select the source or destination of funds.",
          "example": "import React, { useState } from \"react\";\nimport { AccountSelect } from \"@orderly.network/ui-transfer\";\n\nexport const TransferDestination = () => {\n  const [toAccount, setToAccount] = useState<string>(\"\");\n\n  const subAccounts = [\n    { accountId: \"account_1\", label: \"Trading Account\" },\n    { accountId: \"account_2\", label: \"Future Account\" },\n  ];\n\n  return (\n    <div className=\"flex flex-col gap-2\">\n      <label className=\"text-xs text-gray-400\">Destination Account</label>\n      <AccountSelect\n        subAccounts={subAccounts}\n        value={toAccount}\n        onValueChange={(val) => setToAccount(val)}\n        placeholder=\"Select account\"\n      />\n    </div>\n  );\n};",
          "notes": [
            "Requires an array of account objects for 'subAccounts'.",
            "Controlled component: requires 'value' and 'onValueChange' props.",
            "Integrates with the broader transfer state management."
          ],
          "related": ["ChainSelect", "TokenSelect", "TransferForm"]
        },
        {
          "name": "ActionButton",
          "description": "A multi-purpose button that adapts its appearance and behavior based on the user's state (connected, trading enabled) or the specific action being performed (deposit, withdraw).",
          "installation": "npm install @orderly.network/ui-transfer",
          "usage": "The primary call-to-action button in transfer modals and trading forms.",
          "example": "import React from \"react\";\nimport { ActionButton } from \"@orderly.network/ui-transfer\";\n\nexport const TransferActions = () => {\n  const handleSignIn = () => console.log(\"Sign in triggered\");\n  const handleEnableTrading = () => console.log(\"Trading enabled\");\n\n  return (\n    <div className=\"flex flex-col gap-3\">\n      {/* Example 1: Wallet Connection State */}\n      <ActionButton\n        state=\"not_connected\"\n        signIn={handleSignIn}\n      />\n\n      {/* Example 2: Trading Action */}\n      <ActionButton\n        actionType=\"deposit\"\n        symbol=\"USDC\"\n        disabled={false}\n        onClick={() => console.log(\"Deposit clicked\")}\n      />\n    </div>\n  );\n};",
          "notes": [
            "Handles complex logic internally, such as checking if trading is enabled.",
            "Props like 'actionType' and 'symbol' customize the button text and icon.",
            "Can be disabled based on validation logic."
          ],
          "related": ["TransferButton", "ConnectButton", "WalletProvider"]
        },
        {
          "name": "ChainSelect",
          "description": "A dropdown component for selecting the blockchain network. Used to filter assets or set the destination for cross-chain operations.",
          "installation": "npm install @orderly.network/ui-transfer",
          "usage": "Essential in cross-chain transfer interfaces or when filtering assets by network.",
          "example": "import React, { useState } from \"react\";\nimport { ChainSelect } from \"@orderly.network/ui-transfer\";\n\nexport const NetworkSelector = () => {\n  const [currentChain, setCurrentChain] = useState(\"Arbitrum\");\n\n  const chains = [\n    { chainId: 42161, name: \"Arbitrum\" },\n    { chainId: 1, name: \"Ethereum\" },\n    { chainId: 137, name: \"Polygon\" },\n  ];\n\n  return (\n    <ChainSelect\n      chains={chains}\n      value={currentChain}\n      onValueChange={setCurrentChain}\n    />\n  );\n};",
          "notes": [
            "Expects an array of chain objects containing 'chainId' and 'name'.",
            "Controlled component requiring 'value' and 'onValueChange'.",
            "Usually displays network icons alongside names."
          ],
          "related": ["AccountSelect", "TokenSelect", "CrossChainTransfer"]
        },
        {
          "name": "CollateralContribution",
          "description": "An input field specifically designed for entering collateral amounts. It handles precision formatting (e.g., for USDC) and validation against available balances.",
          "installation": "npm install @orderly.network/ui-transfer",
          "usage": "Used in margin management forms where users add or remove collateral to their positions.",
          "example": "import React, { useState } from \"react\";\nimport { CollateralContribution } from \"@orderly.network/ui-transfer\";\n\nexport const CollateralInput = () => {\n  const [value, setValue] = useState(\"\");\n  const usdcToken = { precision: 6, symbol: \"USDC\" };\n\n  return (\n    <div className=\"p-4 border rounded\">\n      <label className=\"block text-sm mb-2\">Add Collateral</label>\n      <CollateralContribution\n        // Enforces USDC precision (6 decimals)\n        precision={usdcToken?.precision ?? 6}\n        value={value}\n        onChange={setValue}\n        max={1000}\n        symbol=\"USDC\"\n      />\n    </div>\n  );\n};",
          "notes": [
            "Takes a 'precision' prop to ensure decimal accuracy for specific tokens.",
            "Often includes a 'Max' button functionality internally.",
            "Visual feedback for insufficient balance."
          ],
          "related": ["LtvWidget", "TransferInput", "MarginAccount"]
        },
        {
          "name": "CollateralRatioUI",
          "description": "A UI component that displays the collateral ratio, indicating the health of a margin account. It often changes color based on whether the ratio is safe or close to liquidation.",
          "installation": "npm install @orderly.network/ui-transfer",
          "usage": "Used in account summaries and risk panels to give users an immediate sense of their account safety.",
          "example": "import React from \"react\";\nimport { CollateralRatioUI } from \"@orderly.network/ui-transfer\";\n\nexport const RatioDisplay = () => {\n  return (\n    <div className=\"flex justify-between items-center\">\n      <span className=\"text-gray-400\">Collateral Ratio</span>\n      <CollateralRatioUI \n        value={150} \n        suffix=\"%\" \n        status=\"healthy\" \n      />\n    </div>\n  );\n};",
          "notes": [
            "Display-only component.",
            "Likely accepts a 'status' prop (e.g., 'safe', 'warning', 'danger') to trigger color changes.",
            "Complements LtvUI by showing the inverse metric."
          ],
          "related": ["LtvUI", "LtvWidget", "AccountHealth"]
        },
        {
          "name": "CollateralRatioWidget",
          "description": "A visual widget that displays the user's collateral ratio (health factor). It typically visualizes the ratio as a progress bar or gauge, indicating how close the account is to liquidation.",
          "installation": "npm install @orderly.network/ui-transfer",
          "usage": "Use this in portfolio or account overview sections to give users a quick visual indication of their margin account health.",
          "example": "import React from \"react\";\nimport { CollateralRatioWidget } from \"@orderly.network/ui-transfer\";\n\nexport const CollateralExample = () => {\n  // Example: Calculate or fetch the current collateral ratio\n  const collateralRatio = 1.5; // 150%\n\n  return (\n    <div className=\"p-4 border rounded\">\n      <h3 className=\"mb-2 text-sm font-semibold\">Account Health</h3>\n      <CollateralRatioWidget value={collateralRatio} />\n    </div>\n  );\n};",
          "notes": [
            "The value prop usually expects a number (e.g., 1.5 for 150%).",
            "The component handles the visual color coding (e.g., green for safe, red for danger) based on the value internally."
          ],
          "related": ["AccountSummary", "MarginInfo", "DepositStatusWidget"]
        },
        {
          "name": "ConvertAction",
          "description": "A button component specifically designed for triggering token conversions or swaps. It handles loading and disabled states visually.",
          "installation": "npm install @orderly.network/ui-transfer",
          "usage": "Place this at the bottom of a conversion form. It connects the user's intent to the conversion logic.",
          "example": "import React, { useState } from \"react\";\nimport { ConvertAction } from \"@orderly.network/ui-transfer\";\n\nexport const ConvertButtonExample = () => {\n  const [loading, setLoading] = useState(false);\n  const networkId = 9000; // Example Orderly Network ID\n\n  const handleConvert = async () => {\n    setLoading(true);\n    // Execute conversion logic here\n    setTimeout(() => {\n      setLoading(false);\n      alert(\"Conversion complete!\");\n    }, 2000);\n  };\n\n  return (\n    <ConvertAction\n      networkId={networkId}\n      disabled={loading}\n      loading={loading}\n      onConvert={handleConvert}\n    />\n  );\n};",
          "notes": [
            "Requires the `networkId` to ensure the transaction is broadcast to the correct chain.",
            "The `loading` prop automatically disables the button and shows a spinner."
          ],
          "related": ["ConvertFormWidget", "ConvertFormUI", "TokenSelect"]
        },
        {
          "name": "ConvertFormUI",
          "description": "A 'dumb' UI component that renders the inputs for a token conversion. It manages the layout of 'From' and 'To' fields but does not handle price fetching or transaction execution logic itself.",
          "installation": "npm install @orderly.network/ui-transfer",
          "usage": "Use this when you need full control over the state management and logic of the conversion form, but want to use Orderly's standard design.",
          "example": "import React, { useState } from \"react\";\nimport { ConvertFormUI } from \"@orderly.network/ui-transfer\";\n\nexport const ConvertFormUIExample = () => {\n  const [fromAmount, setFromAmount] = useState(\"\");\n  const [toAmount, setToAmount] = useState(\"\");\n\n  return (\n    <div className=\"w-full max-w-md\">\n      <ConvertFormUI\n        fromToken={{ symbol: \"USDC\", decimals: 6 }}\n        toToken={{ symbol: \"USDT\", decimals: 6 }}\n        fromAmount={fromAmount}\n        toAmount={toAmount}\n        onFromAmountChange={setFromAmount}\n        onToAmountChange={setToAmount}\n        onSwitchTokens={() => console.log(\"Switch clicked\")}\n      />\n    </div>\n  );\n};",
          "notes": [
            "This is a presentational component. You must manage the state (amounts, selected tokens) in the parent component.",
            "Requires props for token metadata (symbol, decimals) and current amounts."
          ],
          "related": ["ConvertFormWidget", "ConvertAction", "TokenInput"]
        },
        {
          "name": "ConvertFormWidget",
          "description": "A fully functional 'smart' widget for token conversion. It wraps the ConvertFormUI and handles internal state, price fetching, and validation automatically.",
          "installation": "npm install @orderly.network/ui-transfer",
          "usage": "The quickest way to add conversion functionality to your app without building the underlying logic.",
          "example": "import React from \"react\";\nimport { ConvertFormWidget } from \"@orderly.network/ui-transfer\";\n\nexport const ConvertFormWidgetExample = () => {\n  const handleSuccess = (data) => {\n    console.log(\"Conversion successful:\", data);\n  };\n\n  return (\n    <div className=\"w-full max-w-md\">\n      <ConvertFormWidget\n        fromToken=\"USDC\"\n        toToken=\"USDT\"\n        onSuccess={handleSuccess}\n      />\n    </div>\n  );\n};",
          "notes": [
            "Handles API calls to fetch conversion rates internally.",
            "Takes initial token symbols or addresses as props to pre-fill the form."
          ],
          "related": ["ConvertFormUI", "ConvertAction", "MinimumReceived"]
        },
        {
          "name": "DepositStatus",
          "description": "A presentational component that displays the status of a deposit transaction (e.g., Pending, Success, Failed).",
          "installation": "npm install @orderly.network/ui-transfer",
          "usage": "Use this inside modals or dedicated status pages to inform the user about the progress of their transfer.",
          "example": "import React from \"react\";\nimport { DepositStatus } from \"@orderly.network/ui-transfer\";\n\nexport const DepositStatusExample = () => {\n  return (\n    <div className=\"p-4\">\n      <DepositStatus \n        status=\"success\" \n        txHash=\"0x123...abc\" \n        message=\"Deposit confirmed on chain\" \n      />\n      <br />\n      <DepositStatus \n        status=\"pending\" \n        message=\"Waiting for block confirmation...\" \n      />\n    </div>\n  );\n};",
          "notes": [
            "Accepts a status string/enum to determine the icon and color scheme.",
            "Does not fetch status; it only displays what is passed to it."
          ],
          "related": ["DepositStatusWidget", "DepositProvider", "ConvertAction"]
        },
        {
          "name": "DepositStatusWidget",
          "description": "A 'smart' widget that tracks and displays the status of an ongoing deposit. It typically includes logic to check transaction status and provides navigation actions.",
          "installation": "npm install @orderly.network/ui-transfer",
          "usage": "Ideal for placing on a dashboard or deposit page where the user needs to see the result of their last action.",
          "example": "import React from \"react\";\nimport { DepositStatusWidget } from \"@orderly.network/ui-transfer\";\n\nexport const DepositStatusWidgetExample = () => {\n  const navigateToPortfolio = () => {\n    console.log(\"Navigating to portfolio page...\");\n  };\n\n  return (\n    <DepositStatusWidget\n      className=\"oui-mt-3\"\n      onClick={navigateToPortfolio}\n    />\n  );\n};",
          "notes": [
            "Often includes a button or link to view the transaction details or portfolio.",
            "Manages its own data fetching regarding the deposit state."
          ],
          "related": ["DepositStatus", "DepositProvider", "CollateralRatioWidget"]
        },
        {
          "name": "MinimumReceived",
          "description": "A small text component that displays the minimum amount a user will receive after a conversion, accounting for slippage.",
          "installation": "npm install @orderly.network/ui-transfer",
          "usage": "Place this near the 'Convert' button to ensure transparency about price impact and slippage tolerance.",
          "example": "import React from \"react\";\nimport { MinimumReceived } from \"@orderly.network/ui-transfer\";\n\nexport const MinimumReceivedExample = () => {\n  // Example values calculated from a swap\n  const estimatedOutput = 0.045;\n  const targetToken = { symbol: \"ETH\", precision: 18 };\n\n  return (\n    <div className=\"text-sm text-gray-500\">\n      <MinimumReceived\n        value={estimatedOutput}\n        symbol={targetToken.symbol}\n        precision={targetToken.precision}\n      />\n    </div>\n  );\n};",
          "notes": [
            "Requires the token symbol and precision to format the number correctly.",
            "Helps meet UI requirements for decentralized exchange interfaces."
          ],
          "related": ["ConvertFormWidget", "ConvertAction", "SlippageSettings"]
        },
        {
          "name": "DepositProvider",
          "description": "A React Context Provider that supplies deposit-related state and functionality (like checking balances, initiating transfers) to its children components.",
          "installation": "npm install @orderly.network/ui-transfer",
          "usage": "Wrap your application or a specific section of your trading interface with this provider to enable deposit features in child widgets.",
          "example": "import React from \"react\";\nimport { DepositProvider, DepositStatusWidget } from \"@orderly.network/ui-transfer\";\n\nconst AppContent = () => {\n  return (\n    <div>\n      <h1>My Trading App</h1>\n      {/* Other components */}\n      <DepositStatusWidget />\n    </div>\n  );\n};\n\nexport const App = () => {\n  return (\n    <DepositProvider>\n      <AppContent />\n    </DepositProvider>\n  );\n};",
          "notes": [
            "Necessary for 'smart' widgets like DepositStatusWidget to function correctly.",
            "Abstracts away the complex logic of interacting with broker wallets and the Orderly chain."
          ],
          "related": ["DepositStatusWidget", "WithdrawalWidget", "AccountProvider"]
        },
        {
          "name": "TokenOption",
          "description": "A selectable list item component used within token input fields or dropdowns. It displays the token's icon, symbol, and optionally fetches and displays the user's balance.",
          "installation": "npm install @orderly.network/ui-transfer",
          "usage": "Use this component when building a custom token selector dropdown or a list of available assets for transfer/swap. It handles the visual distinction between active (selected) and inactive states.",
          "example": "import React from \"react\";\nimport { TokenOption } from \"@orderly.network/ui-transfer\";\n\nconst MyTokenList = () => {\n  // Mock token data structure typically expected by Orderly\n  const token = {\n    symbol: \"ETH\",\n    name: \"Ethereum\",\n    logo_uri: \"/eth.png\",\n    decimals: 18,\n    address: \"0x...\"\n  };\n\n  const handleFetchBalance = async () => {\n    // Logic to fetch balance from SDK or API\n    return \"1.5\";\n  };\n\n  return (\n    <div className=\"token-list\">\n      {/* Active State */}\n      <TokenOption\n        token={token}\n        isActive={true}\n        fetchBalance={handleFetchBalance}\n        displayType=\"balance\"\n        onClick={() => console.log(\"Selected ETH\")}\n      />\n\n      {/* Inactive State */}\n      <TokenOption\n        token={{ ...token, symbol: \"USDC\", name: \"USD Coin\" }}\n        isActive={false}\n        fetchBalance={handleFetchBalance}\n        displayType=\"balance\"\n        onClick={() => console.log(\"Selected USDC\")}\n      />\n    </div>\n  );\n};\n\nexport default MyTokenList;",
          "notes": [
            "The `displayType` prop controls whether the balance is shown or hidden.",
            "Ensure the `token` object follows the standard Orderly `Token` interface.",
            "The `fetchBalance` function is asynchronous and should return a string representation of the balance."
          ],
          "related": ["TokenSelect", "QuantityInput", "useTokenBalance"]
        },
        {
          "name": "SwapDepositProvider",
          "description": "A Context Provider that manages the state and logic for Swapping and Depositing assets. It wraps child components to provide them with necessary data like selected tokens, amounts, and transaction handlers.",
          "installation": "npm install @orderly.network/ui-transfer",
          "usage": "Wrap your main transfer or swap interface component with this provider. It is essential for enabling the internal logic of swap and deposit forms within the `@orderly.network/ui-transfer` ecosystem.",
          "example": "import React from \"react\";\nimport { SwapDepositProvider, SwapForm } from \"@orderly.network/ui-transfer\";\n\nconst App = () => {\n  return (\n    <SwapDepositProvider\n      // Optional: Configuration for the provider context\n      defaultSourceToken=\"USDC\"\n      defaultTargetToken=\"ETH\"\n    >\n      <div className=\"my-swap-container\">\n        <h2>Swap or Deposit</h2>\n        {/* Child components can now consume the context */}\n        <SwapForm />\n      </div>\n    </SwapDepositProvider>\n  );\n};\n\nexport default App;",
          "notes": [
            "This component does not render any UI itself; it acts as a state container.",
            "It is required when using complex swap components that rely on shared context.",
            "Place it high in the component tree to ensure all child components have access to token state."
          ],
          "related": ["SwapForm", "DepositForm", "useSwapContext"]
        },
        {
          "name": "SwapIndicator",
          "description": "A visual UI component that indicates the direction of a swap between two tokens. It typically renders an arrow or icon and allows the user to click to switch the source and target tokens.",
          "installation": "npm install @orderly.network/ui-transfer",
          "usage": "Place this component between the 'From' and 'To' input fields in a swap interface. It provides visual feedback and interaction for reversing the trade pair.",
          "example": "import React from \"react\";\nimport { SwapIndicator } from \"@orderly.network/ui-transfer\";\n\nconst SwapView = () => {\n  return (\n    <div className=\"flex items-center gap-4\">\n      <div className=\"source-token-box\">USDT</div>\n\n      {/* Visual indicator showing the swap direction */}\n      <SwapIndicator\n        sourceToken=\"USDT\"\n        targetToken=\"PERP\"\n        className=\"my-custom-indicator\"\n        onSwap={() => console.log('Swap direction toggled')}\n      />\n\n      <div className=\"target-token-box\">PERP</div>\n    </div>\n  );\n};\n\nexport default SwapView;",
          "notes": [
            "The `sourceToken` and `targetToken` props are usually strings representing symbols (e.g., 'ETH').",
            "You can pass an `onSwap` callback to handle the logic of switching token values in your parent state.",
            "Styling can be customized via the `className` prop."
          ],
          "related": ["TokenOption", "SwapForm", "QuantityInput"]
        },
        {
          "name": "WithdrawForm",
          "description": "A comprehensive form component designed for handling withdrawals. It includes inputs for the amount, destination address, and chain selection, along with validation and submission logic.",
          "installation": "npm install @orderly.network/ui-transfer",
          "usage": "Use this component to create a withdrawal interface. It encapsulates the complexity of gathering withdrawal data from the user, including network selection and address validation.",
          "example": "import React, { useState } from \"react\";\nimport { WithdrawForm } from \"@orderly.network/ui-transfer\";\n\nconst WithdrawPage = () => {\n  const [selectedToken, setSelectedToken] = useState({\n    symbol: \"USDC\",\n    decimals: 6,\n    address: \"0x...\"\n  });\n\n  const handleWithdrawSubmit = (data) => {\n    console.log(\"Withdraw request:\", data);\n    // Execute withdrawal logic here\n  };\n\n  return (\n    <div className=\"withdraw-container\">\n      <h3>Withdraw Funds</h3>\n      <WithdrawForm\n        token={selectedToken}\n        onSubmit={handleWithdrawSubmit}\n        // Custom UI props if supported by the specific version\n        showAddressBook={true}\n        chains={[{ id: 1, name: \"Ethereum\" }, { id: 56, name: \"BSC\" }]}\n      />\n    </div>\n  );\n};\n\nexport default WithdrawPage;",
          "notes": [
            "Requires a `token` prop to know which asset is being withdrawn.",
            "The `onSubmit` prop receives the form data (amount, address, chainId) for processing.",
            "Ensure the user has sufficient balance before rendering, or handle errors via the form's feedback mechanisms."
          ],
          "related": ["WithdrawWarningMessage", "TokenOption", "useWithdrawal"]
        },
        {
          "name": "WithdrawWarningMessage",
          "description": "A conditional warning component that displays messages to the user based on the state of their withdrawal request. It checks for conditions like bridgeless transfers, cross-chain transactions, or excessive quantities.",
          "installation": "npm install @orderly.network/ui-transfer",
          "usage": "Integrate this component near the submit button of a `WithdrawForm`. It helps prevent user error by warning about network fees, transfer times, or balance issues before the transaction is confirmed.",
          "example": "import React from \"react\";\nimport { WithdrawWarningMessage } from \"@orderly.network/ui-transfer\";\n\nconst WithdrawFeedback = () => {\n  // Scenario: User is trying to withdraw a large amount cross-chain\n  const isBridgeless = false; // Simulating a check\n  const isCrossChain = true;\n  const isQtyTooHigh = true;\n\n  return (\n    <div className=\"withdraw-section\">\n      {/* Form inputs would go here */}\n      \n      <WithdrawWarningMessage\n        checkIsBridgeless={isBridgeless}\n        crossChainTrans={isCrossChain}\n        qtyGreaterThan={isQtyTooHigh}\n      />\n    </div>\n  );\n};\n\nexport default WithdrawFeedback;",
          "notes": [
            "Props act as boolean flags to trigger specific warning messages.",
            "Useful for UX safety, alerting users when they are performing cross-chain transfers which may take longer or cost more.",
            "The component automatically determines which message to show based on the combination of props passed."
          ],
          "related": ["WithdrawForm", "TransferWarning", "useAccount"]
        }
      ]
    }
  ]
}
